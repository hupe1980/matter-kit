//! Differential tests between `matter-kit` and `rs-matter`.
//!
//! The library half is empty on purpose: everything lives in `tests/`, because there is nothing
//! here to depend on — only comparisons to run.
