// Generated by `cargo xtask` from the CSA data model 1.6. Do not edit.
//
// ICD Management — cluster 0x0046.
//
// Regenerate with `cargo xtask clusters --dm <checkout>/data_model/1.6`; `cargo xtask check`
// is the CI gate that keeps this file honest. The XML itself is not vendored here — it
// carries the CSA's own copyright notice and is not redistributable.

//! ICD Management (cluster `0x0046`, revision 3).
//!
//! 15 identifiers, 4 feature bits, and the specification's own conformance for
//! every element. `CLUSTER` is the whole table.

#![allow(unused_imports)]

use crate::dm::conformance::{Clause, Condition, Conform, Conformance};
use crate::dm::meta::{EventPriority, Reporting};
use crate::dm::spec::{Attribute, Cluster, Command, Event, Feature};
use crate::dm::{Access, AccessQualities, AttributeQualities, Privilege};
use crate::error::Result;
use crate::im::{AttributeId, ClusterId, CommandId, EventId};
use crate::tlv::{
    ContainerKind, Element, FromTlv, Nullable, Tag, TlvList, TlvReader, TlvWriter, ToTlv, Value,
};

/// The cluster id.
pub const ID: ClusterId = 0x0046;

/// The highest revision the specification's history defines — what `ClusterRevision`
/// must report (Core §7.13.1).
pub const REVISION: u16 = 3;

/// The PICS code, which a certification declaration names the cluster by.
pub const PICS: &str = "ICDM";

/// The `FeatureMap` bits (Core §7.13.2).
///
/// A feature has conformance of its own: a device may not claim one the
/// specification forbids it alongside another.
pub mod feature {
    /// Device supports attributes and commands for the Check-In Protocol support.
    ///
    /// Code `CIP`, bit 0.
    pub const CHECK_IN_PROTOCOL_SUPPORT: u32 = 1 << 0;
    /// Device supports the user active mode trigger feature.
    ///
    /// Code `UAT`, bit 1.
    pub const USER_ACTIVE_MODE_TRIGGER: u32 = 1 << 1;
    /// Device supports operating as a Long Idle Time ICD.
    ///
    /// Code `LITS`, bit 2.
    pub const LONG_IDLE_TIME_SUPPORT: u32 = 1 << 2;
    /// Device supports dynamic switching from SIT to LIT operating modes.
    ///
    /// Code `DSLS`, bit 3.
    pub const DYNAMIC_SIT_LIT_SUPPORT: u32 = 1 << 3;
}

/// Every feature bit, with its conformance.
pub const FEATURES: &[Feature] = &[
    Feature {
        bit: 0,
        code: "CIP",
        name: "CheckInProtocolSupport",
        conform: Conform {
            clauses: &[
                Clause {
                    verdict: Conformance::Mandatory,
                    when: Condition::Feature(2),
                },
                Clause {
                    verdict: Conformance::Optional,
                    when: Condition::Always,
                },
            ],
        },
    },
    Feature {
        bit: 1,
        code: "UAT",
        name: "UserActiveModeTrigger",
        conform: Conform {
            clauses: &[
                Clause {
                    verdict: Conformance::Mandatory,
                    when: Condition::Feature(2),
                },
                Clause {
                    verdict: Conformance::Optional,
                    when: Condition::Always,
                },
            ],
        },
    },
    Feature {
        bit: 2,
        code: "LITS",
        name: "LongIdleTimeSupport",
        conform: Conform::OPTIONAL,
    },
    Feature {
        bit: 3,
        code: "DSLS",
        name: "DynamicSitLitSupport",
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Optional,
                when: Condition::Feature(2),
            }],
        },
    },
];

/// The attribute ids.
pub mod attribute {
    use crate::im::AttributeId;

    /// `IdleModeDuration`.
    pub const IDLE_MODE_DURATION: AttributeId = 0x00;
    /// `ActiveModeDuration`.
    pub const ACTIVE_MODE_DURATION: AttributeId = 0x01;
    /// `ActiveModeThreshold`.
    pub const ACTIVE_MODE_THRESHOLD: AttributeId = 0x02;
    /// `RegisteredClients`.
    pub const REGISTERED_CLIENTS: AttributeId = 0x03;
    /// `ICDCounter`.
    pub const ICD_COUNTER: AttributeId = 0x04;
    /// `ClientsSupportedPerFabric`.
    pub const CLIENTS_SUPPORTED_PER_FABRIC: AttributeId = 0x05;
    /// `UserActiveModeTriggerHint`.
    pub const USER_ACTIVE_MODE_TRIGGER_HINT: AttributeId = 0x06;
    /// `UserActiveModeTriggerInstruction`.
    pub const USER_ACTIVE_MODE_TRIGGER_INSTRUCTION: AttributeId = 0x07;
    /// `OperatingMode`.
    pub const OPERATING_MODE: AttributeId = 0x08;
    /// `MaximumCheckInBackoff`.
    pub const MAXIMUM_CHECK_IN_BACKOFF: AttributeId = 0x09;
}

/// The command ids.
pub mod command {
    use crate::im::CommandId;

    /// `RegisterClient`.
    pub const REGISTER_CLIENT: CommandId = 0x00;
    /// `RegisterClientResponse`.
    pub const REGISTER_CLIENT_RESPONSE: CommandId = 0x01;
    /// `UnregisterClient`.
    pub const UNREGISTER_CLIENT: CommandId = 0x02;
    /// `StayActiveRequest`.
    pub const STAY_ACTIVE_REQUEST: CommandId = 0x03;
    /// `StayActiveResponse`.
    pub const STAY_ACTIVE_RESPONSE: CommandId = 0x04;
}

/// `ClientTypeEnum`.
#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash)]
#[repr(u8)]
pub enum ClientTypeEnum {
    /// The client is typically resident, always-on, fixed infrastructure in the home.
    Permanent = 0,
    /// The client is mobile or non-resident or not always-on and may not always be available in the home.
    Ephemeral = 1,
}

impl ClientTypeEnum {
    /// Reads a wire value, or `None` for one this revision does not define.
    #[must_use]
    pub const fn from_value(value: u8) -> Option<Self> {
        match value {
            0 => Some(Self::Permanent),
            1 => Some(Self::Ephemeral),
            _ => None,
        }
    }

    /// Its wire value.
    #[must_use]
    pub const fn value(self) -> u8 {
        self as u8
    }
}

impl ToTlv for ClientTypeEnum {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        self.value().to_tlv(w, tag)
    }
}

impl<'a> FromTlv<'a> for ClientTypeEnum {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        let value = <u8 as FromTlv<'a>>::from_tlv(reader, element)?;
        Self::from_value(value).ok_or(crate::Error::new(crate::ErrorCode::TlvOutOfRange))
    }
}

/// `OperatingModeEnum`.
#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash)]
#[repr(u8)]
pub enum OperatingModeEnum {
    /// ICD is operating as a Short Idle Time ICD.
    SIT = 0,
    /// ICD is operating as a Long Idle Time ICD.
    LIT = 1,
}

impl OperatingModeEnum {
    /// Reads a wire value, or `None` for one this revision does not define.
    #[must_use]
    pub const fn from_value(value: u8) -> Option<Self> {
        match value {
            0 => Some(Self::SIT),
            1 => Some(Self::LIT),
            _ => None,
        }
    }

    /// Its wire value.
    #[must_use]
    pub const fn value(self) -> u8 {
        self as u8
    }
}

impl ToTlv for OperatingModeEnum {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        self.value().to_tlv(w, tag)
    }
}

impl<'a> FromTlv<'a> for OperatingModeEnum {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        let value = <u8 as FromTlv<'a>>::from_tlv(reader, element)?;
        Self::from_value(value).ok_or(crate::Error::new(crate::ErrorCode::TlvOutOfRange))
    }
}

bitflags::bitflags! {
    /// `UserActiveModeTriggerBitmap`.
    #[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Default)]
    pub struct UserActiveModeTriggerBitmap: u32 {
        /// Power Cycle to transition the device to ActiveMode
        const POWER_CYCLE = 1 << 0;
        /// Settings menu on the device informs how to transition the device to ActiveMode
        const SETTINGS_MENU = 1 << 1;
        /// Custom Instruction on how to transition the device to ActiveMode
        const CUSTOM_INSTRUCTION = 1 << 2;
        /// Device Manual informs how to transition the device to ActiveMode
        const DEVICE_MANUAL = 1 << 3;
        /// Actuate Sensor to transition the device to ActiveMode
        const ACTUATE_SENSOR = 1 << 4;
        /// Actuate Sensor for N seconds to transition the device to ActiveMode
        const ACTUATE_SENSOR_SECONDS = 1 << 5;
        /// Actuate Sensor N times to transition the device to ActiveMode
        const ACTUATE_SENSOR_TIMES = 1 << 6;
        /// Actuate Sensor until light blinks to transition the device to ActiveMode
        const ACTUATE_SENSOR_LIGHTS_BLINK = 1 << 7;
        /// Press Reset Button to transition the device to ActiveMode
        const RESET_BUTTON = 1 << 8;
        /// Press Reset Button until light blinks to transition the device to ActiveMode
        const RESET_BUTTON_LIGHTS_BLINK = 1 << 9;
        /// Press Reset Button for N seconds to transition the device to ActiveMode
        const RESET_BUTTON_SECONDS = 1 << 10;
        /// Press Reset Button N times to transition the device to ActiveMode
        const RESET_BUTTON_TIMES = 1 << 11;
        /// Press Setup Button to transition the device to ActiveMode
        const SETUP_BUTTON = 1 << 12;
        /// Press Setup Button for N seconds to transition the device to ActiveMode
        const SETUP_BUTTON_SECONDS = 1 << 13;
        /// Press Setup Button until light blinks to transition the device to ActiveMode
        const SETUP_BUTTON_LIGHTS_BLINK = 1 << 14;
        /// Press Setup Button N times to transition the device to ActiveMode
        const SETUP_BUTTON_TIMES = 1 << 15;
        /// Press the N Button to transition the device to ActiveMode
        const APP_DEFINED_BUTTON = 1 << 16;
    }
}

impl ToTlv for UserActiveModeTriggerBitmap {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        self.bits().to_tlv(w, tag)
    }
}

impl<'a> FromTlv<'a> for UserActiveModeTriggerBitmap {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        let bits = <u32 as FromTlv<'a>>::from_tlv(reader, element)?;
        Self::from_bits(bits).ok_or(crate::Error::new(crate::ErrorCode::TlvOutOfRange))
    }
}

/// `MonitoringRegistrationStruct`, fabric-scoped — §7.19.1.9's `FabricIndex` is field 254.
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct MonitoringRegistrationStruct {
    /// `CheckInNodeID`, field 1.
    pub check_in_node_id: crate::msg::NodeId,
    /// `MonitoredSubject`, field 2.
    pub monitored_subject: u64,
    /// `ClientType`, field 4.
    pub client_type: ClientTypeEnum,
    /// `FabricIndex`, field 254.
    pub fabric_index: crate::msg::FabricIndex,
}

impl ToTlv for MonitoringRegistrationStruct {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.check_in_node_id.to_tlv(w, Tag::Context(1))?;
        self.monitored_subject.to_tlv(w, Tag::Context(2))?;
        self.client_type.to_tlv(w, Tag::Context(4))?;
        self.fabric_index.to_tlv(w, Tag::Context(254))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for MonitoringRegistrationStruct {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut check_in_node_id = None;
        let mut monitored_subject = None;
        let mut client_type = None;
        let mut fabric_index = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(1) => check_in_node_id = Some(<crate::msg::NodeId>::from_tlv(reader, &field)?),
                Some(2) => monitored_subject = Some(<u64>::from_tlv(reader, &field)?),
                Some(4) => client_type = Some(<ClientTypeEnum>::from_tlv(reader, &field)?),
                Some(254) => {
                    fabric_index = Some(<crate::msg::FabricIndex>::from_tlv(reader, &field)?)
                }
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            check_in_node_id: check_in_node_id
                .ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            monitored_subject: monitored_subject
                .ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            client_type: client_type.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            fabric_index: fabric_index.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// The fields of `RegisterClient` (command `0x00`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct RegisterClientFields<'a> {
    /// `CheckInNodeID`, field 0.
    pub check_in_node_id: crate::msg::NodeId,
    /// `MonitoredSubject`, field 1.
    pub monitored_subject: u64,
    /// `Key`, field 2.
    pub key: &'a [u8],
    /// `VerificationKey`, field 3.
    pub verification_key: Option<&'a [u8]>,
    /// `ClientType`, field 4.
    pub client_type: ClientTypeEnum,
}

impl<'a> ToTlv for RegisterClientFields<'a> {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.check_in_node_id.to_tlv(w, Tag::Context(0))?;
        self.monitored_subject.to_tlv(w, Tag::Context(1))?;
        self.key.to_tlv(w, Tag::Context(2))?;
        if let Some(value) = &self.verification_key {
            value.to_tlv(w, Tag::Context(3))?;
        }
        self.client_type.to_tlv(w, Tag::Context(4))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for RegisterClientFields<'a> {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut check_in_node_id = None;
        let mut monitored_subject = None;
        let mut key = None;
        let mut verification_key = None;
        let mut client_type = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => check_in_node_id = Some(<crate::msg::NodeId>::from_tlv(reader, &field)?),
                Some(1) => monitored_subject = Some(<u64>::from_tlv(reader, &field)?),
                Some(2) => key = Some(<&'a [u8]>::from_tlv(reader, &field)?),
                Some(3) => verification_key = Some(<&'a [u8]>::from_tlv(reader, &field)?),
                Some(4) => client_type = Some(<ClientTypeEnum>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            check_in_node_id: check_in_node_id
                .ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            monitored_subject: monitored_subject
                .ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            key: key.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            verification_key,
            client_type: client_type.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// The fields of `RegisterClientResponse` (command `0x01`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct RegisterClientResponseFields {
    /// `ICDCounter`, field 0.
    pub icd_counter: u32,
}

impl ToTlv for RegisterClientResponseFields {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.icd_counter.to_tlv(w, Tag::Context(0))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for RegisterClientResponseFields {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut icd_counter = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => icd_counter = Some(<u32>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            icd_counter: icd_counter.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// The fields of `UnregisterClient` (command `0x02`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct UnregisterClientFields<'a> {
    /// `CheckInNodeID`, field 0.
    pub check_in_node_id: crate::msg::NodeId,
    /// `VerificationKey`, field 1.
    pub verification_key: Option<&'a [u8]>,
}

impl<'a> ToTlv for UnregisterClientFields<'a> {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.check_in_node_id.to_tlv(w, Tag::Context(0))?;
        if let Some(value) = &self.verification_key {
            value.to_tlv(w, Tag::Context(1))?;
        }
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for UnregisterClientFields<'a> {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut check_in_node_id = None;
        let mut verification_key = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => check_in_node_id = Some(<crate::msg::NodeId>::from_tlv(reader, &field)?),
                Some(1) => verification_key = Some(<&'a [u8]>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            check_in_node_id: check_in_node_id
                .ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            verification_key,
        })
    }
}

/// The fields of `StayActiveRequest` (command `0x03`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct StayActiveRequestFields {
    /// `StayActiveDuration`, field 0.
    pub stay_active_duration: u32,
}

impl ToTlv for StayActiveRequestFields {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.stay_active_duration.to_tlv(w, Tag::Context(0))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for StayActiveRequestFields {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut stay_active_duration = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => stay_active_duration = Some(<u32>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            stay_active_duration: stay_active_duration
                .ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// The fields of `StayActiveResponse` (command `0x04`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct StayActiveResponseFields {
    /// `PromisedActiveDuration`, field 0.
    pub promised_active_duration: u32,
}

impl ToTlv for StayActiveResponseFields {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.promised_active_duration.to_tlv(w, Tag::Context(0))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for StayActiveResponseFields {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut promised_active_duration = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => promised_active_duration = Some(<u32>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            promised_active_duration: promised_active_duration
                .ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// Every attribute the specification defines, with its conformance.
pub const ATTRIBUTES: &[Attribute] = &[
    Attribute {
        id: 0x0000,
        name: "IdleModeDuration",
        kind: "uint32",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::FIXED,
        reporting: Reporting::OnChange,
        conform: Conform::MANDATORY,
    },
    Attribute {
        id: 0x0001,
        name: "ActiveModeDuration",
        kind: "uint32",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::FIXED,
        reporting: Reporting::OnChange,
        conform: Conform::MANDATORY,
    },
    Attribute {
        id: 0x0002,
        name: "ActiveModeThreshold",
        kind: "uint16",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::FIXED,
        reporting: Reporting::OnChange,
        conform: Conform::MANDATORY,
    },
    Attribute {
        id: 0x0003,
        name: "RegisteredClients",
        kind: "list[MonitoringRegistrationStruct]",
        access: Access::read_only(Privilege::Administer)
            .with_qualities(AccessQualities::FABRIC_SCOPED),
        qualities: AttributeQualities::NON_VOLATILE,
        reporting: Reporting::OnChange,
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(0),
            }],
        },
    },
    Attribute {
        id: 0x0004,
        name: "ICDCounter",
        kind: "uint32",
        access: Access::read_only(Privilege::Administer),
        qualities: AttributeQualities::NON_VOLATILE,
        reporting: Reporting::ChangesOmitted,
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(0),
            }],
        },
    },
    Attribute {
        id: 0x0005,
        name: "ClientsSupportedPerFabric",
        kind: "uint16",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::FIXED,
        reporting: Reporting::OnChange,
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(0),
            }],
        },
    },
    Attribute {
        id: 0x0006,
        name: "UserActiveModeTriggerHint",
        kind: "UserActiveModeTriggerBitmap",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::FIXED,
        reporting: Reporting::OnChange,
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(1),
            }],
        },
    },
    Attribute {
        id: 0x0007,
        name: "UserActiveModeTriggerInstruction",
        kind: "string",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::FIXED,
        reporting: Reporting::OnChange,
        conform: Conform::DESCRIBED,
    },
    Attribute {
        id: 0x0008,
        name: "OperatingMode",
        kind: "OperatingModeEnum",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::empty(),
        reporting: Reporting::OnChange,
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(2),
            }],
        },
    },
    Attribute {
        id: 0x0009,
        name: "MaximumCheckInBackoff",
        kind: "uint32",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::FIXED,
        reporting: Reporting::OnChange,
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(0),
            }],
        },
    },
];

/// Every command, with its conformance.
pub const COMMANDS: &[Command] = &[
    Command {
        id: 0x00,
        name: "RegisterClient",
        to_server: true,
        response: Some(0x01),
        access: Access::invoke(Privilege::Manage).with_qualities(AccessQualities::FABRIC_SCOPED),
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(0),
            }],
        },
    },
    Command {
        id: 0x01,
        name: "RegisterClientResponse",
        to_server: false,
        response: None,
        access: Access::invoke(Privilege::Operate),
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(0),
            }],
        },
    },
    Command {
        id: 0x02,
        name: "UnregisterClient",
        to_server: true,
        response: None,
        access: Access::invoke(Privilege::Manage).with_qualities(AccessQualities::FABRIC_SCOPED),
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(0),
            }],
        },
    },
    Command {
        id: 0x03,
        name: "StayActiveRequest",
        to_server: true,
        response: Some(0x04),
        access: Access::invoke(Privilege::Operate),
        conform: Conform {
            clauses: &[
                Clause {
                    verdict: Conformance::Mandatory,
                    when: Condition::Feature(2),
                },
                Clause {
                    verdict: Conformance::Optional,
                    when: Condition::Always,
                },
            ],
        },
    },
    Command {
        id: 0x04,
        name: "StayActiveResponse",
        to_server: false,
        response: None,
        access: Access::invoke(Privilege::Operate),
        conform: Conform {
            clauses: &[
                Clause {
                    verdict: Conformance::Mandatory,
                    when: Condition::Feature(2),
                },
                Clause {
                    verdict: Conformance::Optional,
                    when: Condition::Always,
                },
            ],
        },
    },
];

/// Every event, with its conformance.
pub const EVENTS: &[Event] = &[];

/// The whole cluster, as the specification defines it.
pub const CLUSTER: Cluster = Cluster {
    id: ID,
    name: "ICD Management",
    revision: REVISION,
    pics: "ICDM",
    derived_from: None,
    features: FEATURES,
    attributes: ATTRIBUTES,
    commands: COMMANDS,
    events: EVENTS,
};
