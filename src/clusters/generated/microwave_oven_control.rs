// Generated by `cargo xtask` from the CSA data model 1.6. Do not edit.
//
// Microwave Oven Control — cluster 0x005F.
//
// Regenerate with `cargo xtask clusters --dm <checkout>/data_model/1.6`; `cargo xtask check`
// is the CI gate that keeps this file honest. The XML itself is not vendored here — it
// carries the CSA's own copyright notice and is not redistributable.

//! Microwave Oven Control (cluster `0x005F`, revision 1).
//!
//! 11 identifiers, 3 feature bits, and the specification's own conformance for
//! every element. `CLUSTER` is the whole table.

#![allow(unused_imports)]

use crate::dm::conformance::{Clause, Condition, Conform, Conformance};
use crate::dm::meta::{EventPriority, Reporting};
use crate::dm::spec::{Attribute, Cluster, Command, Event, Feature};
use crate::dm::{Access, AccessQualities, AttributeQualities, Privilege};
use crate::error::Result;
use crate::im::{AttributeId, ClusterId, CommandId, EventId};
use crate::tlv::{
    ContainerKind, Element, FromTlv, Nullable, Tag, TlvList, TlvReader, TlvWriter, ToTlv, Value,
};

/// The cluster id.
pub const ID: ClusterId = 0x005F;

/// The highest revision the specification's history defines — what `ClusterRevision`
/// must report (Core §7.13.1).
pub const REVISION: u16 = 1;

/// The PICS code, which a certification declaration names the cluster by.
pub const PICS: &str = "MWOCTRL";

/// The `FeatureMap` bits (Core §7.13.2).
///
/// A feature has conformance of its own: a device may not claim one the
/// specification forbids it alongside another.
pub mod feature {
    /// Power is specified as a unitless number or a percentage
    ///
    /// Code `PWRNUM`, bit 0.
    pub const POWER_AS_NUMBER: u32 = 1 << 0;
    /// Power is specified in Watts
    ///
    /// Code `WATTS`, bit 1.
    pub const POWER_IN_WATTS: u32 = 1 << 1;
    /// Supports the limit attributes used with the PWRNUM feature
    ///
    /// Code `PWRLMTS`, bit 2.
    pub const POWER_NUMBER_LIMITS: u32 = 1 << 2;
}

/// Every feature bit, with its conformance.
pub const FEATURES: &[Feature] = &[
    Feature {
        bit: 0,
        code: "PWRNUM",
        name: "PowerAsNumber",
        conform: Conform::OPTIONAL,
    },
    Feature {
        bit: 1,
        code: "WATTS",
        name: "PowerInWatts",
        conform: Conform {
            clauses: &[
                Clause {
                    verdict: Conformance::Provisional,
                    when: Condition::Always,
                },
                Clause {
                    verdict: Conformance::Optional,
                    when: Condition::Always,
                },
            ],
        },
    },
    Feature {
        bit: 2,
        code: "PWRLMTS",
        name: "PowerNumberLimits",
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Optional,
                when: Condition::Feature(0),
            }],
        },
    },
];

/// The attribute ids.
pub mod attribute {
    use crate::im::AttributeId;

    /// `CookTime`.
    pub const COOK_TIME: AttributeId = 0x00;
    /// `MaxCookTime`.
    pub const MAX_COOK_TIME: AttributeId = 0x01;
    /// `PowerSetting`.
    pub const POWER_SETTING: AttributeId = 0x02;
    /// `MinPower`.
    pub const MIN_POWER: AttributeId = 0x03;
    /// `MaxPower`.
    pub const MAX_POWER: AttributeId = 0x04;
    /// `PowerStep`.
    pub const POWER_STEP: AttributeId = 0x05;
    /// `SupportedWatts`.
    pub const SUPPORTED_WATTS: AttributeId = 0x06;
    /// `SelectedWattIndex`.
    pub const SELECTED_WATT_INDEX: AttributeId = 0x07;
    /// `WattRating`.
    pub const WATT_RATING: AttributeId = 0x08;
}

/// The command ids.
pub mod command {
    use crate::im::CommandId;

    /// `SetCookingParameters`.
    pub const SET_COOKING_PARAMETERS: CommandId = 0x00;
    /// `AddMoreTime`.
    pub const ADD_MORE_TIME: CommandId = 0x01;
}

/// The fields of `SetCookingParameters` (command `0x00`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct SetCookingParametersFields {
    /// `CookMode`, field 0.
    pub cook_mode: Option<u8>,
    /// `CookTime`, field 1.
    pub cook_time: Option<u32>,
    /// `PowerSetting`, field 2.
    pub power_setting: Option<u8>,
    /// `WattSettingIndex`, field 3.
    pub watt_setting_index: Option<u8>,
    /// `StartAfterSetting`, field 4.
    pub start_after_setting: Option<bool>,
}

impl ToTlv for SetCookingParametersFields {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        if let Some(value) = &self.cook_mode {
            value.to_tlv(w, Tag::Context(0))?;
        }
        if let Some(value) = &self.cook_time {
            value.to_tlv(w, Tag::Context(1))?;
        }
        if let Some(value) = &self.power_setting {
            value.to_tlv(w, Tag::Context(2))?;
        }
        if let Some(value) = &self.watt_setting_index {
            value.to_tlv(w, Tag::Context(3))?;
        }
        if let Some(value) = &self.start_after_setting {
            value.to_tlv(w, Tag::Context(4))?;
        }
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for SetCookingParametersFields {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut cook_mode = None;
        let mut cook_time = None;
        let mut power_setting = None;
        let mut watt_setting_index = None;
        let mut start_after_setting = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => cook_mode = Some(<u8>::from_tlv(reader, &field)?),
                Some(1) => cook_time = Some(<u32>::from_tlv(reader, &field)?),
                Some(2) => power_setting = Some(<u8>::from_tlv(reader, &field)?),
                Some(3) => watt_setting_index = Some(<u8>::from_tlv(reader, &field)?),
                Some(4) => start_after_setting = Some(<bool>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            cook_mode,
            cook_time,
            power_setting,
            watt_setting_index,
            start_after_setting,
        })
    }
}

/// The fields of `AddMoreTime` (command `0x01`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct AddMoreTimeFields {
    /// `TimeToAdd`, field 0.
    pub time_to_add: u32,
}

impl ToTlv for AddMoreTimeFields {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.time_to_add.to_tlv(w, Tag::Context(0))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for AddMoreTimeFields {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut time_to_add = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => time_to_add = Some(<u32>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            time_to_add: time_to_add.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// Every attribute the specification defines, with its conformance.
pub const ATTRIBUTES: &[Attribute] = &[
    Attribute {
        id: 0x0000,
        name: "CookTime",
        kind: "elapsed-s",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::empty(),
        reporting: Reporting::OnChange,
        conform: Conform::MANDATORY,
    },
    Attribute {
        id: 0x0001,
        name: "MaxCookTime",
        kind: "elapsed-s",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::FIXED,
        reporting: Reporting::OnChange,
        conform: Conform::MANDATORY,
    },
    Attribute {
        id: 0x0002,
        name: "PowerSetting",
        kind: "uint8",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::empty(),
        reporting: Reporting::OnChange,
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(0),
            }],
        },
    },
    Attribute {
        id: 0x0003,
        name: "MinPower",
        kind: "uint8",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::FIXED,
        reporting: Reporting::OnChange,
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(2),
            }],
        },
    },
    Attribute {
        id: 0x0004,
        name: "MaxPower",
        kind: "uint8",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::FIXED,
        reporting: Reporting::OnChange,
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(2),
            }],
        },
    },
    Attribute {
        id: 0x0005,
        name: "PowerStep",
        kind: "uint8",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::FIXED,
        reporting: Reporting::OnChange,
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(2),
            }],
        },
    },
    Attribute {
        id: 0x0006,
        name: "SupportedWatts",
        kind: "list[uint16]",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::FIXED,
        reporting: Reporting::OnChange,
        conform: Conform {
            clauses: &[
                Clause {
                    verdict: Conformance::Provisional,
                    when: Condition::Always,
                },
                Clause {
                    verdict: Conformance::Mandatory,
                    when: Condition::Feature(1),
                },
            ],
        },
    },
    Attribute {
        id: 0x0007,
        name: "SelectedWattIndex",
        kind: "uint8",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::empty(),
        reporting: Reporting::OnChange,
        conform: Conform {
            clauses: &[
                Clause {
                    verdict: Conformance::Provisional,
                    when: Condition::Always,
                },
                Clause {
                    verdict: Conformance::Mandatory,
                    when: Condition::Feature(1),
                },
            ],
        },
    },
    Attribute {
        id: 0x0008,
        name: "WattRating",
        kind: "uint16",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::FIXED,
        reporting: Reporting::OnChange,
        conform: Conform::OPTIONAL,
    },
];

/// Every command, with its conformance.
pub const COMMANDS: &[Command] = &[
    Command {
        id: 0x00,
        name: "SetCookingParameters",
        to_server: true,
        response: None,
        access: Access::invoke(Privilege::Operate),
        conform: Conform::MANDATORY,
    },
    Command {
        id: 0x01,
        name: "AddMoreTime",
        to_server: true,
        response: None,
        access: Access::invoke(Privilege::Operate),
        conform: Conform::OPTIONAL,
    },
];

/// Every event, with its conformance.
pub const EVENTS: &[Event] = &[];

/// The whole cluster, as the specification defines it.
pub const CLUSTER: Cluster = Cluster {
    id: ID,
    name: "Microwave Oven Control",
    revision: REVISION,
    pics: "MWOCTRL",
    derived_from: None,
    features: FEATURES,
    attributes: ATTRIBUTES,
    commands: COMMANDS,
    events: EVENTS,
};
