// Generated by `cargo xtask` from the CSA data model 1.6. Do not edit.
//
// Joint Fabric Administrator — cluster 0x0753.
//
// Regenerate with `cargo xtask clusters --dm <checkout>/data_model/1.6`; `cargo xtask check`
// is the CI gate that keeps this file honest. The XML itself is not vendored here — it
// carries the CSA's own copyright notice and is not redistributable.

//! Joint Fabric Administrator (cluster `0x0753`, revision 1).
//!
//! 10 identifiers, 0 feature bits, and the specification's own conformance for
//! every element. `CLUSTER` is the whole table.

#![allow(unused_imports)]

use crate::dm::conformance::{Clause, Condition, Conform, Conformance};
use crate::dm::meta::{EventPriority, Reporting};
use crate::dm::spec::{Attribute, Cluster, Command, Event, Feature};
use crate::dm::{Access, AccessQualities, AttributeQualities, Privilege};
use crate::error::Result;
use crate::im::{AttributeId, ClusterId, CommandId, EventId};
use crate::tlv::{
    ContainerKind, Element, FromTlv, Nullable, Tag, TlvList, TlvReader, TlvWriter, ToTlv, Value,
};

/// The cluster id.
pub const ID: ClusterId = 0x0753;

/// The highest revision the specification's history defines — what `ClusterRevision`
/// must report (Core §7.13.1).
pub const REVISION: u16 = 1;

/// The PICS code, which a certification declaration names the cluster by.
pub const PICS: &str = "JFADMIN";

/// This cluster defines no features, so its `FeatureMap` is always zero.
pub const FEATURES: &[Feature] = &[];

/// The attribute ids.
pub mod attribute {
    use crate::im::AttributeId;

    /// `AdministratorFabricIndex`.
    pub const ADMINISTRATOR_FABRIC_INDEX: AttributeId = 0x00;
}

/// The command ids.
pub mod command {
    use crate::im::CommandId;

    /// `ICACCSRRequest`.
    pub const ICACCSR_REQUEST: CommandId = 0x00;
    /// `ICACCSRResponse`.
    pub const ICACCSR_RESPONSE: CommandId = 0x01;
    /// `AddICAC`.
    pub const ADD_ICAC: CommandId = 0x02;
    /// `ICACResponse`.
    pub const ICAC_RESPONSE: CommandId = 0x03;
    /// `OpenJointCommissioningWindow`.
    pub const OPEN_JOINT_COMMISSIONING_WINDOW: CommandId = 0x04;
    /// `TransferAnchorRequest`.
    pub const TRANSFER_ANCHOR_REQUEST: CommandId = 0x05;
    /// `TransferAnchorResponse`.
    pub const TRANSFER_ANCHOR_RESPONSE: CommandId = 0x06;
    /// `TransferAnchorComplete`.
    pub const TRANSFER_ANCHOR_COMPLETE: CommandId = 0x07;
    /// `AnnounceJointFabricAdministrator`.
    pub const ANNOUNCE_JOINT_FABRIC_ADMINISTRATOR: CommandId = 0x08;
}

/// `ICACCSRResponseStatusCodeEnum`.
#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash)]
#[repr(u8)]
pub enum ICACCSRResponseStatusCodeEnum {
    /// No error
    OK = 0,
    /// Could not be completed because another commissioning is in progress
    Busy = 1,
    /// Provided PAKE parameters were incorrectly formatted or otherwise invalid
    PAKEParameterError = 2,
    /// The Joint Commissioning Method commissioning window is not currently open
    WindowNotOpen = 3,
    /// ICACCSRRequest command has been invoked by a peer against which Fabric Table VID Verification hasn't been executed
    VIDNotVerified = 4,
    /// OpenJointCommissioningWindow command has been invoked but the AdministratorFabricIndex field has the value of null
    InvalidAdministratorFabricIndex = 5,
}

impl ICACCSRResponseStatusCodeEnum {
    /// Reads a wire value, or `None` for one this revision does not define.
    #[must_use]
    pub const fn from_value(value: u8) -> Option<Self> {
        match value {
            0 => Some(Self::OK),
            1 => Some(Self::Busy),
            2 => Some(Self::PAKEParameterError),
            3 => Some(Self::WindowNotOpen),
            4 => Some(Self::VIDNotVerified),
            5 => Some(Self::InvalidAdministratorFabricIndex),
            _ => None,
        }
    }

    /// Its wire value.
    #[must_use]
    pub const fn value(self) -> u8 {
        self as u8
    }
}

impl ToTlv for ICACCSRResponseStatusCodeEnum {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        self.value().to_tlv(w, tag)
    }
}

impl<'a> FromTlv<'a> for ICACCSRResponseStatusCodeEnum {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        let value = <u8 as FromTlv<'a>>::from_tlv(reader, element)?;
        Self::from_value(value).ok_or(crate::Error::new(crate::ErrorCode::TlvOutOfRange))
    }
}

/// `ICACResponseStatusEnum`.
#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash)]
#[repr(u8)]
pub enum ICACResponseStatusEnum {
    /// No error
    OK = 0,
    /// Public Key in the ICAC is invalid
    InvalidPublicKey = 1,
    /// ICAC chain validation failed / ICAC DN Encoding rules verification failed
    InvalidICAC = 2,
}

impl ICACResponseStatusEnum {
    /// Reads a wire value, or `None` for one this revision does not define.
    #[must_use]
    pub const fn from_value(value: u8) -> Option<Self> {
        match value {
            0 => Some(Self::OK),
            1 => Some(Self::InvalidPublicKey),
            2 => Some(Self::InvalidICAC),
            _ => None,
        }
    }

    /// Its wire value.
    #[must_use]
    pub const fn value(self) -> u8 {
        self as u8
    }
}

impl ToTlv for ICACResponseStatusEnum {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        self.value().to_tlv(w, tag)
    }
}

impl<'a> FromTlv<'a> for ICACResponseStatusEnum {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        let value = <u8 as FromTlv<'a>>::from_tlv(reader, element)?;
        Self::from_value(value).ok_or(crate::Error::new(crate::ErrorCode::TlvOutOfRange))
    }
}

/// `TransferAnchorResponseStatusEnum`.
#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash)]
#[repr(u8)]
pub enum TransferAnchorResponseStatusEnum {
    /// No error
    OK = 0,
    /// Anchor Transfer was not started due to on-going Datastore operations
    TransferAnchorStatusDatastoreBusy = 1,
    /// User has not consented for Anchor Transfer
    TransferAnchorStatusNoUserConsent = 2,
}

impl TransferAnchorResponseStatusEnum {
    /// Reads a wire value, or `None` for one this revision does not define.
    #[must_use]
    pub const fn from_value(value: u8) -> Option<Self> {
        match value {
            0 => Some(Self::OK),
            1 => Some(Self::TransferAnchorStatusDatastoreBusy),
            2 => Some(Self::TransferAnchorStatusNoUserConsent),
            _ => None,
        }
    }

    /// Its wire value.
    #[must_use]
    pub const fn value(self) -> u8 {
        self as u8
    }
}

impl ToTlv for TransferAnchorResponseStatusEnum {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        self.value().to_tlv(w, tag)
    }
}

impl<'a> FromTlv<'a> for TransferAnchorResponseStatusEnum {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        let value = <u8 as FromTlv<'a>>::from_tlv(reader, element)?;
        Self::from_value(value).ok_or(crate::Error::new(crate::ErrorCode::TlvOutOfRange))
    }
}

/// The fields of `ICACCSRResponse` (command `0x01`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct ICACCSRResponseFields<'a> {
    /// `StatusCode`, field 0.
    pub status_code: ICACCSRResponseStatusCodeEnum,
    /// `ICACCSR`, field 1.
    pub icaccsr: Option<&'a [u8]>,
}

impl<'a> ToTlv for ICACCSRResponseFields<'a> {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.status_code.to_tlv(w, Tag::Context(0))?;
        if let Some(value) = &self.icaccsr {
            value.to_tlv(w, Tag::Context(1))?;
        }
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for ICACCSRResponseFields<'a> {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut status_code = None;
        let mut icaccsr = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => {
                    status_code = Some(<ICACCSRResponseStatusCodeEnum>::from_tlv(reader, &field)?)
                }
                Some(1) => icaccsr = Some(<&'a [u8]>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            status_code: status_code.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            icaccsr,
        })
    }
}

/// The fields of `AddICAC` (command `0x02`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct AddICACFields<'a> {
    /// `ICACValue`, field 0.
    pub icac_value: &'a [u8],
}

impl<'a> ToTlv for AddICACFields<'a> {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.icac_value.to_tlv(w, Tag::Context(0))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for AddICACFields<'a> {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut icac_value = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => icac_value = Some(<&'a [u8]>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            icac_value: icac_value.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// The fields of `ICACResponse` (command `0x03`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct ICACResponseFields {
    /// `StatusCode`, field 0.
    pub status_code: ICACResponseStatusEnum,
}

impl ToTlv for ICACResponseFields {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.status_code.to_tlv(w, Tag::Context(0))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for ICACResponseFields {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut status_code = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => status_code = Some(<ICACResponseStatusEnum>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            status_code: status_code.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// The fields of `OpenJointCommissioningWindow` (command `0x04`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct OpenJointCommissioningWindowFields<'a> {
    /// `CommissioningTimeout`, field 0.
    pub commissioning_timeout: u16,
    /// `PAKEPasscodeVerifier`, field 1.
    pub pake_passcode_verifier: &'a [u8],
    /// `Discriminator`, field 2.
    pub discriminator: u16,
    /// `Iterations`, field 3.
    pub iterations: u32,
    /// `Salt`, field 4.
    pub salt: &'a [u8],
}

impl<'a> ToTlv for OpenJointCommissioningWindowFields<'a> {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.commissioning_timeout.to_tlv(w, Tag::Context(0))?;
        self.pake_passcode_verifier.to_tlv(w, Tag::Context(1))?;
        self.discriminator.to_tlv(w, Tag::Context(2))?;
        self.iterations.to_tlv(w, Tag::Context(3))?;
        self.salt.to_tlv(w, Tag::Context(4))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for OpenJointCommissioningWindowFields<'a> {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut commissioning_timeout = None;
        let mut pake_passcode_verifier = None;
        let mut discriminator = None;
        let mut iterations = None;
        let mut salt = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => commissioning_timeout = Some(<u16>::from_tlv(reader, &field)?),
                Some(1) => pake_passcode_verifier = Some(<&'a [u8]>::from_tlv(reader, &field)?),
                Some(2) => discriminator = Some(<u16>::from_tlv(reader, &field)?),
                Some(3) => iterations = Some(<u32>::from_tlv(reader, &field)?),
                Some(4) => salt = Some(<&'a [u8]>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            commissioning_timeout: commissioning_timeout
                .ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            pake_passcode_verifier: pake_passcode_verifier
                .ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            discriminator: discriminator.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            iterations: iterations.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            salt: salt.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// The fields of `TransferAnchorResponse` (command `0x06`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct TransferAnchorResponseFields {
    /// `StatusCode`, field 0.
    pub status_code: TransferAnchorResponseStatusEnum,
}

impl ToTlv for TransferAnchorResponseFields {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.status_code.to_tlv(w, Tag::Context(0))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for TransferAnchorResponseFields {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut status_code = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => {
                    status_code = Some(<TransferAnchorResponseStatusEnum>::from_tlv(
                        reader, &field,
                    )?)
                }
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            status_code: status_code.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// The fields of `AnnounceJointFabricAdministrator` (command `0x08`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct AnnounceJointFabricAdministratorFields {
    /// `EndpointID`, field 0.
    pub endpoint_id: u16,
}

impl ToTlv for AnnounceJointFabricAdministratorFields {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.endpoint_id.to_tlv(w, Tag::Context(0))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for AnnounceJointFabricAdministratorFields {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut endpoint_id = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => endpoint_id = Some(<u16>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            endpoint_id: endpoint_id.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// Every attribute the specification defines, with its conformance.
pub const ATTRIBUTES: &[Attribute] = &[Attribute {
    id: 0x0000,
    name: "AdministratorFabricIndex",
    kind: "fabric-idx",
    access: Access::read_only(Privilege::Administer),
    qualities: AttributeQualities::NULLABLE,
    reporting: Reporting::OnChange,
    conform: Conform::MANDATORY,
}];

/// Every command, with its conformance.
pub const COMMANDS: &[Command] = &[
    Command {
        id: 0x00,
        name: "ICACCSRRequest",
        to_server: true,
        response: Some(0x01),
        access: Access::invoke(Privilege::Administer),
        conform: Conform::MANDATORY,
    },
    Command {
        id: 0x01,
        name: "ICACCSRResponse",
        to_server: false,
        response: None,
        access: Access::invoke(Privilege::Operate),
        conform: Conform::MANDATORY,
    },
    Command {
        id: 0x02,
        name: "AddICAC",
        to_server: true,
        response: Some(0x03),
        access: Access::invoke(Privilege::Administer),
        conform: Conform::MANDATORY,
    },
    Command {
        id: 0x03,
        name: "ICACResponse",
        to_server: false,
        response: None,
        access: Access::invoke(Privilege::Operate),
        conform: Conform::MANDATORY,
    },
    Command {
        id: 0x04,
        name: "OpenJointCommissioningWindow",
        to_server: true,
        response: None,
        access: Access::invoke(Privilege::Administer),
        conform: Conform::MANDATORY,
    },
    Command {
        id: 0x05,
        name: "TransferAnchorRequest",
        to_server: true,
        response: Some(0x06),
        access: Access::invoke(Privilege::Administer),
        conform: Conform::MANDATORY,
    },
    Command {
        id: 0x06,
        name: "TransferAnchorResponse",
        to_server: false,
        response: None,
        access: Access::invoke(Privilege::Operate),
        conform: Conform::MANDATORY,
    },
    Command {
        id: 0x07,
        name: "TransferAnchorComplete",
        to_server: true,
        response: None,
        access: Access::invoke(Privilege::Administer),
        conform: Conform::MANDATORY,
    },
    Command {
        id: 0x08,
        name: "AnnounceJointFabricAdministrator",
        to_server: true,
        response: None,
        access: Access::invoke(Privilege::Administer),
        conform: Conform::MANDATORY,
    },
];

/// Every event, with its conformance.
pub const EVENTS: &[Event] = &[];

/// The whole cluster, as the specification defines it.
pub const CLUSTER: Cluster = Cluster {
    id: ID,
    name: "Joint Fabric Administrator",
    revision: REVISION,
    pics: "JFADMIN",
    derived_from: None,
    features: FEATURES,
    attributes: ATTRIBUTES,
    commands: COMMANDS,
    events: EVENTS,
};
