// Generated by `cargo xtask` from the CSA data model 1.6. Do not edit.
//
// WebRTC Transport Provider — cluster 0x0553.
//
// Regenerate with `cargo xtask clusters --dm <checkout>/data_model/1.6`; `cargo xtask check`
// is the CI gate that keeps this file honest. The XML itself is not vendored here — it
// carries the CSA's own copyright notice and is not redistributable.

//! WebRTC Transport Provider (cluster `0x0553`, revision 2).
//!
//! 8 identifiers, 1 feature bits, and the specification's own conformance for
//! every element. `CLUSTER` is the whole table.

#![allow(unused_imports)]

use crate::dm::conformance::{Clause, Condition, Conform, Conformance};
use crate::dm::meta::{EventPriority, Reporting};
use crate::dm::spec::{Attribute, Cluster, Command, Event, Feature};
use crate::dm::{Access, AccessQualities, AttributeQualities, Privilege};
use crate::error::Result;
use crate::im::{AttributeId, ClusterId, CommandId, EventId};
use crate::tlv::{
    ContainerKind, Element, FromTlv, Nullable, Tag, TlvList, TlvReader, TlvWriter, ToTlv, Value,
};

/// The cluster id.
pub const ID: ClusterId = 0x0553;

/// The highest revision the specification's history defines — what `ClusterRevision`
/// must report (Core §7.13.1).
pub const REVISION: u16 = 2;

/// The PICS code, which a certification declaration names the cluster by.
pub const PICS: &str = "WEBRTCP";

/// The `FeatureMap` bits (Core §7.13.2).
///
/// A feature has conformance of its own: a device may not claim one the
/// specification forbids it alongside another.
pub mod feature {
    /// Supports metadata transmission in WebRTC sessions
    ///
    /// Code `METADATA`, bit 0.
    pub const METADATA: u32 = 1 << 0;
}

/// Every feature bit, with its conformance.
pub const FEATURES: &[Feature] = &[Feature {
    bit: 0,
    code: "METADATA",
    name: "Metadata",
    conform: Conform {
        clauses: &[
            Clause {
                verdict: Conformance::Provisional,
                when: Condition::Always,
            },
            Clause {
                verdict: Conformance::Optional,
                when: Condition::Always,
            },
        ],
    },
}];

/// The attribute ids.
pub mod attribute {
    use crate::im::AttributeId;

    /// `CurrentSessions`.
    pub const CURRENT_SESSIONS: AttributeId = 0x00;
}

/// The command ids.
pub mod command {
    use crate::im::CommandId;

    /// `SolicitOffer`.
    pub const SOLICIT_OFFER: CommandId = 0x00;
    /// `SolicitOfferResponse`.
    pub const SOLICIT_OFFER_RESPONSE: CommandId = 0x01;
    /// `ProvideOffer`.
    pub const PROVIDE_OFFER: CommandId = 0x02;
    /// `ProvideOfferResponse`.
    pub const PROVIDE_OFFER_RESPONSE: CommandId = 0x03;
    /// `ProvideAnswer`.
    pub const PROVIDE_ANSWER: CommandId = 0x04;
    /// `ProvideICECandidates`.
    pub const PROVIDE_ICE_CANDIDATES: CommandId = 0x05;
    /// `EndSession`.
    pub const END_SESSION: CommandId = 0x06;
}

/// The fields of `SolicitOffer` (command `0x00`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct SolicitOfferFields<'a> {
    /// `StreamUsage`, field 0.
    pub stream_usage: crate::clusters::generated::globals::StreamUsageEnum,
    /// `OriginatingEndpointID`, field 1.
    pub originating_endpoint_id: u16,
    /// `VideoStreamID`, field 2.
    pub video_stream_id: Option<Nullable<u16>>,
    /// `AudioStreamID`, field 3.
    pub audio_stream_id: Option<Nullable<u16>>,
    /// `ICEServers`, field 4.
    pub ice_servers: Option<TlvList<'a, crate::clusters::generated::globals::ICEServerStruct<'a>>>,
    /// `ICETransportPolicy`, field 5.
    pub ice_transport_policy: Option<&'a str>,
    /// `MetadataEnabled`, field 6.
    pub metadata_enabled: Option<bool>,
    /// `VideoStreams`, field 8.
    pub video_streams: Option<TlvList<'a, u16>>,
    /// `AudioStreams`, field 9.
    pub audio_streams: Option<TlvList<'a, u16>>,
}

impl<'a> ToTlv for SolicitOfferFields<'a> {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.stream_usage.to_tlv(w, Tag::Context(0))?;
        self.originating_endpoint_id.to_tlv(w, Tag::Context(1))?;
        if let Some(value) = &self.video_stream_id {
            value.to_tlv(w, Tag::Context(2))?;
        }
        if let Some(value) = &self.audio_stream_id {
            value.to_tlv(w, Tag::Context(3))?;
        }
        if let Some(value) = &self.ice_servers {
            value.to_tlv(w, Tag::Context(4))?;
        }
        if let Some(value) = &self.ice_transport_policy {
            value.to_tlv(w, Tag::Context(5))?;
        }
        if let Some(value) = &self.metadata_enabled {
            value.to_tlv(w, Tag::Context(6))?;
        }
        if let Some(value) = &self.video_streams {
            value.to_tlv(w, Tag::Context(8))?;
        }
        if let Some(value) = &self.audio_streams {
            value.to_tlv(w, Tag::Context(9))?;
        }
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for SolicitOfferFields<'a> {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut stream_usage = None;
        let mut originating_endpoint_id = None;
        let mut video_stream_id = None;
        let mut audio_stream_id = None;
        let mut ice_servers = None;
        let mut ice_transport_policy = None;
        let mut metadata_enabled = None;
        let mut video_streams = None;
        let mut audio_streams = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => {
                    stream_usage = Some(
                        <crate::clusters::generated::globals::StreamUsageEnum>::from_tlv(
                            reader, &field,
                        )?,
                    )
                }
                Some(1) => originating_endpoint_id = Some(<u16>::from_tlv(reader, &field)?),
                Some(2) => video_stream_id = Some(<Nullable<u16>>::from_tlv(reader, &field)?),
                Some(3) => audio_stream_id = Some(<Nullable<u16>>::from_tlv(reader, &field)?),
                Some(4) => {
                    ice_servers = Some(<TlvList<
                        'a,
                        crate::clusters::generated::globals::ICEServerStruct<'a>,
                    >>::from_tlv(reader, &field)?)
                }
                Some(5) => ice_transport_policy = Some(<&'a str>::from_tlv(reader, &field)?),
                Some(6) => metadata_enabled = Some(<bool>::from_tlv(reader, &field)?),
                Some(8) => video_streams = Some(<TlvList<'a, u16>>::from_tlv(reader, &field)?),
                Some(9) => audio_streams = Some(<TlvList<'a, u16>>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            stream_usage: stream_usage.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            originating_endpoint_id: originating_endpoint_id
                .ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            video_stream_id,
            audio_stream_id,
            ice_servers,
            ice_transport_policy,
            metadata_enabled,
            video_streams,
            audio_streams,
        })
    }
}

/// The fields of `SolicitOfferResponse` (command `0x01`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct SolicitOfferResponseFields {
    /// `WebRTCSessionID`, field 0.
    pub web_rtc_session_id: u16,
    /// `DeferredOffer`, field 1.
    pub deferred_offer: bool,
    /// `VideoStreamID`, field 2.
    pub video_stream_id: Option<Nullable<u16>>,
    /// `AudioStreamID`, field 3.
    pub audio_stream_id: Option<Nullable<u16>>,
}

impl ToTlv for SolicitOfferResponseFields {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.web_rtc_session_id.to_tlv(w, Tag::Context(0))?;
        self.deferred_offer.to_tlv(w, Tag::Context(1))?;
        if let Some(value) = &self.video_stream_id {
            value.to_tlv(w, Tag::Context(2))?;
        }
        if let Some(value) = &self.audio_stream_id {
            value.to_tlv(w, Tag::Context(3))?;
        }
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for SolicitOfferResponseFields {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut web_rtc_session_id = None;
        let mut deferred_offer = None;
        let mut video_stream_id = None;
        let mut audio_stream_id = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => web_rtc_session_id = Some(<u16>::from_tlv(reader, &field)?),
                Some(1) => deferred_offer = Some(<bool>::from_tlv(reader, &field)?),
                Some(2) => video_stream_id = Some(<Nullable<u16>>::from_tlv(reader, &field)?),
                Some(3) => audio_stream_id = Some(<Nullable<u16>>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            web_rtc_session_id: web_rtc_session_id
                .ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            deferred_offer: deferred_offer
                .ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            video_stream_id,
            audio_stream_id,
        })
    }
}

/// The fields of `ProvideOffer` (command `0x02`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct ProvideOfferFields<'a> {
    /// `WebRTCSessionID`, field 0.
    pub web_rtc_session_id: Nullable<u16>,
    /// `SDP`, field 1.
    pub sdp: &'a str,
    /// `StreamUsage`, field 2.
    pub stream_usage: Option<crate::clusters::generated::globals::StreamUsageEnum>,
    /// `OriginatingEndpointID`, field 3.
    pub originating_endpoint_id: Option<u16>,
    /// `VideoStreamID`, field 4.
    pub video_stream_id: Option<Nullable<u16>>,
    /// `AudioStreamID`, field 5.
    pub audio_stream_id: Option<Nullable<u16>>,
    /// `ICEServers`, field 6.
    pub ice_servers: Option<TlvList<'a, crate::clusters::generated::globals::ICEServerStruct<'a>>>,
    /// `ICETransportPolicy`, field 7.
    pub ice_transport_policy: Option<&'a str>,
    /// `MetadataEnabled`, field 8.
    pub metadata_enabled: Option<bool>,
    /// `VideoStreams`, field 10.
    pub video_streams: Option<TlvList<'a, u16>>,
    /// `AudioStreams`, field 11.
    pub audio_streams: Option<TlvList<'a, u16>>,
}

impl<'a> ToTlv for ProvideOfferFields<'a> {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.web_rtc_session_id.to_tlv(w, Tag::Context(0))?;
        self.sdp.to_tlv(w, Tag::Context(1))?;
        if let Some(value) = &self.stream_usage {
            value.to_tlv(w, Tag::Context(2))?;
        }
        if let Some(value) = &self.originating_endpoint_id {
            value.to_tlv(w, Tag::Context(3))?;
        }
        if let Some(value) = &self.video_stream_id {
            value.to_tlv(w, Tag::Context(4))?;
        }
        if let Some(value) = &self.audio_stream_id {
            value.to_tlv(w, Tag::Context(5))?;
        }
        if let Some(value) = &self.ice_servers {
            value.to_tlv(w, Tag::Context(6))?;
        }
        if let Some(value) = &self.ice_transport_policy {
            value.to_tlv(w, Tag::Context(7))?;
        }
        if let Some(value) = &self.metadata_enabled {
            value.to_tlv(w, Tag::Context(8))?;
        }
        if let Some(value) = &self.video_streams {
            value.to_tlv(w, Tag::Context(10))?;
        }
        if let Some(value) = &self.audio_streams {
            value.to_tlv(w, Tag::Context(11))?;
        }
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for ProvideOfferFields<'a> {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut web_rtc_session_id = None;
        let mut sdp = None;
        let mut stream_usage = None;
        let mut originating_endpoint_id = None;
        let mut video_stream_id = None;
        let mut audio_stream_id = None;
        let mut ice_servers = None;
        let mut ice_transport_policy = None;
        let mut metadata_enabled = None;
        let mut video_streams = None;
        let mut audio_streams = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => web_rtc_session_id = Some(<Nullable<u16>>::from_tlv(reader, &field)?),
                Some(1) => sdp = Some(<&'a str>::from_tlv(reader, &field)?),
                Some(2) => {
                    stream_usage = Some(
                        <crate::clusters::generated::globals::StreamUsageEnum>::from_tlv(
                            reader, &field,
                        )?,
                    )
                }
                Some(3) => originating_endpoint_id = Some(<u16>::from_tlv(reader, &field)?),
                Some(4) => video_stream_id = Some(<Nullable<u16>>::from_tlv(reader, &field)?),
                Some(5) => audio_stream_id = Some(<Nullable<u16>>::from_tlv(reader, &field)?),
                Some(6) => {
                    ice_servers = Some(<TlvList<
                        'a,
                        crate::clusters::generated::globals::ICEServerStruct<'a>,
                    >>::from_tlv(reader, &field)?)
                }
                Some(7) => ice_transport_policy = Some(<&'a str>::from_tlv(reader, &field)?),
                Some(8) => metadata_enabled = Some(<bool>::from_tlv(reader, &field)?),
                Some(10) => video_streams = Some(<TlvList<'a, u16>>::from_tlv(reader, &field)?),
                Some(11) => audio_streams = Some(<TlvList<'a, u16>>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            web_rtc_session_id: web_rtc_session_id
                .ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            sdp: sdp.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            stream_usage,
            originating_endpoint_id,
            video_stream_id,
            audio_stream_id,
            ice_servers,
            ice_transport_policy,
            metadata_enabled,
            video_streams,
            audio_streams,
        })
    }
}

/// The fields of `ProvideOfferResponse` (command `0x03`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct ProvideOfferResponseFields {
    /// `WebRTCSessionID`, field 0.
    pub web_rtc_session_id: u16,
    /// `VideoStreamID`, field 1.
    pub video_stream_id: Option<Nullable<u16>>,
    /// `AudioStreamID`, field 2.
    pub audio_stream_id: Option<Nullable<u16>>,
}

impl ToTlv for ProvideOfferResponseFields {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.web_rtc_session_id.to_tlv(w, Tag::Context(0))?;
        if let Some(value) = &self.video_stream_id {
            value.to_tlv(w, Tag::Context(1))?;
        }
        if let Some(value) = &self.audio_stream_id {
            value.to_tlv(w, Tag::Context(2))?;
        }
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for ProvideOfferResponseFields {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut web_rtc_session_id = None;
        let mut video_stream_id = None;
        let mut audio_stream_id = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => web_rtc_session_id = Some(<u16>::from_tlv(reader, &field)?),
                Some(1) => video_stream_id = Some(<Nullable<u16>>::from_tlv(reader, &field)?),
                Some(2) => audio_stream_id = Some(<Nullable<u16>>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            web_rtc_session_id: web_rtc_session_id
                .ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            video_stream_id,
            audio_stream_id,
        })
    }
}

/// The fields of `ProvideAnswer` (command `0x04`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct ProvideAnswerFields<'a> {
    /// `WebRTCSessionID`, field 0.
    pub web_rtc_session_id: u16,
    /// `SDP`, field 1.
    pub sdp: &'a str,
}

impl<'a> ToTlv for ProvideAnswerFields<'a> {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.web_rtc_session_id.to_tlv(w, Tag::Context(0))?;
        self.sdp.to_tlv(w, Tag::Context(1))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for ProvideAnswerFields<'a> {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut web_rtc_session_id = None;
        let mut sdp = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => web_rtc_session_id = Some(<u16>::from_tlv(reader, &field)?),
                Some(1) => sdp = Some(<&'a str>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            web_rtc_session_id: web_rtc_session_id
                .ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            sdp: sdp.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// The fields of `ProvideICECandidates` (command `0x05`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct ProvideICECandidatesFields<'a> {
    /// `WebRTCSessionID`, field 0.
    pub web_rtc_session_id: u16,
    /// `ICECandidates`, field 1.
    pub ice_candidates: TlvList<'a, crate::clusters::generated::globals::ICECandidateStruct<'a>>,
}

impl<'a> ToTlv for ProvideICECandidatesFields<'a> {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.web_rtc_session_id.to_tlv(w, Tag::Context(0))?;
        self.ice_candidates.to_tlv(w, Tag::Context(1))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for ProvideICECandidatesFields<'a> {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut web_rtc_session_id = None;
        let mut ice_candidates = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => web_rtc_session_id = Some(<u16>::from_tlv(reader, &field)?),
                Some(1) => {
                    ice_candidates = Some(<TlvList<
                        'a,
                        crate::clusters::generated::globals::ICECandidateStruct<'a>,
                    >>::from_tlv(reader, &field)?)
                }
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            web_rtc_session_id: web_rtc_session_id
                .ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            ice_candidates: ice_candidates
                .ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// The fields of `EndSession` (command `0x06`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct EndSessionFields {
    /// `WebRTCSessionID`, field 0.
    pub web_rtc_session_id: u16,
    /// `Reason`, field 1.
    pub reason: crate::clusters::generated::globals::WebRTCEndReasonEnum,
}

impl ToTlv for EndSessionFields {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.web_rtc_session_id.to_tlv(w, Tag::Context(0))?;
        self.reason.to_tlv(w, Tag::Context(1))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for EndSessionFields {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut web_rtc_session_id = None;
        let mut reason = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => web_rtc_session_id = Some(<u16>::from_tlv(reader, &field)?),
                Some(1) => {
                    reason = Some(
                        <crate::clusters::generated::globals::WebRTCEndReasonEnum>::from_tlv(
                            reader, &field,
                        )?,
                    )
                }
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            web_rtc_session_id: web_rtc_session_id
                .ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            reason: reason.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// Every attribute the specification defines, with its conformance.
pub const ATTRIBUTES: &[Attribute] = &[Attribute {
    id: 0x0000,
    name: "CurrentSessions",
    kind: "list[WebRTCSessionStruct]",
    access: Access::read_only(Privilege::Manage).with_qualities(AccessQualities::FABRIC_SENSITIVE),
    qualities: AttributeQualities::empty(),
    reporting: Reporting::OnChange,
    conform: Conform::MANDATORY,
}];

/// Every command, with its conformance.
pub const COMMANDS: &[Command] = &[
    Command {
        id: 0x00,
        name: "SolicitOffer",
        to_server: true,
        response: Some(0x01),
        access: Access::invoke(Privilege::Operate)
            .with_qualities(AccessQualities::FABRIC_SCOPED.union(AccessQualities::LARGE)),
        conform: Conform::MANDATORY,
    },
    Command {
        id: 0x01,
        name: "SolicitOfferResponse",
        to_server: false,
        response: None,
        access: Access::invoke(Privilege::Operate).with_qualities(AccessQualities::LARGE),
        conform: Conform::MANDATORY,
    },
    Command {
        id: 0x02,
        name: "ProvideOffer",
        to_server: true,
        response: Some(0x03),
        access: Access::invoke(Privilege::Operate)
            .with_qualities(AccessQualities::FABRIC_SCOPED.union(AccessQualities::LARGE)),
        conform: Conform::MANDATORY,
    },
    Command {
        id: 0x03,
        name: "ProvideOfferResponse",
        to_server: false,
        response: None,
        access: Access::invoke(Privilege::Operate).with_qualities(AccessQualities::LARGE),
        conform: Conform::MANDATORY,
    },
    Command {
        id: 0x04,
        name: "ProvideAnswer",
        to_server: true,
        response: None,
        access: Access::invoke(Privilege::Operate)
            .with_qualities(AccessQualities::FABRIC_SCOPED.union(AccessQualities::LARGE)),
        conform: Conform::MANDATORY,
    },
    Command {
        id: 0x05,
        name: "ProvideICECandidates",
        to_server: true,
        response: None,
        access: Access::invoke(Privilege::Operate)
            .with_qualities(AccessQualities::FABRIC_SCOPED.union(AccessQualities::LARGE)),
        conform: Conform::MANDATORY,
    },
    Command {
        id: 0x06,
        name: "EndSession",
        to_server: true,
        response: None,
        access: Access::invoke(Privilege::Operate)
            .with_qualities(AccessQualities::FABRIC_SCOPED.union(AccessQualities::LARGE)),
        conform: Conform::MANDATORY,
    },
];

/// Every event, with its conformance.
pub const EVENTS: &[Event] = &[];

/// The whole cluster, as the specification defines it.
pub const CLUSTER: Cluster = Cluster {
    id: ID,
    name: "WebRTC Transport Provider",
    revision: REVISION,
    pics: "WEBRTCP",
    derived_from: None,
    features: FEATURES,
    attributes: ATTRIBUTES,
    commands: COMMANDS,
    events: EVENTS,
};
