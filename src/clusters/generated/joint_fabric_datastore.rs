// Generated by `cargo xtask` from the CSA data model 1.6. Do not edit.
//
// Joint Fabric Datastore — cluster 0x0752.
//
// Regenerate with `cargo xtask clusters --dm <checkout>/data_model/1.6`; `cargo xtask check`
// is the CI gate that keeps this file honest. The XML itself is not vendored here — it
// carries the CSA's own copyright notice and is not redistributable.

//! Joint Fabric Datastore (cluster `0x0752`, revision 1).
//!
//! 34 identifiers, 0 feature bits, and the specification's own conformance for
//! every element. `CLUSTER` is the whole table.

#![allow(unused_imports)]

use crate::dm::conformance::{Clause, Condition, Conform, Conformance};
use crate::dm::meta::{EventPriority, Reporting};
use crate::dm::spec::{Attribute, Cluster, Command, Event, Feature};
use crate::dm::{Access, AccessQualities, AttributeQualities, Privilege};
use crate::error::Result;
use crate::im::{AttributeId, ClusterId, CommandId, EventId};
use crate::tlv::{
    ContainerKind, Element, FromTlv, Nullable, Tag, TlvList, TlvReader, TlvWriter, ToTlv, Value,
};

/// The cluster id.
pub const ID: ClusterId = 0x0752;

/// The highest revision the specification's history defines — what `ClusterRevision`
/// must report (Core §7.13.1).
pub const REVISION: u16 = 1;

/// The PICS code, which a certification declaration names the cluster by.
pub const PICS: &str = "JFDS";

/// This cluster defines no features, so its `FeatureMap` is always zero.
pub const FEATURES: &[Feature] = &[];

/// The attribute ids.
pub mod attribute {
    use crate::im::AttributeId;

    /// `AnchorRootCA`.
    pub const ANCHOR_ROOT_CA: AttributeId = 0x00;
    /// `AnchorNodeID`.
    pub const ANCHOR_NODE_ID: AttributeId = 0x01;
    /// `AnchorVendorID`.
    pub const ANCHOR_VENDOR_ID: AttributeId = 0x02;
    /// `FriendlyName`.
    pub const FRIENDLY_NAME: AttributeId = 0x03;
    /// `GroupKeySetList`.
    pub const GROUP_KEY_SET_LIST: AttributeId = 0x04;
    /// `GroupList`.
    pub const GROUP_LIST: AttributeId = 0x05;
    /// `NodeList`.
    pub const NODE_LIST: AttributeId = 0x06;
    /// `AdminList`.
    pub const ADMIN_LIST: AttributeId = 0x07;
    /// `Status`.
    pub const STATUS: AttributeId = 0x08;
    /// `EndpointGroupIDList`.
    pub const ENDPOINT_GROUP_ID_LIST: AttributeId = 0x09;
    /// `EndpointBindingList`.
    pub const ENDPOINT_BINDING_LIST: AttributeId = 0x0A;
    /// `NodeKeySetList`.
    pub const NODE_KEY_SET_LIST: AttributeId = 0x0B;
    /// `NodeACLList`.
    pub const NODE_ACL_LIST: AttributeId = 0x0C;
    /// `NodeEndpointList`.
    pub const NODE_ENDPOINT_LIST: AttributeId = 0x0D;
}

/// The command ids.
pub mod command {
    use crate::im::CommandId;

    /// `AddKeySet`.
    pub const ADD_KEY_SET: CommandId = 0x00;
    /// `UpdateKeySet`.
    pub const UPDATE_KEY_SET: CommandId = 0x01;
    /// `RemoveKeySet`.
    pub const REMOVE_KEY_SET: CommandId = 0x02;
    /// `AddGroup`.
    pub const ADD_GROUP: CommandId = 0x03;
    /// `UpdateGroup`.
    pub const UPDATE_GROUP: CommandId = 0x04;
    /// `RemoveGroup`.
    pub const REMOVE_GROUP: CommandId = 0x05;
    /// `AddAdmin`.
    pub const ADD_ADMIN: CommandId = 0x06;
    /// `UpdateAdmin`.
    pub const UPDATE_ADMIN: CommandId = 0x07;
    /// `RemoveAdmin`.
    pub const REMOVE_ADMIN: CommandId = 0x08;
    /// `AddPendingNode`.
    pub const ADD_PENDING_NODE: CommandId = 0x09;
    /// `RefreshNode`.
    pub const REFRESH_NODE: CommandId = 0x0A;
    /// `UpdateNode`.
    pub const UPDATE_NODE: CommandId = 0x0B;
    /// `RemoveNode`.
    pub const REMOVE_NODE: CommandId = 0x0C;
    /// `UpdateEndpointForNode`.
    pub const UPDATE_ENDPOINT_FOR_NODE: CommandId = 0x0D;
    /// `AddGroupIDToEndpointForNode`.
    pub const ADD_GROUP_ID_TO_ENDPOINT_FOR_NODE: CommandId = 0x0E;
    /// `RemoveGroupIDFromEndpointForNode`.
    pub const REMOVE_GROUP_ID_FROM_ENDPOINT_FOR_NODE: CommandId = 0x0F;
    /// `AddBindingToEndpointForNode`.
    pub const ADD_BINDING_TO_ENDPOINT_FOR_NODE: CommandId = 0x10;
    /// `RemoveBindingFromEndpointForNode`.
    pub const REMOVE_BINDING_FROM_ENDPOINT_FOR_NODE: CommandId = 0x11;
    /// `AddACLToNode`.
    pub const ADD_ACL_TO_NODE: CommandId = 0x12;
    /// `RemoveACLFromNode`.
    pub const REMOVE_ACL_FROM_NODE: CommandId = 0x13;
}

/// `DatastoreAccessControlEntryAuthModeEnum`.
#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash)]
#[repr(u8)]
pub enum DatastoreAccessControlEntryAuthModeEnum {
    /// Passcode authenticated session
    PASE = 1,
    /// Certificate authenticated session
    CASE = 2,
    /// Group authenticated session
    Group = 3,
}

impl DatastoreAccessControlEntryAuthModeEnum {
    /// Reads a wire value, or `None` for one this revision does not define.
    #[must_use]
    pub const fn from_value(value: u8) -> Option<Self> {
        match value {
            1 => Some(Self::PASE),
            2 => Some(Self::CASE),
            3 => Some(Self::Group),
            _ => None,
        }
    }

    /// Its wire value.
    #[must_use]
    pub const fn value(self) -> u8 {
        self as u8
    }
}

impl ToTlv for DatastoreAccessControlEntryAuthModeEnum {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        self.value().to_tlv(w, tag)
    }
}

impl<'a> FromTlv<'a> for DatastoreAccessControlEntryAuthModeEnum {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        let value = <u8 as FromTlv<'a>>::from_tlv(reader, element)?;
        Self::from_value(value).ok_or(crate::Error::new(crate::ErrorCode::TlvOutOfRange))
    }
}

/// `DatastoreAccessControlEntryPrivilegeEnum`.
#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash)]
#[repr(u8)]
pub enum DatastoreAccessControlEntryPrivilegeEnum {
    /// Can read and observe all (except Access Control Cluster)
    View = 1,
    /// View privileges, and can perform the primary function of this Node (except Access Control Cluster)
    Operate = 3,
    /// Operate privileges, and can modify persistent configuration of this Node (except Access Control Cluster)
    Manage = 4,
    /// Manage privileges, and can observe and modify the Access Control Cluster
    Administer = 5,
}

impl DatastoreAccessControlEntryPrivilegeEnum {
    /// Reads a wire value, or `None` for one this revision does not define.
    #[must_use]
    pub const fn from_value(value: u8) -> Option<Self> {
        match value {
            1 => Some(Self::View),
            3 => Some(Self::Operate),
            4 => Some(Self::Manage),
            5 => Some(Self::Administer),
            _ => None,
        }
    }

    /// Its wire value.
    #[must_use]
    pub const fn value(self) -> u8 {
        self as u8
    }
}

impl ToTlv for DatastoreAccessControlEntryPrivilegeEnum {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        self.value().to_tlv(w, tag)
    }
}

impl<'a> FromTlv<'a> for DatastoreAccessControlEntryPrivilegeEnum {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        let value = <u8 as FromTlv<'a>>::from_tlv(reader, element)?;
        Self::from_value(value).ok_or(crate::Error::new(crate::ErrorCode::TlvOutOfRange))
    }
}

/// `DatastoreGroupKeyMulticastPolicyEnum`.
#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash)]
#[repr(u8)]
pub enum DatastoreGroupKeyMulticastPolicyEnum {
    /// Indicates filtering of multicast messages for a specific Group ID
    PerGroupID = 0,
    /// Indicates not filtering of multicast messages
    AllNodes = 1,
}

impl DatastoreGroupKeyMulticastPolicyEnum {
    /// Reads a wire value, or `None` for one this revision does not define.
    #[must_use]
    pub const fn from_value(value: u8) -> Option<Self> {
        match value {
            0 => Some(Self::PerGroupID),
            1 => Some(Self::AllNodes),
            _ => None,
        }
    }

    /// Its wire value.
    #[must_use]
    pub const fn value(self) -> u8 {
        self as u8
    }
}

impl ToTlv for DatastoreGroupKeyMulticastPolicyEnum {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        self.value().to_tlv(w, tag)
    }
}

impl<'a> FromTlv<'a> for DatastoreGroupKeyMulticastPolicyEnum {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        let value = <u8 as FromTlv<'a>>::from_tlv(reader, element)?;
        Self::from_value(value).ok_or(crate::Error::new(crate::ErrorCode::TlvOutOfRange))
    }
}

/// `DatastoreGroupKeySecurityPolicyEnum`.
#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash)]
#[repr(u8)]
pub enum DatastoreGroupKeySecurityPolicyEnum {
    /// Message counter synchronization using trust-first
    TrustFirst = 0,
}

impl DatastoreGroupKeySecurityPolicyEnum {
    /// Reads a wire value, or `None` for one this revision does not define.
    #[must_use]
    pub const fn from_value(value: u8) -> Option<Self> {
        match value {
            0 => Some(Self::TrustFirst),
            _ => None,
        }
    }

    /// Its wire value.
    #[must_use]
    pub const fn value(self) -> u8 {
        self as u8
    }
}

impl ToTlv for DatastoreGroupKeySecurityPolicyEnum {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        self.value().to_tlv(w, tag)
    }
}

impl<'a> FromTlv<'a> for DatastoreGroupKeySecurityPolicyEnum {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        let value = <u8 as FromTlv<'a>>::from_tlv(reader, element)?;
        Self::from_value(value).ok_or(crate::Error::new(crate::ErrorCode::TlvOutOfRange))
    }
}

/// `DatastoreStateEnum`.
#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash)]
#[repr(u8)]
pub enum DatastoreStateEnum {
    /// Target device operation is pending
    Pending = 0,
    /// Target device operation has been committed
    Committed = 1,
    /// Target device delete operation is pending
    DeletePending = 2,
    /// Target device operation has failed
    CommitFailed = 3,
}

impl DatastoreStateEnum {
    /// Reads a wire value, or `None` for one this revision does not define.
    #[must_use]
    pub const fn from_value(value: u8) -> Option<Self> {
        match value {
            0 => Some(Self::Pending),
            1 => Some(Self::Committed),
            2 => Some(Self::DeletePending),
            3 => Some(Self::CommitFailed),
            _ => None,
        }
    }

    /// Its wire value.
    #[must_use]
    pub const fn value(self) -> u8 {
        self as u8
    }
}

impl ToTlv for DatastoreStateEnum {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        self.value().to_tlv(w, tag)
    }
}

impl<'a> FromTlv<'a> for DatastoreStateEnum {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        let value = <u8 as FromTlv<'a>>::from_tlv(reader, element)?;
        Self::from_value(value).ok_or(crate::Error::new(crate::ErrorCode::TlvOutOfRange))
    }
}

/// `DatastoreACLEntryStruct`.
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct DatastoreACLEntryStruct<'a> {
    /// `NodeID`, field 0.
    pub node_id: crate::msg::NodeId,
    /// `ListID`, field 1.
    pub list_id: u16,
    /// `ACLEntry`, field 2.
    pub acl_entry: DatastoreAccessControlEntryStruct<'a>,
    /// `StatusEntry`, field 3.
    pub status_entry: DatastoreStatusEntryStruct,
}

impl<'a> ToTlv for DatastoreACLEntryStruct<'a> {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.node_id.to_tlv(w, Tag::Context(0))?;
        self.list_id.to_tlv(w, Tag::Context(1))?;
        self.acl_entry.to_tlv(w, Tag::Context(2))?;
        self.status_entry.to_tlv(w, Tag::Context(3))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for DatastoreACLEntryStruct<'a> {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut node_id = None;
        let mut list_id = None;
        let mut acl_entry = None;
        let mut status_entry = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => node_id = Some(<crate::msg::NodeId>::from_tlv(reader, &field)?),
                Some(1) => list_id = Some(<u16>::from_tlv(reader, &field)?),
                Some(2) => {
                    acl_entry = Some(<DatastoreAccessControlEntryStruct<'a>>::from_tlv(
                        reader, &field,
                    )?)
                }
                Some(3) => {
                    status_entry = Some(<DatastoreStatusEntryStruct>::from_tlv(reader, &field)?)
                }
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            node_id: node_id.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            list_id: list_id.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            acl_entry: acl_entry.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            status_entry: status_entry.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// `DatastoreAccessControlEntryStruct`.
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct DatastoreAccessControlEntryStruct<'a> {
    /// `Privilege`, field 1.
    pub privilege: DatastoreAccessControlEntryPrivilegeEnum,
    /// `AuthMode`, field 2.
    pub auth_mode: DatastoreAccessControlEntryAuthModeEnum,
    /// `Subjects`, field 3.
    pub subjects: Nullable<TlvList<'a, u64>>,
    /// `Targets`, field 4.
    pub targets: Nullable<TlvList<'a, DatastoreAccessControlTargetStruct>>,
}

impl<'a> ToTlv for DatastoreAccessControlEntryStruct<'a> {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.privilege.to_tlv(w, Tag::Context(1))?;
        self.auth_mode.to_tlv(w, Tag::Context(2))?;
        self.subjects.to_tlv(w, Tag::Context(3))?;
        self.targets.to_tlv(w, Tag::Context(4))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for DatastoreAccessControlEntryStruct<'a> {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut privilege = None;
        let mut auth_mode = None;
        let mut subjects = None;
        let mut targets = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(1) => {
                    privilege = Some(<DatastoreAccessControlEntryPrivilegeEnum>::from_tlv(
                        reader, &field,
                    )?)
                }
                Some(2) => {
                    auth_mode = Some(<DatastoreAccessControlEntryAuthModeEnum>::from_tlv(
                        reader, &field,
                    )?)
                }
                Some(3) => subjects = Some(<Nullable<TlvList<'a, u64>>>::from_tlv(reader, &field)?),
                Some(4) => {
                    targets = Some(
                        <Nullable<TlvList<'a, DatastoreAccessControlTargetStruct>>>::from_tlv(
                            reader, &field,
                        )?,
                    )
                }
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            privilege: privilege.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            auth_mode: auth_mode.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            subjects: subjects.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            targets: targets.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// `DatastoreAccessControlTargetStruct`.
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct DatastoreAccessControlTargetStruct {
    /// `Cluster`, field 0.
    pub cluster: Nullable<u32>,
    /// `Endpoint`, field 1.
    pub endpoint: Nullable<u16>,
    /// `DeviceType`, field 2.
    pub device_type: Nullable<u32>,
}

impl ToTlv for DatastoreAccessControlTargetStruct {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.cluster.to_tlv(w, Tag::Context(0))?;
        self.endpoint.to_tlv(w, Tag::Context(1))?;
        self.device_type.to_tlv(w, Tag::Context(2))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for DatastoreAccessControlTargetStruct {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut cluster = None;
        let mut endpoint = None;
        let mut device_type = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => cluster = Some(<Nullable<u32>>::from_tlv(reader, &field)?),
                Some(1) => endpoint = Some(<Nullable<u16>>::from_tlv(reader, &field)?),
                Some(2) => device_type = Some(<Nullable<u32>>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            cluster: cluster.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            endpoint: endpoint.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            device_type: device_type.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// `DatastoreAdministratorInformationEntryStruct`.
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct DatastoreAdministratorInformationEntryStruct<'a> {
    /// `NodeID`, field 0.
    pub node_id: crate::msg::NodeId,
    /// `FriendlyName`, field 1.
    pub friendly_name: &'a str,
    /// `VendorID`, field 2.
    pub vendor_id: crate::msg::VendorId,
    /// `ICAC`, field 3.
    pub icac: &'a [u8],
}

impl<'a> ToTlv for DatastoreAdministratorInformationEntryStruct<'a> {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.node_id.to_tlv(w, Tag::Context(0))?;
        self.friendly_name.to_tlv(w, Tag::Context(1))?;
        self.vendor_id.to_tlv(w, Tag::Context(2))?;
        self.icac.to_tlv(w, Tag::Context(3))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for DatastoreAdministratorInformationEntryStruct<'a> {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut node_id = None;
        let mut friendly_name = None;
        let mut vendor_id = None;
        let mut icac = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => node_id = Some(<crate::msg::NodeId>::from_tlv(reader, &field)?),
                Some(1) => friendly_name = Some(<&'a str>::from_tlv(reader, &field)?),
                Some(2) => vendor_id = Some(<crate::msg::VendorId>::from_tlv(reader, &field)?),
                Some(3) => icac = Some(<&'a [u8]>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            node_id: node_id.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            friendly_name: friendly_name.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            vendor_id: vendor_id.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            icac: icac.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// `DatastoreBindingTargetStruct`.
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct DatastoreBindingTargetStruct {
    /// `Node`, field 1.
    pub node: Option<crate::msg::NodeId>,
    /// `Group`, field 2.
    pub group: Option<crate::msg::GroupId>,
    /// `Endpoint`, field 3.
    pub endpoint: Option<u16>,
    /// `Cluster`, field 4.
    pub cluster: Option<u32>,
}

impl ToTlv for DatastoreBindingTargetStruct {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        if let Some(value) = &self.node {
            value.to_tlv(w, Tag::Context(1))?;
        }
        if let Some(value) = &self.group {
            value.to_tlv(w, Tag::Context(2))?;
        }
        if let Some(value) = &self.endpoint {
            value.to_tlv(w, Tag::Context(3))?;
        }
        if let Some(value) = &self.cluster {
            value.to_tlv(w, Tag::Context(4))?;
        }
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for DatastoreBindingTargetStruct {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut node = None;
        let mut group = None;
        let mut endpoint = None;
        let mut cluster = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(1) => node = Some(<crate::msg::NodeId>::from_tlv(reader, &field)?),
                Some(2) => group = Some(<crate::msg::GroupId>::from_tlv(reader, &field)?),
                Some(3) => endpoint = Some(<u16>::from_tlv(reader, &field)?),
                Some(4) => cluster = Some(<u32>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            node,
            group,
            endpoint,
            cluster,
        })
    }
}

/// `DatastoreEndpointBindingEntryStruct`.
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct DatastoreEndpointBindingEntryStruct {
    /// `NodeID`, field 0.
    pub node_id: crate::msg::NodeId,
    /// `EndpointID`, field 1.
    pub endpoint_id: u16,
    /// `ListID`, field 2.
    pub list_id: u16,
    /// `Binding`, field 3.
    pub binding: DatastoreBindingTargetStruct,
    /// `StatusEntry`, field 4.
    pub status_entry: DatastoreStatusEntryStruct,
}

impl ToTlv for DatastoreEndpointBindingEntryStruct {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.node_id.to_tlv(w, Tag::Context(0))?;
        self.endpoint_id.to_tlv(w, Tag::Context(1))?;
        self.list_id.to_tlv(w, Tag::Context(2))?;
        self.binding.to_tlv(w, Tag::Context(3))?;
        self.status_entry.to_tlv(w, Tag::Context(4))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for DatastoreEndpointBindingEntryStruct {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut node_id = None;
        let mut endpoint_id = None;
        let mut list_id = None;
        let mut binding = None;
        let mut status_entry = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => node_id = Some(<crate::msg::NodeId>::from_tlv(reader, &field)?),
                Some(1) => endpoint_id = Some(<u16>::from_tlv(reader, &field)?),
                Some(2) => list_id = Some(<u16>::from_tlv(reader, &field)?),
                Some(3) => {
                    binding = Some(<DatastoreBindingTargetStruct>::from_tlv(reader, &field)?)
                }
                Some(4) => {
                    status_entry = Some(<DatastoreStatusEntryStruct>::from_tlv(reader, &field)?)
                }
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            node_id: node_id.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            endpoint_id: endpoint_id.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            list_id: list_id.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            binding: binding.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            status_entry: status_entry.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// `DatastoreEndpointEntryStruct`.
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct DatastoreEndpointEntryStruct<'a> {
    /// `EndpointID`, field 0.
    pub endpoint_id: u16,
    /// `NodeID`, field 1.
    pub node_id: crate::msg::NodeId,
    /// `FriendlyName`, field 2.
    pub friendly_name: &'a str,
}

impl<'a> ToTlv for DatastoreEndpointEntryStruct<'a> {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.endpoint_id.to_tlv(w, Tag::Context(0))?;
        self.node_id.to_tlv(w, Tag::Context(1))?;
        self.friendly_name.to_tlv(w, Tag::Context(2))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for DatastoreEndpointEntryStruct<'a> {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut endpoint_id = None;
        let mut node_id = None;
        let mut friendly_name = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => endpoint_id = Some(<u16>::from_tlv(reader, &field)?),
                Some(1) => node_id = Some(<crate::msg::NodeId>::from_tlv(reader, &field)?),
                Some(2) => friendly_name = Some(<&'a str>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            endpoint_id: endpoint_id.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            node_id: node_id.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            friendly_name: friendly_name.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// `DatastoreEndpointGroupIDEntryStruct`.
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct DatastoreEndpointGroupIDEntryStruct {
    /// `NodeID`, field 0.
    pub node_id: crate::msg::NodeId,
    /// `EndpointID`, field 1.
    pub endpoint_id: u16,
    /// `GroupID`, field 2.
    pub group_id: crate::msg::GroupId,
    /// `StatusEntry`, field 3.
    pub status_entry: DatastoreStatusEntryStruct,
}

impl ToTlv for DatastoreEndpointGroupIDEntryStruct {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.node_id.to_tlv(w, Tag::Context(0))?;
        self.endpoint_id.to_tlv(w, Tag::Context(1))?;
        self.group_id.to_tlv(w, Tag::Context(2))?;
        self.status_entry.to_tlv(w, Tag::Context(3))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for DatastoreEndpointGroupIDEntryStruct {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut node_id = None;
        let mut endpoint_id = None;
        let mut group_id = None;
        let mut status_entry = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => node_id = Some(<crate::msg::NodeId>::from_tlv(reader, &field)?),
                Some(1) => endpoint_id = Some(<u16>::from_tlv(reader, &field)?),
                Some(2) => group_id = Some(<crate::msg::GroupId>::from_tlv(reader, &field)?),
                Some(3) => {
                    status_entry = Some(<DatastoreStatusEntryStruct>::from_tlv(reader, &field)?)
                }
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            node_id: node_id.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            endpoint_id: endpoint_id.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            group_id: group_id.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            status_entry: status_entry.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// `DatastoreGroupInformationEntryStruct`.
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct DatastoreGroupInformationEntryStruct<'a> {
    /// `GroupID`, field 0.
    pub group_id: u64,
    /// `FriendlyName`, field 1.
    pub friendly_name: &'a str,
    /// `GroupKeySetID`, field 2.
    pub group_key_set_id: Nullable<u16>,
    /// `GroupCAT`, field 3.
    pub group_cat: Nullable<u16>,
    /// `GroupCATVersion`, field 4.
    pub group_cat_version: Nullable<u16>,
    /// `GroupPermission`, field 5.
    pub group_permission: DatastoreAccessControlEntryPrivilegeEnum,
}

impl<'a> ToTlv for DatastoreGroupInformationEntryStruct<'a> {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.group_id.to_tlv(w, Tag::Context(0))?;
        self.friendly_name.to_tlv(w, Tag::Context(1))?;
        self.group_key_set_id.to_tlv(w, Tag::Context(2))?;
        self.group_cat.to_tlv(w, Tag::Context(3))?;
        self.group_cat_version.to_tlv(w, Tag::Context(4))?;
        self.group_permission.to_tlv(w, Tag::Context(5))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for DatastoreGroupInformationEntryStruct<'a> {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut group_id = None;
        let mut friendly_name = None;
        let mut group_key_set_id = None;
        let mut group_cat = None;
        let mut group_cat_version = None;
        let mut group_permission = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => group_id = Some(<u64>::from_tlv(reader, &field)?),
                Some(1) => friendly_name = Some(<&'a str>::from_tlv(reader, &field)?),
                Some(2) => group_key_set_id = Some(<Nullable<u16>>::from_tlv(reader, &field)?),
                Some(3) => group_cat = Some(<Nullable<u16>>::from_tlv(reader, &field)?),
                Some(4) => group_cat_version = Some(<Nullable<u16>>::from_tlv(reader, &field)?),
                Some(5) => {
                    group_permission = Some(<DatastoreAccessControlEntryPrivilegeEnum>::from_tlv(
                        reader, &field,
                    )?)
                }
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            group_id: group_id.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            friendly_name: friendly_name.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            group_key_set_id: group_key_set_id
                .ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            group_cat: group_cat.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            group_cat_version: group_cat_version
                .ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            group_permission: group_permission
                .ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// `DatastoreGroupKeySetStruct`.
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct DatastoreGroupKeySetStruct<'a> {
    /// `GroupKeySetID`, field 0.
    pub group_key_set_id: u16,
    /// `GroupKeySecurityPolicy`, field 1.
    pub group_key_security_policy: DatastoreGroupKeySecurityPolicyEnum,
    /// `EpochKey0`, field 2.
    pub epoch_key0: Nullable<&'a [u8]>,
    /// `EpochStartTime0`, field 3.
    pub epoch_start_time0: Nullable<u64>,
    /// `EpochKey1`, field 4.
    pub epoch_key1: Nullable<&'a [u8]>,
    /// `EpochStartTime1`, field 5.
    pub epoch_start_time1: Nullable<u64>,
    /// `EpochKey2`, field 6.
    pub epoch_key2: Nullable<&'a [u8]>,
    /// `EpochStartTime2`, field 7.
    pub epoch_start_time2: Nullable<u64>,
    /// `GroupKeyMulticastPolicy`, field 8.
    pub group_key_multicast_policy: Option<DatastoreGroupKeyMulticastPolicyEnum>,
}

impl<'a> ToTlv for DatastoreGroupKeySetStruct<'a> {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.group_key_set_id.to_tlv(w, Tag::Context(0))?;
        self.group_key_security_policy.to_tlv(w, Tag::Context(1))?;
        self.epoch_key0.to_tlv(w, Tag::Context(2))?;
        self.epoch_start_time0.to_tlv(w, Tag::Context(3))?;
        self.epoch_key1.to_tlv(w, Tag::Context(4))?;
        self.epoch_start_time1.to_tlv(w, Tag::Context(5))?;
        self.epoch_key2.to_tlv(w, Tag::Context(6))?;
        self.epoch_start_time2.to_tlv(w, Tag::Context(7))?;
        if let Some(value) = &self.group_key_multicast_policy {
            value.to_tlv(w, Tag::Context(8))?;
        }
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for DatastoreGroupKeySetStruct<'a> {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut group_key_set_id = None;
        let mut group_key_security_policy = None;
        let mut epoch_key0 = None;
        let mut epoch_start_time0 = None;
        let mut epoch_key1 = None;
        let mut epoch_start_time1 = None;
        let mut epoch_key2 = None;
        let mut epoch_start_time2 = None;
        let mut group_key_multicast_policy = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => group_key_set_id = Some(<u16>::from_tlv(reader, &field)?),
                Some(1) => {
                    group_key_security_policy = Some(
                        <DatastoreGroupKeySecurityPolicyEnum>::from_tlv(reader, &field)?,
                    )
                }
                Some(2) => epoch_key0 = Some(<Nullable<&'a [u8]>>::from_tlv(reader, &field)?),
                Some(3) => epoch_start_time0 = Some(<Nullable<u64>>::from_tlv(reader, &field)?),
                Some(4) => epoch_key1 = Some(<Nullable<&'a [u8]>>::from_tlv(reader, &field)?),
                Some(5) => epoch_start_time1 = Some(<Nullable<u64>>::from_tlv(reader, &field)?),
                Some(6) => epoch_key2 = Some(<Nullable<&'a [u8]>>::from_tlv(reader, &field)?),
                Some(7) => epoch_start_time2 = Some(<Nullable<u64>>::from_tlv(reader, &field)?),
                Some(8) => {
                    group_key_multicast_policy = Some(
                        <DatastoreGroupKeyMulticastPolicyEnum>::from_tlv(reader, &field)?,
                    )
                }
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            group_key_set_id: group_key_set_id
                .ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            group_key_security_policy: group_key_security_policy
                .ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            epoch_key0: epoch_key0.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            epoch_start_time0: epoch_start_time0
                .ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            epoch_key1: epoch_key1.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            epoch_start_time1: epoch_start_time1
                .ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            epoch_key2: epoch_key2.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            epoch_start_time2: epoch_start_time2
                .ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            group_key_multicast_policy,
        })
    }
}

/// `DatastoreNodeInformationEntryStruct`.
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct DatastoreNodeInformationEntryStruct<'a> {
    /// `NodeID`, field 0.
    pub node_id: crate::msg::NodeId,
    /// `FriendlyName`, field 1.
    pub friendly_name: &'a str,
    /// `CommissioningStatusEntry`, field 2.
    pub commissioning_status_entry: DatastoreStatusEntryStruct,
}

impl<'a> ToTlv for DatastoreNodeInformationEntryStruct<'a> {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.node_id.to_tlv(w, Tag::Context(0))?;
        self.friendly_name.to_tlv(w, Tag::Context(1))?;
        self.commissioning_status_entry.to_tlv(w, Tag::Context(2))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for DatastoreNodeInformationEntryStruct<'a> {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut node_id = None;
        let mut friendly_name = None;
        let mut commissioning_status_entry = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => node_id = Some(<crate::msg::NodeId>::from_tlv(reader, &field)?),
                Some(1) => friendly_name = Some(<&'a str>::from_tlv(reader, &field)?),
                Some(2) => {
                    commissioning_status_entry =
                        Some(<DatastoreStatusEntryStruct>::from_tlv(reader, &field)?)
                }
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            node_id: node_id.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            friendly_name: friendly_name.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            commissioning_status_entry: commissioning_status_entry
                .ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// `DatastoreNodeKeySetEntryStruct`.
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct DatastoreNodeKeySetEntryStruct {
    /// `NodeID`, field 0.
    pub node_id: crate::msg::NodeId,
    /// `GroupKeySetID`, field 1.
    pub group_key_set_id: u16,
    /// `StatusEntry`, field 2.
    pub status_entry: DatastoreStatusEntryStruct,
}

impl ToTlv for DatastoreNodeKeySetEntryStruct {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.node_id.to_tlv(w, Tag::Context(0))?;
        self.group_key_set_id.to_tlv(w, Tag::Context(1))?;
        self.status_entry.to_tlv(w, Tag::Context(2))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for DatastoreNodeKeySetEntryStruct {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut node_id = None;
        let mut group_key_set_id = None;
        let mut status_entry = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => node_id = Some(<crate::msg::NodeId>::from_tlv(reader, &field)?),
                Some(1) => group_key_set_id = Some(<u16>::from_tlv(reader, &field)?),
                Some(2) => {
                    status_entry = Some(<DatastoreStatusEntryStruct>::from_tlv(reader, &field)?)
                }
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            node_id: node_id.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            group_key_set_id: group_key_set_id
                .ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            status_entry: status_entry.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// `DatastoreStatusEntryStruct`.
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct DatastoreStatusEntryStruct {
    /// `State`, field 0.
    pub state: DatastoreStateEnum,
    /// `UpdateTimestamp`, field 1.
    pub update_timestamp: u32,
    /// `FailureCode`, field 2.
    pub failure_code: u8,
}

impl ToTlv for DatastoreStatusEntryStruct {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.state.to_tlv(w, Tag::Context(0))?;
        self.update_timestamp.to_tlv(w, Tag::Context(1))?;
        self.failure_code.to_tlv(w, Tag::Context(2))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for DatastoreStatusEntryStruct {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut state = None;
        let mut update_timestamp = None;
        let mut failure_code = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => state = Some(<DatastoreStateEnum>::from_tlv(reader, &field)?),
                Some(1) => update_timestamp = Some(<u32>::from_tlv(reader, &field)?),
                Some(2) => failure_code = Some(<u8>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            state: state.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            update_timestamp: update_timestamp
                .ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            failure_code: failure_code.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// The fields of `AddKeySet` (command `0x00`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct AddKeySetFields<'a> {
    /// `GroupKeySet`, field 0.
    pub group_key_set: DatastoreGroupKeySetStruct<'a>,
}

impl<'a> ToTlv for AddKeySetFields<'a> {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.group_key_set.to_tlv(w, Tag::Context(0))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for AddKeySetFields<'a> {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut group_key_set = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => {
                    group_key_set =
                        Some(<DatastoreGroupKeySetStruct<'a>>::from_tlv(reader, &field)?)
                }
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            group_key_set: group_key_set.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// The fields of `UpdateKeySet` (command `0x01`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct UpdateKeySetFields<'a> {
    /// `GroupKeySet`, field 0.
    pub group_key_set: DatastoreGroupKeySetStruct<'a>,
}

impl<'a> ToTlv for UpdateKeySetFields<'a> {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.group_key_set.to_tlv(w, Tag::Context(0))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for UpdateKeySetFields<'a> {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut group_key_set = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => {
                    group_key_set =
                        Some(<DatastoreGroupKeySetStruct<'a>>::from_tlv(reader, &field)?)
                }
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            group_key_set: group_key_set.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// The fields of `RemoveKeySet` (command `0x02`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct RemoveKeySetFields {
    /// `GroupKeySetID`, field 0.
    pub group_key_set_id: u16,
}

impl ToTlv for RemoveKeySetFields {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.group_key_set_id.to_tlv(w, Tag::Context(0))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for RemoveKeySetFields {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut group_key_set_id = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => group_key_set_id = Some(<u16>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            group_key_set_id: group_key_set_id
                .ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// The fields of `AddGroup` (command `0x03`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct AddGroupFields<'a> {
    /// `GroupID`, field 0.
    pub group_id: crate::msg::GroupId,
    /// `FriendlyName`, field 1.
    pub friendly_name: &'a str,
    /// `GroupKeySetID`, field 2.
    pub group_key_set_id: Nullable<u16>,
    /// `GroupCAT`, field 3.
    pub group_cat: Nullable<u16>,
    /// `GroupCATVersion`, field 4.
    pub group_cat_version: Nullable<u16>,
    /// `GroupPermission`, field 5.
    pub group_permission: DatastoreAccessControlEntryPrivilegeEnum,
}

impl<'a> ToTlv for AddGroupFields<'a> {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.group_id.to_tlv(w, Tag::Context(0))?;
        self.friendly_name.to_tlv(w, Tag::Context(1))?;
        self.group_key_set_id.to_tlv(w, Tag::Context(2))?;
        self.group_cat.to_tlv(w, Tag::Context(3))?;
        self.group_cat_version.to_tlv(w, Tag::Context(4))?;
        self.group_permission.to_tlv(w, Tag::Context(5))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for AddGroupFields<'a> {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut group_id = None;
        let mut friendly_name = None;
        let mut group_key_set_id = None;
        let mut group_cat = None;
        let mut group_cat_version = None;
        let mut group_permission = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => group_id = Some(<crate::msg::GroupId>::from_tlv(reader, &field)?),
                Some(1) => friendly_name = Some(<&'a str>::from_tlv(reader, &field)?),
                Some(2) => group_key_set_id = Some(<Nullable<u16>>::from_tlv(reader, &field)?),
                Some(3) => group_cat = Some(<Nullable<u16>>::from_tlv(reader, &field)?),
                Some(4) => group_cat_version = Some(<Nullable<u16>>::from_tlv(reader, &field)?),
                Some(5) => {
                    group_permission = Some(<DatastoreAccessControlEntryPrivilegeEnum>::from_tlv(
                        reader, &field,
                    )?)
                }
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            group_id: group_id.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            friendly_name: friendly_name.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            group_key_set_id: group_key_set_id
                .ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            group_cat: group_cat.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            group_cat_version: group_cat_version
                .ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            group_permission: group_permission
                .ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// The fields of `UpdateGroup` (command `0x04`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct UpdateGroupFields<'a> {
    /// `GroupID`, field 0.
    pub group_id: crate::msg::GroupId,
    /// `FriendlyName`, field 1.
    pub friendly_name: Nullable<&'a str>,
    /// `GroupKeySetID`, field 2.
    pub group_key_set_id: Nullable<u16>,
    /// `GroupCAT`, field 3.
    pub group_cat: Nullable<u16>,
    /// `GroupCATVersion`, field 4.
    pub group_cat_version: Nullable<u16>,
    /// `GroupPermission`, field 5.
    pub group_permission: Nullable<DatastoreAccessControlEntryPrivilegeEnum>,
}

impl<'a> ToTlv for UpdateGroupFields<'a> {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.group_id.to_tlv(w, Tag::Context(0))?;
        self.friendly_name.to_tlv(w, Tag::Context(1))?;
        self.group_key_set_id.to_tlv(w, Tag::Context(2))?;
        self.group_cat.to_tlv(w, Tag::Context(3))?;
        self.group_cat_version.to_tlv(w, Tag::Context(4))?;
        self.group_permission.to_tlv(w, Tag::Context(5))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for UpdateGroupFields<'a> {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut group_id = None;
        let mut friendly_name = None;
        let mut group_key_set_id = None;
        let mut group_cat = None;
        let mut group_cat_version = None;
        let mut group_permission = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => group_id = Some(<crate::msg::GroupId>::from_tlv(reader, &field)?),
                Some(1) => friendly_name = Some(<Nullable<&'a str>>::from_tlv(reader, &field)?),
                Some(2) => group_key_set_id = Some(<Nullable<u16>>::from_tlv(reader, &field)?),
                Some(3) => group_cat = Some(<Nullable<u16>>::from_tlv(reader, &field)?),
                Some(4) => group_cat_version = Some(<Nullable<u16>>::from_tlv(reader, &field)?),
                Some(5) => {
                    group_permission = Some(
                        <Nullable<DatastoreAccessControlEntryPrivilegeEnum>>::from_tlv(
                            reader, &field,
                        )?,
                    )
                }
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            group_id: group_id.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            friendly_name: friendly_name.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            group_key_set_id: group_key_set_id
                .ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            group_cat: group_cat.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            group_cat_version: group_cat_version
                .ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            group_permission: group_permission
                .ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// The fields of `RemoveGroup` (command `0x05`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct RemoveGroupFields {
    /// `GroupID`, field 0.
    pub group_id: crate::msg::GroupId,
}

impl ToTlv for RemoveGroupFields {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.group_id.to_tlv(w, Tag::Context(0))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for RemoveGroupFields {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut group_id = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => group_id = Some(<crate::msg::GroupId>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            group_id: group_id.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// The fields of `AddAdmin` (command `0x06`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct AddAdminFields<'a> {
    /// `NodeID`, field 0.
    pub node_id: crate::msg::NodeId,
    /// `FriendlyName`, field 1.
    pub friendly_name: &'a str,
    /// `VendorID`, field 2.
    pub vendor_id: crate::msg::VendorId,
    /// `ICAC`, field 3.
    pub icac: &'a [u8],
}

impl<'a> ToTlv for AddAdminFields<'a> {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.node_id.to_tlv(w, Tag::Context(0))?;
        self.friendly_name.to_tlv(w, Tag::Context(1))?;
        self.vendor_id.to_tlv(w, Tag::Context(2))?;
        self.icac.to_tlv(w, Tag::Context(3))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for AddAdminFields<'a> {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut node_id = None;
        let mut friendly_name = None;
        let mut vendor_id = None;
        let mut icac = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => node_id = Some(<crate::msg::NodeId>::from_tlv(reader, &field)?),
                Some(1) => friendly_name = Some(<&'a str>::from_tlv(reader, &field)?),
                Some(2) => vendor_id = Some(<crate::msg::VendorId>::from_tlv(reader, &field)?),
                Some(3) => icac = Some(<&'a [u8]>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            node_id: node_id.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            friendly_name: friendly_name.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            vendor_id: vendor_id.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            icac: icac.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// The fields of `UpdateAdmin` (command `0x07`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct UpdateAdminFields<'a> {
    /// `NodeID`, field 0.
    pub node_id: crate::msg::NodeId,
    /// `FriendlyName`, field 1.
    pub friendly_name: Option<&'a str>,
    /// `ICAC`, field 2.
    pub icac: Option<&'a [u8]>,
}

impl<'a> ToTlv for UpdateAdminFields<'a> {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.node_id.to_tlv(w, Tag::Context(0))?;
        if let Some(value) = &self.friendly_name {
            value.to_tlv(w, Tag::Context(1))?;
        }
        if let Some(value) = &self.icac {
            value.to_tlv(w, Tag::Context(2))?;
        }
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for UpdateAdminFields<'a> {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut node_id = None;
        let mut friendly_name = None;
        let mut icac = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => node_id = Some(<crate::msg::NodeId>::from_tlv(reader, &field)?),
                Some(1) => friendly_name = Some(<&'a str>::from_tlv(reader, &field)?),
                Some(2) => icac = Some(<&'a [u8]>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            node_id: node_id.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            friendly_name,
            icac,
        })
    }
}

/// The fields of `RemoveAdmin` (command `0x08`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct RemoveAdminFields {
    /// `NodeID`, field 0.
    pub node_id: crate::msg::NodeId,
}

impl ToTlv for RemoveAdminFields {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.node_id.to_tlv(w, Tag::Context(0))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for RemoveAdminFields {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut node_id = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => node_id = Some(<crate::msg::NodeId>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            node_id: node_id.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// The fields of `AddPendingNode` (command `0x09`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct AddPendingNodeFields<'a> {
    /// `NodeID`, field 0.
    pub node_id: crate::msg::NodeId,
    /// `FriendlyName`, field 1.
    pub friendly_name: &'a str,
}

impl<'a> ToTlv for AddPendingNodeFields<'a> {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.node_id.to_tlv(w, Tag::Context(0))?;
        self.friendly_name.to_tlv(w, Tag::Context(1))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for AddPendingNodeFields<'a> {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut node_id = None;
        let mut friendly_name = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => node_id = Some(<crate::msg::NodeId>::from_tlv(reader, &field)?),
                Some(1) => friendly_name = Some(<&'a str>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            node_id: node_id.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            friendly_name: friendly_name.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// The fields of `RefreshNode` (command `0x0A`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct RefreshNodeFields {
    /// `NodeID`, field 0.
    pub node_id: crate::msg::NodeId,
}

impl ToTlv for RefreshNodeFields {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.node_id.to_tlv(w, Tag::Context(0))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for RefreshNodeFields {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut node_id = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => node_id = Some(<crate::msg::NodeId>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            node_id: node_id.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// The fields of `UpdateNode` (command `0x0B`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct UpdateNodeFields<'a> {
    /// `NodeID`, field 0.
    pub node_id: crate::msg::NodeId,
    /// `FriendlyName`, field 1.
    pub friendly_name: &'a str,
}

impl<'a> ToTlv for UpdateNodeFields<'a> {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.node_id.to_tlv(w, Tag::Context(0))?;
        self.friendly_name.to_tlv(w, Tag::Context(1))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for UpdateNodeFields<'a> {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut node_id = None;
        let mut friendly_name = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => node_id = Some(<crate::msg::NodeId>::from_tlv(reader, &field)?),
                Some(1) => friendly_name = Some(<&'a str>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            node_id: node_id.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            friendly_name: friendly_name.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// The fields of `RemoveNode` (command `0x0C`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct RemoveNodeFields {
    /// `NodeID`, field 0.
    pub node_id: crate::msg::NodeId,
}

impl ToTlv for RemoveNodeFields {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.node_id.to_tlv(w, Tag::Context(0))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for RemoveNodeFields {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut node_id = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => node_id = Some(<crate::msg::NodeId>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            node_id: node_id.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// The fields of `UpdateEndpointForNode` (command `0x0D`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct UpdateEndpointForNodeFields<'a> {
    /// `EndpointID`, field 0.
    pub endpoint_id: u16,
    /// `NodeID`, field 1.
    pub node_id: crate::msg::NodeId,
    /// `FriendlyName`, field 2.
    pub friendly_name: &'a str,
}

impl<'a> ToTlv for UpdateEndpointForNodeFields<'a> {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.endpoint_id.to_tlv(w, Tag::Context(0))?;
        self.node_id.to_tlv(w, Tag::Context(1))?;
        self.friendly_name.to_tlv(w, Tag::Context(2))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for UpdateEndpointForNodeFields<'a> {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut endpoint_id = None;
        let mut node_id = None;
        let mut friendly_name = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => endpoint_id = Some(<u16>::from_tlv(reader, &field)?),
                Some(1) => node_id = Some(<crate::msg::NodeId>::from_tlv(reader, &field)?),
                Some(2) => friendly_name = Some(<&'a str>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            endpoint_id: endpoint_id.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            node_id: node_id.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            friendly_name: friendly_name.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// The fields of `AddGroupIDToEndpointForNode` (command `0x0E`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct AddGroupIDToEndpointForNodeFields {
    /// `NodeID`, field 0.
    pub node_id: crate::msg::NodeId,
    /// `EndpointID`, field 1.
    pub endpoint_id: u16,
    /// `GroupID`, field 2.
    pub group_id: crate::msg::GroupId,
}

impl ToTlv for AddGroupIDToEndpointForNodeFields {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.node_id.to_tlv(w, Tag::Context(0))?;
        self.endpoint_id.to_tlv(w, Tag::Context(1))?;
        self.group_id.to_tlv(w, Tag::Context(2))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for AddGroupIDToEndpointForNodeFields {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut node_id = None;
        let mut endpoint_id = None;
        let mut group_id = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => node_id = Some(<crate::msg::NodeId>::from_tlv(reader, &field)?),
                Some(1) => endpoint_id = Some(<u16>::from_tlv(reader, &field)?),
                Some(2) => group_id = Some(<crate::msg::GroupId>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            node_id: node_id.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            endpoint_id: endpoint_id.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            group_id: group_id.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// The fields of `RemoveGroupIDFromEndpointForNode` (command `0x0F`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct RemoveGroupIDFromEndpointForNodeFields {
    /// `NodeID`, field 0.
    pub node_id: crate::msg::NodeId,
    /// `EndpointID`, field 1.
    pub endpoint_id: u16,
    /// `GroupID`, field 2.
    pub group_id: crate::msg::GroupId,
}

impl ToTlv for RemoveGroupIDFromEndpointForNodeFields {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.node_id.to_tlv(w, Tag::Context(0))?;
        self.endpoint_id.to_tlv(w, Tag::Context(1))?;
        self.group_id.to_tlv(w, Tag::Context(2))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for RemoveGroupIDFromEndpointForNodeFields {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut node_id = None;
        let mut endpoint_id = None;
        let mut group_id = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => node_id = Some(<crate::msg::NodeId>::from_tlv(reader, &field)?),
                Some(1) => endpoint_id = Some(<u16>::from_tlv(reader, &field)?),
                Some(2) => group_id = Some(<crate::msg::GroupId>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            node_id: node_id.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            endpoint_id: endpoint_id.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            group_id: group_id.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// The fields of `AddBindingToEndpointForNode` (command `0x10`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct AddBindingToEndpointForNodeFields {
    /// `NodeID`, field 0.
    pub node_id: crate::msg::NodeId,
    /// `EndpointID`, field 1.
    pub endpoint_id: u16,
    /// `Binding`, field 2.
    pub binding: DatastoreBindingTargetStruct,
}

impl ToTlv for AddBindingToEndpointForNodeFields {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.node_id.to_tlv(w, Tag::Context(0))?;
        self.endpoint_id.to_tlv(w, Tag::Context(1))?;
        self.binding.to_tlv(w, Tag::Context(2))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for AddBindingToEndpointForNodeFields {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut node_id = None;
        let mut endpoint_id = None;
        let mut binding = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => node_id = Some(<crate::msg::NodeId>::from_tlv(reader, &field)?),
                Some(1) => endpoint_id = Some(<u16>::from_tlv(reader, &field)?),
                Some(2) => {
                    binding = Some(<DatastoreBindingTargetStruct>::from_tlv(reader, &field)?)
                }
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            node_id: node_id.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            endpoint_id: endpoint_id.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            binding: binding.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// The fields of `RemoveBindingFromEndpointForNode` (command `0x11`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct RemoveBindingFromEndpointForNodeFields {
    /// `ListID`, field 0.
    pub list_id: u16,
    /// `EndpointID`, field 1.
    pub endpoint_id: u16,
    /// `NodeID`, field 2.
    pub node_id: crate::msg::NodeId,
}

impl ToTlv for RemoveBindingFromEndpointForNodeFields {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.list_id.to_tlv(w, Tag::Context(0))?;
        self.endpoint_id.to_tlv(w, Tag::Context(1))?;
        self.node_id.to_tlv(w, Tag::Context(2))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for RemoveBindingFromEndpointForNodeFields {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut list_id = None;
        let mut endpoint_id = None;
        let mut node_id = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => list_id = Some(<u16>::from_tlv(reader, &field)?),
                Some(1) => endpoint_id = Some(<u16>::from_tlv(reader, &field)?),
                Some(2) => node_id = Some(<crate::msg::NodeId>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            list_id: list_id.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            endpoint_id: endpoint_id.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            node_id: node_id.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// The fields of `AddACLToNode` (command `0x12`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct AddACLToNodeFields<'a> {
    /// `NodeID`, field 0.
    pub node_id: crate::msg::NodeId,
    /// `ACLEntry`, field 1.
    pub acl_entry: DatastoreAccessControlEntryStruct<'a>,
}

impl<'a> ToTlv for AddACLToNodeFields<'a> {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.node_id.to_tlv(w, Tag::Context(0))?;
        self.acl_entry.to_tlv(w, Tag::Context(1))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for AddACLToNodeFields<'a> {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut node_id = None;
        let mut acl_entry = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => node_id = Some(<crate::msg::NodeId>::from_tlv(reader, &field)?),
                Some(1) => {
                    acl_entry = Some(<DatastoreAccessControlEntryStruct<'a>>::from_tlv(
                        reader, &field,
                    )?)
                }
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            node_id: node_id.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            acl_entry: acl_entry.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// The fields of `RemoveACLFromNode` (command `0x13`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct RemoveACLFromNodeFields {
    /// `ListID`, field 0.
    pub list_id: u16,
    /// `NodeID`, field 1.
    pub node_id: crate::msg::NodeId,
}

impl ToTlv for RemoveACLFromNodeFields {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.list_id.to_tlv(w, Tag::Context(0))?;
        self.node_id.to_tlv(w, Tag::Context(1))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for RemoveACLFromNodeFields {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut list_id = None;
        let mut node_id = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => list_id = Some(<u16>::from_tlv(reader, &field)?),
                Some(1) => node_id = Some(<crate::msg::NodeId>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            list_id: list_id.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            node_id: node_id.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// Every attribute the specification defines, with its conformance.
pub const ATTRIBUTES: &[Attribute] = &[
    Attribute {
        id: 0x0000,
        name: "AnchorRootCA",
        kind: "octstr",
        access: Access::read_only(Privilege::Administer),
        qualities: AttributeQualities::NON_VOLATILE,
        reporting: Reporting::OnChange,
        conform: Conform::MANDATORY,
    },
    Attribute {
        id: 0x0001,
        name: "AnchorNodeID",
        kind: "node-id",
        access: Access::read_only(Privilege::Administer),
        qualities: AttributeQualities::NON_VOLATILE,
        reporting: Reporting::OnChange,
        conform: Conform::MANDATORY,
    },
    Attribute {
        id: 0x0002,
        name: "AnchorVendorID",
        kind: "vendor-id",
        access: Access::read_only(Privilege::Administer),
        qualities: AttributeQualities::NON_VOLATILE,
        reporting: Reporting::OnChange,
        conform: Conform::MANDATORY,
    },
    Attribute {
        id: 0x0003,
        name: "FriendlyName",
        kind: "string",
        access: Access::read_only(Privilege::Administer),
        qualities: AttributeQualities::NON_VOLATILE,
        reporting: Reporting::OnChange,
        conform: Conform::MANDATORY,
    },
    Attribute {
        id: 0x0004,
        name: "GroupKeySetList",
        kind: "list[DatastoreGroupKeySetStruct]",
        access: Access::read_only(Privilege::Administer),
        qualities: AttributeQualities::NON_VOLATILE,
        reporting: Reporting::OnChange,
        conform: Conform::MANDATORY,
    },
    Attribute {
        id: 0x0005,
        name: "GroupList",
        kind: "list[DatastoreGroupInformationEntryStruct]",
        access: Access::read_only(Privilege::Administer),
        qualities: AttributeQualities::NON_VOLATILE,
        reporting: Reporting::OnChange,
        conform: Conform::MANDATORY,
    },
    Attribute {
        id: 0x0006,
        name: "NodeList",
        kind: "list[DatastoreNodeInformationEntryStruct]",
        access: Access::read_only(Privilege::Administer),
        qualities: AttributeQualities::NON_VOLATILE,
        reporting: Reporting::OnChange,
        conform: Conform::MANDATORY,
    },
    Attribute {
        id: 0x0007,
        name: "AdminList",
        kind: "list[DatastoreAdministratorInformationEntryStruct]",
        access: Access::read_only(Privilege::Administer),
        qualities: AttributeQualities::NON_VOLATILE,
        reporting: Reporting::OnChange,
        conform: Conform::MANDATORY,
    },
    Attribute {
        id: 0x0008,
        name: "Status",
        kind: "DatastoreStatusEntryStruct",
        access: Access::read_only(Privilege::Administer),
        qualities: AttributeQualities::NON_VOLATILE,
        reporting: Reporting::OnChange,
        conform: Conform::MANDATORY,
    },
    Attribute {
        id: 0x0009,
        name: "EndpointGroupIDList",
        kind: "list[DatastoreEndpointGroupIDEntryStruct]",
        access: Access::read_only(Privilege::Administer),
        qualities: AttributeQualities::NON_VOLATILE,
        reporting: Reporting::OnChange,
        conform: Conform::MANDATORY,
    },
    Attribute {
        id: 0x000A,
        name: "EndpointBindingList",
        kind: "list[DatastoreEndpointBindingEntryStruct]",
        access: Access::read_only(Privilege::Administer),
        qualities: AttributeQualities::NON_VOLATILE,
        reporting: Reporting::OnChange,
        conform: Conform::MANDATORY,
    },
    Attribute {
        id: 0x000B,
        name: "NodeKeySetList",
        kind: "list[DatastoreNodeKeySetEntryStruct]",
        access: Access::read_only(Privilege::Administer),
        qualities: AttributeQualities::NON_VOLATILE,
        reporting: Reporting::OnChange,
        conform: Conform::MANDATORY,
    },
    Attribute {
        id: 0x000C,
        name: "NodeACLList",
        kind: "list[DatastoreACLEntryStruct]",
        access: Access::read_only(Privilege::Administer),
        qualities: AttributeQualities::NON_VOLATILE,
        reporting: Reporting::OnChange,
        conform: Conform::MANDATORY,
    },
    Attribute {
        id: 0x000D,
        name: "NodeEndpointList",
        kind: "list[DatastoreEndpointEntryStruct]",
        access: Access::read_only(Privilege::Administer),
        qualities: AttributeQualities::NON_VOLATILE,
        reporting: Reporting::OnChange,
        conform: Conform::MANDATORY,
    },
];

/// Every command, with its conformance.
pub const COMMANDS: &[Command] = &[
    Command {
        id: 0x00,
        name: "AddKeySet",
        to_server: true,
        response: None,
        access: Access::invoke(Privilege::Administer),
        conform: Conform::MANDATORY,
    },
    Command {
        id: 0x01,
        name: "UpdateKeySet",
        to_server: true,
        response: None,
        access: Access::invoke(Privilege::Administer),
        conform: Conform::MANDATORY,
    },
    Command {
        id: 0x02,
        name: "RemoveKeySet",
        to_server: true,
        response: None,
        access: Access::invoke(Privilege::Administer),
        conform: Conform::MANDATORY,
    },
    Command {
        id: 0x03,
        name: "AddGroup",
        to_server: true,
        response: None,
        access: Access::invoke(Privilege::Administer),
        conform: Conform::MANDATORY,
    },
    Command {
        id: 0x04,
        name: "UpdateGroup",
        to_server: true,
        response: None,
        access: Access::invoke(Privilege::Administer),
        conform: Conform::MANDATORY,
    },
    Command {
        id: 0x05,
        name: "RemoveGroup",
        to_server: true,
        response: None,
        access: Access::invoke(Privilege::Administer),
        conform: Conform::MANDATORY,
    },
    Command {
        id: 0x06,
        name: "AddAdmin",
        to_server: true,
        response: None,
        access: Access::invoke(Privilege::Administer),
        conform: Conform::MANDATORY,
    },
    Command {
        id: 0x07,
        name: "UpdateAdmin",
        to_server: true,
        response: None,
        access: Access::invoke(Privilege::Administer),
        conform: Conform::MANDATORY,
    },
    Command {
        id: 0x08,
        name: "RemoveAdmin",
        to_server: true,
        response: None,
        access: Access::invoke(Privilege::Administer),
        conform: Conform::MANDATORY,
    },
    Command {
        id: 0x09,
        name: "AddPendingNode",
        to_server: true,
        response: None,
        access: Access::invoke(Privilege::Administer),
        conform: Conform::MANDATORY,
    },
    Command {
        id: 0x0A,
        name: "RefreshNode",
        to_server: true,
        response: None,
        access: Access::invoke(Privilege::Administer),
        conform: Conform::MANDATORY,
    },
    Command {
        id: 0x0B,
        name: "UpdateNode",
        to_server: true,
        response: None,
        access: Access::invoke(Privilege::Administer),
        conform: Conform::MANDATORY,
    },
    Command {
        id: 0x0C,
        name: "RemoveNode",
        to_server: true,
        response: None,
        access: Access::invoke(Privilege::Administer),
        conform: Conform::MANDATORY,
    },
    Command {
        id: 0x0D,
        name: "UpdateEndpointForNode",
        to_server: true,
        response: None,
        access: Access::invoke(Privilege::Administer),
        conform: Conform::MANDATORY,
    },
    Command {
        id: 0x0E,
        name: "AddGroupIDToEndpointForNode",
        to_server: true,
        response: None,
        access: Access::invoke(Privilege::Administer),
        conform: Conform::MANDATORY,
    },
    Command {
        id: 0x0F,
        name: "RemoveGroupIDFromEndpointForNode",
        to_server: true,
        response: None,
        access: Access::invoke(Privilege::Administer),
        conform: Conform::MANDATORY,
    },
    Command {
        id: 0x10,
        name: "AddBindingToEndpointForNode",
        to_server: true,
        response: None,
        access: Access::invoke(Privilege::Administer),
        conform: Conform::MANDATORY,
    },
    Command {
        id: 0x11,
        name: "RemoveBindingFromEndpointForNode",
        to_server: true,
        response: None,
        access: Access::invoke(Privilege::Administer),
        conform: Conform::MANDATORY,
    },
    Command {
        id: 0x12,
        name: "AddACLToNode",
        to_server: true,
        response: None,
        access: Access::invoke(Privilege::Administer),
        conform: Conform::MANDATORY,
    },
    Command {
        id: 0x13,
        name: "RemoveACLFromNode",
        to_server: true,
        response: None,
        access: Access::invoke(Privilege::Administer),
        conform: Conform::MANDATORY,
    },
];

/// Every event, with its conformance.
pub const EVENTS: &[Event] = &[];

/// The whole cluster, as the specification defines it.
pub const CLUSTER: Cluster = Cluster {
    id: ID,
    name: "Joint Fabric Datastore",
    revision: REVISION,
    pics: "JFDS",
    derived_from: None,
    features: FEATURES,
    attributes: ATTRIBUTES,
    commands: COMMANDS,
    events: EVENTS,
};
