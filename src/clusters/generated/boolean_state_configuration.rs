// Generated by `cargo xtask` from the CSA data model 1.6. Do not edit.
//
// Boolean State Configuration — cluster 0x0080.
//
// Regenerate with `cargo xtask clusters --dm <checkout>/data_model/1.6`; `cargo xtask check`
// is the CI gate that keeps this file honest. The XML itself is not vendored here — it
// carries the CSA's own copyright notice and is not redistributable.

//! Boolean State Configuration (cluster `0x0080`, revision 2).
//!
//! 12 identifiers, 5 feature bits, and the specification's own conformance for
//! every element. `CLUSTER` is the whole table.

#![allow(unused_imports)]

use crate::dm::conformance::{Clause, Condition, Conform, Conformance};
use crate::dm::meta::{EventPriority, Reporting};
use crate::dm::spec::{Attribute, Cluster, Command, Event, Feature};
use crate::dm::{Access, AccessQualities, AttributeQualities, Privilege};
use crate::error::Result;
use crate::im::{AttributeId, ClusterId, CommandId, EventId};
use crate::tlv::{
    ContainerKind, Element, FromTlv, Nullable, Tag, TlvList, TlvReader, TlvWriter, ToTlv, Value,
};

/// The cluster id.
pub const ID: ClusterId = 0x0080;

/// The highest revision the specification's history defines — what `ClusterRevision`
/// must report (Core §7.13.1).
pub const REVISION: u16 = 2;

/// The PICS code, which a certification declaration names the cluster by.
pub const PICS: &str = "BOOLCFG";

/// The `FeatureMap` bits (Core §7.13.2).
///
/// A feature has conformance of its own: a device may not claim one the
/// specification forbids it alongside another.
pub mod feature {
    /// Supports visual alarms
    ///
    /// Code `VIS`, bit 0.
    pub const VISUAL: u32 = 1 << 0;
    /// Supports audible alarms
    ///
    /// Code `AUD`, bit 1.
    pub const AUDIBLE: u32 = 1 << 1;
    /// Supports ability to suppress or acknowledge alarms
    ///
    /// Code `SPRS`, bit 2.
    pub const ALARM_SUPPRESS: u32 = 1 << 2;
    /// Supports ability to set sensor sensitivity
    ///
    /// Code `SENSLVL`, bit 3.
    pub const SENSITIVITY_LEVEL: u32 = 1 << 3;
    /// Supports reporting fault events
    ///
    /// Code `FAULTEV`, bit 4.
    pub const FAULT_EVENTS: u32 = 1 << 4;
}

/// Every feature bit, with its conformance.
pub const FEATURES: &[Feature] = &[
    Feature {
        bit: 0,
        code: "VIS",
        name: "Visual",
        conform: Conform::OPTIONAL,
    },
    Feature {
        bit: 1,
        code: "AUD",
        name: "Audible",
        conform: Conform::OPTIONAL,
    },
    Feature {
        bit: 2,
        code: "SPRS",
        name: "AlarmSuppress",
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Optional,
                when: Condition::Any(&[Condition::Feature(0), Condition::Feature(1)]),
            }],
        },
    },
    Feature {
        bit: 3,
        code: "SENSLVL",
        name: "SensitivityLevel",
        conform: Conform::OPTIONAL,
    },
    Feature {
        bit: 4,
        code: "FAULTEV",
        name: "FaultEvents",
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Described,
                when: Condition::Named("comparison"),
            }],
        },
    },
];

/// The attribute ids.
pub mod attribute {
    use crate::im::AttributeId;

    /// `CurrentSensitivityLevel`.
    pub const CURRENT_SENSITIVITY_LEVEL: AttributeId = 0x00;
    /// `SupportedSensitivityLevels`.
    pub const SUPPORTED_SENSITIVITY_LEVELS: AttributeId = 0x01;
    /// `DefaultSensitivityLevel`.
    pub const DEFAULT_SENSITIVITY_LEVEL: AttributeId = 0x02;
    /// `AlarmsActive`.
    pub const ALARMS_ACTIVE: AttributeId = 0x03;
    /// `AlarmsSuppressed`.
    pub const ALARMS_SUPPRESSED: AttributeId = 0x04;
    /// `AlarmsEnabled`.
    pub const ALARMS_ENABLED: AttributeId = 0x05;
    /// `AlarmsSupported`.
    pub const ALARMS_SUPPORTED: AttributeId = 0x06;
    /// `SensorFault`.
    pub const SENSOR_FAULT: AttributeId = 0x07;
}

/// The command ids.
pub mod command {
    use crate::im::CommandId;

    /// `SuppressAlarm`.
    pub const SUPPRESS_ALARM: CommandId = 0x00;
    /// `EnableDisableAlarm`.
    pub const ENABLE_DISABLE_ALARM: CommandId = 0x01;
}

/// The event ids.
pub mod event {
    use crate::im::EventId;

    /// `AlarmsStateChanged`.
    pub const ALARMS_STATE_CHANGED: EventId = 0x00;
    /// `SensorFault`.
    pub const SENSOR_FAULT: EventId = 0x01;
}

bitflags::bitflags! {
    /// `AlarmModeBitmap`.
    #[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Default)]
    pub struct AlarmModeBitmap: u8 {
        /// Visual alarming
        const VISUAL = 1 << 0;
        /// Audible alarming
        const AUDIBLE = 1 << 1;
    }
}

impl ToTlv for AlarmModeBitmap {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        self.bits().to_tlv(w, tag)
    }
}

impl<'a> FromTlv<'a> for AlarmModeBitmap {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        let bits = <u8 as FromTlv<'a>>::from_tlv(reader, element)?;
        Self::from_bits(bits).ok_or(crate::Error::new(crate::ErrorCode::TlvOutOfRange))
    }
}

bitflags::bitflags! {
    /// `SensorFaultBitmap`.
    #[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Default)]
    pub struct SensorFaultBitmap: u8 {
        /// Unspecified fault detected
        const GENERAL_FAULT = 1 << 0;
    }
}

impl ToTlv for SensorFaultBitmap {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        self.bits().to_tlv(w, tag)
    }
}

impl<'a> FromTlv<'a> for SensorFaultBitmap {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        let bits = <u8 as FromTlv<'a>>::from_tlv(reader, element)?;
        Self::from_bits(bits).ok_or(crate::Error::new(crate::ErrorCode::TlvOutOfRange))
    }
}

/// The fields of `SuppressAlarm` (command `0x00`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct SuppressAlarmFields {
    /// `AlarmsToSuppress`, field 0.
    pub alarms_to_suppress: AlarmModeBitmap,
}

impl ToTlv for SuppressAlarmFields {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.alarms_to_suppress.to_tlv(w, Tag::Context(0))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for SuppressAlarmFields {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut alarms_to_suppress = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => alarms_to_suppress = Some(<AlarmModeBitmap>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            alarms_to_suppress: alarms_to_suppress
                .ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// The fields of `EnableDisableAlarm` (command `0x01`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct EnableDisableAlarmFields {
    /// `AlarmsToEnableDisable`, field 0.
    pub alarms_to_enable_disable: AlarmModeBitmap,
}

impl ToTlv for EnableDisableAlarmFields {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.alarms_to_enable_disable.to_tlv(w, Tag::Context(0))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for EnableDisableAlarmFields {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut alarms_to_enable_disable = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => {
                    alarms_to_enable_disable = Some(<AlarmModeBitmap>::from_tlv(reader, &field)?)
                }
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            alarms_to_enable_disable: alarms_to_enable_disable
                .ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// Every attribute the specification defines, with its conformance.
pub const ATTRIBUTES: &[Attribute] = &[
    Attribute {
        id: 0x0000,
        name: "CurrentSensitivityLevel",
        kind: "uint8",
        access: Access::read_write_with(Privilege::View, Privilege::Operate),
        qualities: AttributeQualities::NON_VOLATILE,
        reporting: Reporting::OnChange,
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(3),
            }],
        },
    },
    Attribute {
        id: 0x0001,
        name: "SupportedSensitivityLevels",
        kind: "uint8",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::FIXED,
        reporting: Reporting::OnChange,
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(3),
            }],
        },
    },
    Attribute {
        id: 0x0002,
        name: "DefaultSensitivityLevel",
        kind: "uint8",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::FIXED,
        reporting: Reporting::OnChange,
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Optional,
                when: Condition::Feature(3),
            }],
        },
    },
    Attribute {
        id: 0x0003,
        name: "AlarmsActive",
        kind: "AlarmModeBitmap",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::empty(),
        reporting: Reporting::OnChange,
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Any(&[Condition::Feature(0), Condition::Feature(1)]),
            }],
        },
    },
    Attribute {
        id: 0x0004,
        name: "AlarmsSuppressed",
        kind: "AlarmModeBitmap",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::empty(),
        reporting: Reporting::OnChange,
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(2),
            }],
        },
    },
    Attribute {
        id: 0x0005,
        name: "AlarmsEnabled",
        kind: "AlarmModeBitmap",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::NON_VOLATILE,
        reporting: Reporting::OnChange,
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Optional,
                when: Condition::Any(&[Condition::Feature(0), Condition::Feature(1)]),
            }],
        },
    },
    Attribute {
        id: 0x0006,
        name: "AlarmsSupported",
        kind: "AlarmModeBitmap",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::FIXED,
        reporting: Reporting::OnChange,
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Any(&[Condition::Feature(0), Condition::Feature(1)]),
            }],
        },
    },
    Attribute {
        id: 0x0007,
        name: "SensorFault",
        kind: "SensorFaultBitmap",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::empty(),
        reporting: Reporting::OnChange,
        conform: Conform::OPTIONAL,
    },
];

/// Every command, with its conformance.
pub const COMMANDS: &[Command] = &[
    Command {
        id: 0x00,
        name: "SuppressAlarm",
        to_server: true,
        response: None,
        access: Access::invoke(Privilege::Operate),
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(2),
            }],
        },
    },
    Command {
        id: 0x01,
        name: "EnableDisableAlarm",
        to_server: true,
        response: None,
        access: Access::invoke(Privilege::Operate),
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Any(&[Condition::Feature(0), Condition::Feature(1)]),
            }],
        },
    },
];

/// Every event, with its conformance.
pub const EVENTS: &[Event] = &[
    Event {
        id: 0x00,
        name: "AlarmsStateChanged",
        priority: EventPriority::Info,
        access: Access::read_only(Privilege::View),
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Any(&[Condition::Feature(0), Condition::Feature(1)]),
            }],
        },
    },
    Event {
        id: 0x01,
        name: "SensorFault",
        priority: EventPriority::Info,
        access: Access::read_only(Privilege::View),
        conform: Conform {
            clauses: &[
                Clause {
                    verdict: Conformance::Mandatory,
                    when: Condition::Feature(4),
                },
                Clause {
                    verdict: Conformance::Optional,
                    when: Condition::Always,
                },
            ],
        },
    },
];

/// The whole cluster, as the specification defines it.
pub const CLUSTER: Cluster = Cluster {
    id: ID,
    name: "Boolean State Configuration",
    revision: REVISION,
    pics: "BOOLCFG",
    derived_from: None,
    features: FEATURES,
    attributes: ATTRIBUTES,
    commands: COMMANDS,
    events: EVENTS,
};
