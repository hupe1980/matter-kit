// Generated by `cargo xtask` from the CSA data model 1.6. Do not edit.
//
// Thermostat User Interface Configuration — cluster 0x0204.
//
// Regenerate with `cargo xtask clusters --dm <checkout>/data_model/1.6`; `cargo xtask check`
// is the CI gate that keeps this file honest. The XML itself is not vendored here — it
// carries the CSA's own copyright notice and is not redistributable.

//! Thermostat User Interface Configuration (cluster `0x0204`, revision 2).
//!
//! 3 identifiers, 0 feature bits, and the specification's own conformance for
//! every element. `CLUSTER` is the whole table.

#![allow(unused_imports)]

use crate::dm::conformance::{Clause, Condition, Conform, Conformance};
use crate::dm::meta::{EventPriority, Reporting};
use crate::dm::spec::{Attribute, Cluster, Command, Event, Feature};
use crate::dm::{Access, AccessQualities, AttributeQualities, Privilege};
use crate::error::Result;
use crate::im::{AttributeId, ClusterId, CommandId, EventId};
use crate::tlv::{
    ContainerKind, Element, FromTlv, Nullable, Tag, TlvList, TlvReader, TlvWriter, ToTlv, Value,
};

/// The cluster id.
pub const ID: ClusterId = 0x0204;

/// The highest revision the specification's history defines — what `ClusterRevision`
/// must report (Core §7.13.1).
pub const REVISION: u16 = 2;

/// The PICS code, which a certification declaration names the cluster by.
pub const PICS: &str = "TSUIC";

/// This cluster defines no features, so its `FeatureMap` is always zero.
pub const FEATURES: &[Feature] = &[];

/// The attribute ids.
pub mod attribute {
    use crate::im::AttributeId;

    /// `TemperatureDisplayMode`.
    pub const TEMPERATURE_DISPLAY_MODE: AttributeId = 0x00;
    /// `KeypadLockout`.
    pub const KEYPAD_LOCKOUT: AttributeId = 0x01;
    /// `ScheduleProgrammingVisibility`.
    pub const SCHEDULE_PROGRAMMING_VISIBILITY: AttributeId = 0x02;
}

/// `KeypadLockoutEnum`.
#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash)]
#[repr(u8)]
pub enum KeypadLockoutEnum {
    /// All functionality available to the user
    NoLockout = 0,
    /// Level 1 reduced functionality
    Lockout1 = 1,
    /// Level 2 reduced functionality
    Lockout2 = 2,
    /// Level 3 reduced functionality
    Lockout3 = 3,
    /// Level 4 reduced functionality
    Lockout4 = 4,
    /// Least functionality available to the user
    Lockout5 = 5,
}

impl KeypadLockoutEnum {
    /// Reads a wire value, or `None` for one this revision does not define.
    #[must_use]
    pub const fn from_value(value: u8) -> Option<Self> {
        match value {
            0 => Some(Self::NoLockout),
            1 => Some(Self::Lockout1),
            2 => Some(Self::Lockout2),
            3 => Some(Self::Lockout3),
            4 => Some(Self::Lockout4),
            5 => Some(Self::Lockout5),
            _ => None,
        }
    }

    /// Its wire value.
    #[must_use]
    pub const fn value(self) -> u8 {
        self as u8
    }
}

impl ToTlv for KeypadLockoutEnum {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        self.value().to_tlv(w, tag)
    }
}

impl<'a> FromTlv<'a> for KeypadLockoutEnum {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        let value = <u8 as FromTlv<'a>>::from_tlv(reader, element)?;
        Self::from_value(value).ok_or(crate::Error::new(crate::ErrorCode::TlvOutOfRange))
    }
}

/// `ScheduleProgrammingVisibilityEnum`.
#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash)]
#[repr(u8)]
pub enum ScheduleProgrammingVisibilityEnum {
    /// Local schedule programming functionality is enabled at the thermostat
    ScheduleProgrammingPermitted = 0,
    /// Local schedule programming functionality is disabled at the thermostat
    ScheduleProgrammingDenied = 1,
}

impl ScheduleProgrammingVisibilityEnum {
    /// Reads a wire value, or `None` for one this revision does not define.
    #[must_use]
    pub const fn from_value(value: u8) -> Option<Self> {
        match value {
            0 => Some(Self::ScheduleProgrammingPermitted),
            1 => Some(Self::ScheduleProgrammingDenied),
            _ => None,
        }
    }

    /// Its wire value.
    #[must_use]
    pub const fn value(self) -> u8 {
        self as u8
    }
}

impl ToTlv for ScheduleProgrammingVisibilityEnum {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        self.value().to_tlv(w, tag)
    }
}

impl<'a> FromTlv<'a> for ScheduleProgrammingVisibilityEnum {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        let value = <u8 as FromTlv<'a>>::from_tlv(reader, element)?;
        Self::from_value(value).ok_or(crate::Error::new(crate::ErrorCode::TlvOutOfRange))
    }
}

/// `TemperatureDisplayModeEnum`.
#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash)]
#[repr(u8)]
pub enum TemperatureDisplayModeEnum {
    /// Temperature displayed in °C
    Celsius = 0,
    /// Temperature displayed in °F
    Fahrenheit = 1,
}

impl TemperatureDisplayModeEnum {
    /// Reads a wire value, or `None` for one this revision does not define.
    #[must_use]
    pub const fn from_value(value: u8) -> Option<Self> {
        match value {
            0 => Some(Self::Celsius),
            1 => Some(Self::Fahrenheit),
            _ => None,
        }
    }

    /// Its wire value.
    #[must_use]
    pub const fn value(self) -> u8 {
        self as u8
    }
}

impl ToTlv for TemperatureDisplayModeEnum {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        self.value().to_tlv(w, tag)
    }
}

impl<'a> FromTlv<'a> for TemperatureDisplayModeEnum {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        let value = <u8 as FromTlv<'a>>::from_tlv(reader, element)?;
        Self::from_value(value).ok_or(crate::Error::new(crate::ErrorCode::TlvOutOfRange))
    }
}

/// Every attribute the specification defines, with its conformance.
pub const ATTRIBUTES: &[Attribute] = &[
    Attribute {
        id: 0x0000,
        name: "TemperatureDisplayMode",
        kind: "TemperatureDisplayModeEnum",
        access: Access::read_write_with(Privilege::View, Privilege::Operate),
        qualities: AttributeQualities::empty(),
        reporting: Reporting::OnChange,
        conform: Conform::MANDATORY,
    },
    Attribute {
        id: 0x0001,
        name: "KeypadLockout",
        kind: "KeypadLockoutEnum",
        access: Access::read_write_with(Privilege::View, Privilege::Manage),
        qualities: AttributeQualities::empty(),
        reporting: Reporting::OnChange,
        conform: Conform::MANDATORY,
    },
    Attribute {
        id: 0x0002,
        name: "ScheduleProgrammingVisibility",
        kind: "ScheduleProgrammingVisibilityEnum",
        access: Access::read_write_with(Privilege::View, Privilege::Manage),
        qualities: AttributeQualities::empty(),
        reporting: Reporting::OnChange,
        conform: Conform::OPTIONAL,
    },
];

/// Every command, with its conformance.
pub const COMMANDS: &[Command] = &[];

/// Every event, with its conformance.
pub const EVENTS: &[Event] = &[];

/// The whole cluster, as the specification defines it.
pub const CLUSTER: Cluster = Cluster {
    id: ID,
    name: "Thermostat User Interface Configuration",
    revision: REVISION,
    pics: "TSUIC",
    derived_from: None,
    features: FEATURES,
    attributes: ATTRIBUTES,
    commands: COMMANDS,
    events: EVENTS,
};
