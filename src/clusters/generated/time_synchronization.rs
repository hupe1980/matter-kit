// Generated by `cargo xtask` from the CSA data model 1.6. Do not edit.
//
// Time Synchronization — cluster 0x0038.
//
// Regenerate with `cargo xtask clusters --dm <checkout>/data_model/1.6`; `cargo xtask check`
// is the CI gate that keeps this file honest. The XML itself is not vendored here — it
// carries the CSA's own copyright notice and is not redistributable.

//! Time Synchronization (cluster `0x0038`, revision 2).
//!
//! 24 identifiers, 4 feature bits, and the specification's own conformance for
//! every element. `CLUSTER` is the whole table.

#![allow(unused_imports)]

use crate::dm::conformance::{Clause, Condition, Conform, Conformance};
use crate::dm::meta::{EventPriority, Reporting};
use crate::dm::spec::{Attribute, Cluster, Command, Event, Feature};
use crate::dm::{Access, AccessQualities, AttributeQualities, Privilege};
use crate::error::Result;
use crate::im::{AttributeId, ClusterId, CommandId, EventId};
use crate::tlv::{
    ContainerKind, Element, FromTlv, Nullable, Tag, TlvList, TlvReader, TlvWriter, ToTlv, Value,
};

/// The cluster id.
pub const ID: ClusterId = 0x0038;

/// The highest revision the specification's history defines — what `ClusterRevision`
/// must report (Core §7.13.1).
pub const REVISION: u16 = 2;

/// The PICS code, which a certification declaration names the cluster by.
pub const PICS: &str = "TIMESYNC";

/// The `FeatureMap` bits (Core §7.13.2).
///
/// A feature has conformance of its own: a device may not claim one the
/// specification forbids it alongside another.
pub mod feature {
    /// Server supports time zone.
    ///
    /// Code `TZ`, bit 0.
    pub const TIME_ZONE: u32 = 1 << 0;
    /// Server supports an NTP or SNTP client.
    ///
    /// Code `NTPC`, bit 1.
    pub const NTP_CLIENT: u32 = 1 << 1;
    /// Server supports an NTP server role.
    ///
    /// Code `NTPS`, bit 2.
    pub const NTP_SERVER: u32 = 1 << 2;
    /// Time synchronization client cluster is present.
    ///
    /// Code `TSC`, bit 3.
    pub const TIME_SYNC_CLIENT: u32 = 1 << 3;
}

/// Every feature bit, with its conformance.
pub const FEATURES: &[Feature] = &[
    Feature {
        bit: 0,
        code: "TZ",
        name: "TimeZone",
        conform: Conform::OPTIONAL,
    },
    Feature {
        bit: 1,
        code: "NTPC",
        name: "NTPClient",
        conform: Conform::OPTIONAL,
    },
    Feature {
        bit: 2,
        code: "NTPS",
        name: "NTPServer",
        conform: Conform::OPTIONAL,
    },
    Feature {
        bit: 3,
        code: "TSC",
        name: "TimeSyncClient",
        conform: Conform::OPTIONAL,
    },
];

/// The attribute ids.
pub mod attribute {
    use crate::im::AttributeId;

    /// `UTCTime`.
    pub const UTC_TIME: AttributeId = 0x00;
    /// `Granularity`.
    pub const GRANULARITY: AttributeId = 0x01;
    /// `TimeSource`.
    pub const TIME_SOURCE: AttributeId = 0x02;
    /// `TrustedTimeSource`.
    pub const TRUSTED_TIME_SOURCE: AttributeId = 0x03;
    /// `DefaultNTP`.
    pub const DEFAULT_NTP: AttributeId = 0x04;
    /// `TimeZone`.
    pub const TIME_ZONE: AttributeId = 0x05;
    /// `DSTOffset`.
    pub const DST_OFFSET: AttributeId = 0x06;
    /// `LocalTime`.
    pub const LOCAL_TIME: AttributeId = 0x07;
    /// `TimeZoneDatabase`.
    pub const TIME_ZONE_DATABASE: AttributeId = 0x08;
    /// `NTPServerAvailable`.
    pub const NTP_SERVER_AVAILABLE: AttributeId = 0x09;
    /// `TimeZoneListMaxSize`.
    pub const TIME_ZONE_LIST_MAX_SIZE: AttributeId = 0x0A;
    /// `DSTOffsetListMaxSize`.
    pub const DST_OFFSET_LIST_MAX_SIZE: AttributeId = 0x0B;
    /// `SupportsDNSResolve`.
    pub const SUPPORTS_DNS_RESOLVE: AttributeId = 0x0C;
}

/// The command ids.
pub mod command {
    use crate::im::CommandId;

    /// `SetUTCTime`.
    pub const SET_UTC_TIME: CommandId = 0x00;
    /// `SetTrustedTimeSource`.
    pub const SET_TRUSTED_TIME_SOURCE: CommandId = 0x01;
    /// `SetTimeZone`.
    pub const SET_TIME_ZONE: CommandId = 0x02;
    /// `SetTimeZoneResponse`.
    pub const SET_TIME_ZONE_RESPONSE: CommandId = 0x03;
    /// `SetDSTOffset`.
    pub const SET_DST_OFFSET: CommandId = 0x04;
    /// `SetDefaultNTP`.
    pub const SET_DEFAULT_NTP: CommandId = 0x05;
}

/// The event ids.
pub mod event {
    use crate::im::EventId;

    /// `DSTTableEmpty`.
    pub const DST_TABLE_EMPTY: EventId = 0x00;
    /// `DSTStatus`.
    pub const DST_STATUS: EventId = 0x01;
    /// `TimeZoneStatus`.
    pub const TIME_ZONE_STATUS: EventId = 0x02;
    /// `TimeFailure`.
    pub const TIME_FAILURE: EventId = 0x03;
    /// `MissingTrustedTimeSource`.
    pub const MISSING_TRUSTED_TIME_SOURCE: EventId = 0x04;
}

/// `GranularityEnum`.
#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash)]
#[repr(u8)]
pub enum GranularityEnum {
    /// This indicates that the node is not currently synchronized with a UTC Time source and its clock is based on the Last Known Good UTC Time only.
    NoTimeGranularity = 0,
    /// This indicates the node was synchronized to an upstream source in the past, but sufficient clock drift has occurred such that the clock error is now > 5 seconds.
    MinutesGranularity = 1,
    /// This indicates the node is synchronized to an upstream source using a low resolution protocol. UTC Time is accurate to ± 5 seconds.
    SecondsGranularity = 2,
    /// This indicates the node is synchronized to an upstream source using high resolution time-synchronization protocol such as NTP, or has built-in GNSS with some amount of jitter applying its GNSS timestamp. UTC Time is accurate to ± 50 ms.
    MillisecondsGranularity = 3,
    /// This indicates the node is synchronized to an upstream source using a highly precise time-synchronization protocol such as PTP, or has built-in GNSS. UTC time is accurate to ± 10 μs.
    MicrosecondsGranularity = 4,
}

impl GranularityEnum {
    /// Reads a wire value, or `None` for one this revision does not define.
    #[must_use]
    pub const fn from_value(value: u8) -> Option<Self> {
        match value {
            0 => Some(Self::NoTimeGranularity),
            1 => Some(Self::MinutesGranularity),
            2 => Some(Self::SecondsGranularity),
            3 => Some(Self::MillisecondsGranularity),
            4 => Some(Self::MicrosecondsGranularity),
            _ => None,
        }
    }

    /// Its wire value.
    #[must_use]
    pub const fn value(self) -> u8 {
        self as u8
    }
}

impl ToTlv for GranularityEnum {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        self.value().to_tlv(w, tag)
    }
}

impl<'a> FromTlv<'a> for GranularityEnum {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        let value = <u8 as FromTlv<'a>>::from_tlv(reader, element)?;
        Self::from_value(value).ok_or(crate::Error::new(crate::ErrorCode::TlvOutOfRange))
    }
}

/// `StatusCodeEnum`.
#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash)]
#[repr(u8)]
pub enum StatusCodeEnum {
    /// Node rejected the attempt to set the UTC time
    TimeNotAccepted = 2,
}

impl StatusCodeEnum {
    /// Reads a wire value, or `None` for one this revision does not define.
    #[must_use]
    pub const fn from_value(value: u8) -> Option<Self> {
        match value {
            2 => Some(Self::TimeNotAccepted),
            _ => None,
        }
    }

    /// Its wire value.
    #[must_use]
    pub const fn value(self) -> u8 {
        self as u8
    }
}

impl ToTlv for StatusCodeEnum {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        self.value().to_tlv(w, tag)
    }
}

impl<'a> FromTlv<'a> for StatusCodeEnum {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        let value = <u8 as FromTlv<'a>>::from_tlv(reader, element)?;
        Self::from_value(value).ok_or(crate::Error::new(crate::ErrorCode::TlvOutOfRange))
    }
}

/// `TimeSourceEnum`.
#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash)]
#[repr(u8)]
pub enum TimeSourceEnum {
    /// Node is not currently synchronized with a UTC Time source.
    None = 0,
    /// Node uses an unlisted time source.
    Unknown = 1,
    /// Node received time from a client using the SetUTCTime Command.
    Admin = 2,
    /// Synchronized time by querying the Time Synchronization cluster of another Node.
    NodeTimeCluster = 3,
    /// SNTP from a server not in the Matter network. NTS is not used.
    NonMatterSNTP = 4,
    /// NTP from servers not in the Matter network. None of the servers used NTS.
    NonMatterNTP = 5,
    /// SNTP from a server within the Matter network. NTS is not used.
    MatterSNTP = 6,
    /// NTP from servers within the Matter network. None of the servers used NTS.
    MatterNTP = 7,
    /// NTP from multiple servers in the Matter network and external. None of the servers used NTS.
    MixedNTP = 8,
    /// SNTP from a server not in the Matter network. NTS is used.
    NonMatterSNTPNTS = 9,
    /// NTP from servers not in the Matter network. NTS is used on at least one server.
    NonMatterNTPNTS = 10,
    /// SNTP from a server within the Matter network. NTS is used.
    MatterSNTPNTS = 11,
    /// NTP from a server within the Matter network. NTS is used on at least one server.
    MatterNTPNTS = 12,
    /// NTP from multiple servers in the Matter network and external. NTS is used on at least one server.
    MixedNTPNTS = 13,
    /// Time synchronization comes from a vendor cloud-based source (e.g. "Date" header in authenticated HTTPS connection).
    CloudSource = 14,
    /// Time synchronization comes from PTP.
    PTP = 15,
    /// Time synchronization comes from a GNSS source.
    GNSS = 16,
}

impl TimeSourceEnum {
    /// Reads a wire value, or `None` for one this revision does not define.
    #[must_use]
    pub const fn from_value(value: u8) -> Option<Self> {
        match value {
            0 => Some(Self::None),
            1 => Some(Self::Unknown),
            2 => Some(Self::Admin),
            3 => Some(Self::NodeTimeCluster),
            4 => Some(Self::NonMatterSNTP),
            5 => Some(Self::NonMatterNTP),
            6 => Some(Self::MatterSNTP),
            7 => Some(Self::MatterNTP),
            8 => Some(Self::MixedNTP),
            9 => Some(Self::NonMatterSNTPNTS),
            10 => Some(Self::NonMatterNTPNTS),
            11 => Some(Self::MatterSNTPNTS),
            12 => Some(Self::MatterNTPNTS),
            13 => Some(Self::MixedNTPNTS),
            14 => Some(Self::CloudSource),
            15 => Some(Self::PTP),
            16 => Some(Self::GNSS),
            _ => None,
        }
    }

    /// Its wire value.
    #[must_use]
    pub const fn value(self) -> u8 {
        self as u8
    }
}

impl ToTlv for TimeSourceEnum {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        self.value().to_tlv(w, tag)
    }
}

impl<'a> FromTlv<'a> for TimeSourceEnum {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        let value = <u8 as FromTlv<'a>>::from_tlv(reader, element)?;
        Self::from_value(value).ok_or(crate::Error::new(crate::ErrorCode::TlvOutOfRange))
    }
}

/// `TimeZoneDatabaseEnum`.
#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash)]
#[repr(u8)]
pub enum TimeZoneDatabaseEnum {
    /// Node has a full list of the available time zones
    Full = 0,
    /// Node has a partial list of the available time zones
    Partial = 1,
    /// Node does not have a time zone database
    None = 2,
}

impl TimeZoneDatabaseEnum {
    /// Reads a wire value, or `None` for one this revision does not define.
    #[must_use]
    pub const fn from_value(value: u8) -> Option<Self> {
        match value {
            0 => Some(Self::Full),
            1 => Some(Self::Partial),
            2 => Some(Self::None),
            _ => None,
        }
    }

    /// Its wire value.
    #[must_use]
    pub const fn value(self) -> u8 {
        self as u8
    }
}

impl ToTlv for TimeZoneDatabaseEnum {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        self.value().to_tlv(w, tag)
    }
}

impl<'a> FromTlv<'a> for TimeZoneDatabaseEnum {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        let value = <u8 as FromTlv<'a>>::from_tlv(reader, element)?;
        Self::from_value(value).ok_or(crate::Error::new(crate::ErrorCode::TlvOutOfRange))
    }
}

/// `DSTOffsetStruct`.
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct DSTOffsetStruct {
    /// `Offset`, field 0.
    pub offset: i32,
    /// `ValidStarting`, field 1.
    pub valid_starting: u64,
    /// `ValidUntil`, field 2.
    pub valid_until: Nullable<u64>,
}

impl ToTlv for DSTOffsetStruct {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.offset.to_tlv(w, Tag::Context(0))?;
        self.valid_starting.to_tlv(w, Tag::Context(1))?;
        self.valid_until.to_tlv(w, Tag::Context(2))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for DSTOffsetStruct {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut offset = None;
        let mut valid_starting = None;
        let mut valid_until = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => offset = Some(<i32>::from_tlv(reader, &field)?),
                Some(1) => valid_starting = Some(<u64>::from_tlv(reader, &field)?),
                Some(2) => valid_until = Some(<Nullable<u64>>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            offset: offset.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            valid_starting: valid_starting
                .ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            valid_until: valid_until.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// `FabricScopedTrustedTimeSourceStruct`.
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct FabricScopedTrustedTimeSourceStruct {
    /// `NodeID`, field 0.
    pub node_id: crate::msg::NodeId,
    /// `Endpoint`, field 1.
    pub endpoint: u16,
}

impl ToTlv for FabricScopedTrustedTimeSourceStruct {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.node_id.to_tlv(w, Tag::Context(0))?;
        self.endpoint.to_tlv(w, Tag::Context(1))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for FabricScopedTrustedTimeSourceStruct {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut node_id = None;
        let mut endpoint = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => node_id = Some(<crate::msg::NodeId>::from_tlv(reader, &field)?),
                Some(1) => endpoint = Some(<u16>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            node_id: node_id.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            endpoint: endpoint.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// `TimeZoneStruct`.
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct TimeZoneStruct<'a> {
    /// `Offset`, field 0.
    pub offset: i32,
    /// `ValidAt`, field 1.
    pub valid_at: u64,
    /// `Name`, field 2.
    pub name: Option<&'a str>,
}

impl<'a> ToTlv for TimeZoneStruct<'a> {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.offset.to_tlv(w, Tag::Context(0))?;
        self.valid_at.to_tlv(w, Tag::Context(1))?;
        if let Some(value) = &self.name {
            value.to_tlv(w, Tag::Context(2))?;
        }
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for TimeZoneStruct<'a> {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut offset = None;
        let mut valid_at = None;
        let mut name = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => offset = Some(<i32>::from_tlv(reader, &field)?),
                Some(1) => valid_at = Some(<u64>::from_tlv(reader, &field)?),
                Some(2) => name = Some(<&'a str>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            offset: offset.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            valid_at: valid_at.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            name,
        })
    }
}

/// `TrustedTimeSourceStruct`.
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct TrustedTimeSourceStruct {
    /// `FabricIndex`, field 0.
    pub fabric_index: crate::msg::FabricIndex,
    /// `NodeID`, field 1.
    pub node_id: crate::msg::NodeId,
    /// `Endpoint`, field 2.
    pub endpoint: u16,
}

impl ToTlv for TrustedTimeSourceStruct {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.fabric_index.to_tlv(w, Tag::Context(0))?;
        self.node_id.to_tlv(w, Tag::Context(1))?;
        self.endpoint.to_tlv(w, Tag::Context(2))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for TrustedTimeSourceStruct {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut fabric_index = None;
        let mut node_id = None;
        let mut endpoint = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => {
                    fabric_index = Some(<crate::msg::FabricIndex>::from_tlv(reader, &field)?)
                }
                Some(1) => node_id = Some(<crate::msg::NodeId>::from_tlv(reader, &field)?),
                Some(2) => endpoint = Some(<u16>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            fabric_index: fabric_index.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            node_id: node_id.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            endpoint: endpoint.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// The fields of `SetUTCTime` (command `0x00`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct SetUTCTimeFields {
    /// `UTCTime`, field 0.
    pub utc_time: u64,
    /// `Granularity`, field 1.
    pub granularity: GranularityEnum,
    /// `TimeSource`, field 2.
    pub time_source: Option<TimeSourceEnum>,
}

impl ToTlv for SetUTCTimeFields {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.utc_time.to_tlv(w, Tag::Context(0))?;
        self.granularity.to_tlv(w, Tag::Context(1))?;
        if let Some(value) = &self.time_source {
            value.to_tlv(w, Tag::Context(2))?;
        }
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for SetUTCTimeFields {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut utc_time = None;
        let mut granularity = None;
        let mut time_source = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => utc_time = Some(<u64>::from_tlv(reader, &field)?),
                Some(1) => granularity = Some(<GranularityEnum>::from_tlv(reader, &field)?),
                Some(2) => time_source = Some(<TimeSourceEnum>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            utc_time: utc_time.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            granularity: granularity.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            time_source,
        })
    }
}

/// The fields of `SetTrustedTimeSource` (command `0x01`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct SetTrustedTimeSourceFields {
    /// `TrustedTimeSource`, field 0.
    pub trusted_time_source: Nullable<FabricScopedTrustedTimeSourceStruct>,
}

impl ToTlv for SetTrustedTimeSourceFields {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.trusted_time_source.to_tlv(w, Tag::Context(0))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for SetTrustedTimeSourceFields {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut trusted_time_source = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => {
                    trusted_time_source = Some(
                        <Nullable<FabricScopedTrustedTimeSourceStruct>>::from_tlv(reader, &field)?,
                    )
                }
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            trusted_time_source: trusted_time_source
                .ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// The fields of `SetTimeZone` (command `0x02`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct SetTimeZoneFields<'a> {
    /// `TimeZone`, field 0.
    pub time_zone: TlvList<'a, TimeZoneStruct<'a>>,
}

impl<'a> ToTlv for SetTimeZoneFields<'a> {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.time_zone.to_tlv(w, Tag::Context(0))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for SetTimeZoneFields<'a> {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut time_zone = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => {
                    time_zone = Some(<TlvList<'a, TimeZoneStruct<'a>>>::from_tlv(reader, &field)?)
                }
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            time_zone: time_zone.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// The fields of `SetTimeZoneResponse` (command `0x03`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct SetTimeZoneResponseFields {
    /// `DSTOffsetRequired`, field 0.
    pub dst_offset_required: bool,
}

impl ToTlv for SetTimeZoneResponseFields {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.dst_offset_required.to_tlv(w, Tag::Context(0))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for SetTimeZoneResponseFields {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut dst_offset_required = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => dst_offset_required = Some(<bool>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            dst_offset_required: dst_offset_required
                .ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// The fields of `SetDSTOffset` (command `0x04`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct SetDSTOffsetFields<'a> {
    /// `DSTOffset`, field 0.
    pub dst_offset: TlvList<'a, DSTOffsetStruct>,
}

impl<'a> ToTlv for SetDSTOffsetFields<'a> {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.dst_offset.to_tlv(w, Tag::Context(0))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for SetDSTOffsetFields<'a> {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut dst_offset = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => {
                    dst_offset = Some(<TlvList<'a, DSTOffsetStruct>>::from_tlv(reader, &field)?)
                }
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            dst_offset: dst_offset.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// The fields of `SetDefaultNTP` (command `0x05`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct SetDefaultNTPFields<'a> {
    /// `DefaultNTP`, field 0.
    pub default_ntp: Nullable<&'a str>,
}

impl<'a> ToTlv for SetDefaultNTPFields<'a> {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.default_ntp.to_tlv(w, Tag::Context(0))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for SetDefaultNTPFields<'a> {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut default_ntp = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => default_ntp = Some(<Nullable<&'a str>>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            default_ntp: default_ntp.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// Every attribute the specification defines, with its conformance.
pub const ATTRIBUTES: &[Attribute] = &[
    Attribute {
        id: 0x0000,
        name: "UTCTime",
        kind: "epoch-us",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::NULLABLE,
        reporting: Reporting::ChangesOmitted,
        conform: Conform::MANDATORY,
    },
    Attribute {
        id: 0x0001,
        name: "Granularity",
        kind: "GranularityEnum",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::empty(),
        reporting: Reporting::OnChange,
        conform: Conform::MANDATORY,
    },
    Attribute {
        id: 0x0002,
        name: "TimeSource",
        kind: "TimeSourceEnum",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::empty(),
        reporting: Reporting::OnChange,
        conform: Conform::OPTIONAL,
    },
    Attribute {
        id: 0x0003,
        name: "TrustedTimeSource",
        kind: "TrustedTimeSourceStruct",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::NULLABLE.union(AttributeQualities::NON_VOLATILE),
        reporting: Reporting::OnChange,
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(3),
            }],
        },
    },
    Attribute {
        id: 0x0004,
        name: "DefaultNTP",
        kind: "string",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::NULLABLE.union(AttributeQualities::NON_VOLATILE),
        reporting: Reporting::OnChange,
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(1),
            }],
        },
    },
    Attribute {
        id: 0x0005,
        name: "TimeZone",
        kind: "list[TimeZoneStruct]",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::NON_VOLATILE,
        reporting: Reporting::OnChange,
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(0),
            }],
        },
    },
    Attribute {
        id: 0x0006,
        name: "DSTOffset",
        kind: "list[DSTOffsetStruct]",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::NON_VOLATILE,
        reporting: Reporting::OnChange,
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(0),
            }],
        },
    },
    Attribute {
        id: 0x0007,
        name: "LocalTime",
        kind: "epoch-us",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::NULLABLE,
        reporting: Reporting::ChangesOmitted,
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(0),
            }],
        },
    },
    Attribute {
        id: 0x0008,
        name: "TimeZoneDatabase",
        kind: "TimeZoneDatabaseEnum",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::FIXED,
        reporting: Reporting::OnChange,
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(0),
            }],
        },
    },
    Attribute {
        id: 0x0009,
        name: "NTPServerAvailable",
        kind: "bool",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::empty(),
        reporting: Reporting::OnChange,
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(2),
            }],
        },
    },
    Attribute {
        id: 0x000A,
        name: "TimeZoneListMaxSize",
        kind: "uint8",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::FIXED,
        reporting: Reporting::OnChange,
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(0),
            }],
        },
    },
    Attribute {
        id: 0x000B,
        name: "DSTOffsetListMaxSize",
        kind: "uint8",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::FIXED,
        reporting: Reporting::OnChange,
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(0),
            }],
        },
    },
    Attribute {
        id: 0x000C,
        name: "SupportsDNSResolve",
        kind: "bool",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::FIXED,
        reporting: Reporting::OnChange,
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(1),
            }],
        },
    },
];

/// Every command, with its conformance.
pub const COMMANDS: &[Command] = &[
    Command {
        id: 0x00,
        name: "SetUTCTime",
        to_server: true,
        response: None,
        access: Access::invoke(Privilege::Administer),
        conform: Conform::MANDATORY,
    },
    Command {
        id: 0x01,
        name: "SetTrustedTimeSource",
        to_server: true,
        response: None,
        access: Access::invoke(Privilege::Administer)
            .with_qualities(AccessQualities::FABRIC_SCOPED),
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(3),
            }],
        },
    },
    Command {
        id: 0x02,
        name: "SetTimeZone",
        to_server: true,
        response: Some(0x03),
        access: Access::invoke(Privilege::Manage),
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(0),
            }],
        },
    },
    Command {
        id: 0x03,
        name: "SetTimeZoneResponse",
        to_server: false,
        response: None,
        access: Access::invoke(Privilege::Operate),
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(0),
            }],
        },
    },
    Command {
        id: 0x04,
        name: "SetDSTOffset",
        to_server: true,
        response: None,
        access: Access::invoke(Privilege::Manage),
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(0),
            }],
        },
    },
    Command {
        id: 0x05,
        name: "SetDefaultNTP",
        to_server: true,
        response: None,
        access: Access::invoke(Privilege::Administer),
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(1),
            }],
        },
    },
];

/// Every event, with its conformance.
pub const EVENTS: &[Event] = &[
    Event {
        id: 0x00,
        name: "DSTTableEmpty",
        priority: EventPriority::Info,
        access: Access::read_only(Privilege::View),
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(0),
            }],
        },
    },
    Event {
        id: 0x01,
        name: "DSTStatus",
        priority: EventPriority::Info,
        access: Access::read_only(Privilege::View),
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(0),
            }],
        },
    },
    Event {
        id: 0x02,
        name: "TimeZoneStatus",
        priority: EventPriority::Info,
        access: Access::read_only(Privilege::View),
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(0),
            }],
        },
    },
    Event {
        id: 0x03,
        name: "TimeFailure",
        priority: EventPriority::Info,
        access: Access::read_only(Privilege::View),
        conform: Conform::MANDATORY,
    },
    Event {
        id: 0x04,
        name: "MissingTrustedTimeSource",
        priority: EventPriority::Info,
        access: Access::read_only(Privilege::View),
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(3),
            }],
        },
    },
];

/// The whole cluster, as the specification defines it.
pub const CLUSTER: Cluster = Cluster {
    id: ID,
    name: "Time Synchronization",
    revision: REVISION,
    pics: "TIMESYNC",
    derived_from: None,
    features: FEATURES,
    attributes: ATTRIBUTES,
    commands: COMMANDS,
    events: EVENTS,
};
