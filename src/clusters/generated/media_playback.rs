// Generated by `cargo xtask` from the CSA data model 1.6. Do not edit.
//
// Media Playback — cluster 0x0506.
//
// Regenerate with `cargo xtask clusters --dm <checkout>/data_model/1.6`; `cargo xtask check`
// is the CI gate that keeps this file honest. The XML itself is not vendored here — it
// carries the CSA's own copyright notice and is not redistributable.

//! Media Playback (cluster `0x0506`, revision 2).
//!
//! 27 identifiers, 5 feature bits, and the specification's own conformance for
//! every element. `CLUSTER` is the whole table.

#![allow(unused_imports)]

use crate::dm::conformance::{Clause, Condition, Conform, Conformance};
use crate::dm::meta::{EventPriority, Reporting};
use crate::dm::spec::{Attribute, Cluster, Command, Event, Feature};
use crate::dm::{Access, AccessQualities, AttributeQualities, Privilege};
use crate::error::Result;
use crate::im::{AttributeId, ClusterId, CommandId, EventId};
use crate::tlv::{
    ContainerKind, Element, FromTlv, Nullable, Tag, TlvList, TlvReader, TlvWriter, ToTlv, Value,
};

/// The cluster id.
pub const ID: ClusterId = 0x0506;

/// The highest revision the specification's history defines — what `ClusterRevision`
/// must report (Core §7.13.1).
pub const REVISION: u16 = 2;

/// The PICS code, which a certification declaration names the cluster by.
pub const PICS: &str = "MEDIAPLAYBACK";

/// The `FeatureMap` bits (Core §7.13.2).
///
/// A feature has conformance of its own: a device may not claim one the
/// specification forbids it alongside another.
pub mod feature {
    /// Advanced media seeking
    ///
    /// Code `AS`, bit 0.
    pub const ADVANCED_SEEK: u32 = 1 << 0;
    /// Variable speed playback
    ///
    /// Code `VS`, bit 1.
    pub const VARIABLE_SPEED: u32 = 1 << 1;
    /// Text Tracks
    ///
    /// Code `TT`, bit 2.
    pub const TEXT_TRACKS: u32 = 1 << 2;
    /// Audio Tracks
    ///
    /// Code `AT`, bit 3.
    pub const AUDIO_TRACKS: u32 = 1 << 3;
    /// Can play audio during fast and slow playback speeds
    ///
    /// Code `AA`, bit 4.
    pub const AUDIO_ADVANCE: u32 = 1 << 4;
}

/// Every feature bit, with its conformance.
pub const FEATURES: &[Feature] = &[
    Feature {
        bit: 0,
        code: "AS",
        name: "AdvancedSeek",
        conform: Conform::OPTIONAL,
    },
    Feature {
        bit: 1,
        code: "VS",
        name: "VariableSpeed",
        conform: Conform::OPTIONAL,
    },
    Feature {
        bit: 2,
        code: "TT",
        name: "TextTracks",
        conform: Conform::OPTIONAL,
    },
    Feature {
        bit: 3,
        code: "AT",
        name: "AudioTracks",
        conform: Conform::OPTIONAL,
    },
    Feature {
        bit: 4,
        code: "AA",
        name: "AudioAdvance",
        conform: Conform::OPTIONAL,
    },
];

/// The attribute ids.
pub mod attribute {
    use crate::im::AttributeId;

    /// `CurrentState`.
    pub const CURRENT_STATE: AttributeId = 0x00;
    /// `StartTime`.
    pub const START_TIME: AttributeId = 0x01;
    /// `Duration`.
    pub const DURATION: AttributeId = 0x02;
    /// `SampledPosition`.
    pub const SAMPLED_POSITION: AttributeId = 0x03;
    /// `PlaybackSpeed`.
    pub const PLAYBACK_SPEED: AttributeId = 0x04;
    /// `SeekRangeEnd`.
    pub const SEEK_RANGE_END: AttributeId = 0x05;
    /// `SeekRangeStart`.
    pub const SEEK_RANGE_START: AttributeId = 0x06;
    /// `ActiveAudioTrack`.
    pub const ACTIVE_AUDIO_TRACK: AttributeId = 0x07;
    /// `AvailableAudioTracks`.
    pub const AVAILABLE_AUDIO_TRACKS: AttributeId = 0x08;
    /// `ActiveTextTrack`.
    pub const ACTIVE_TEXT_TRACK: AttributeId = 0x09;
    /// `AvailableTextTracks`.
    pub const AVAILABLE_TEXT_TRACKS: AttributeId = 0x0A;
}

/// The command ids.
pub mod command {
    use crate::im::CommandId;

    /// `Play`.
    pub const PLAY: CommandId = 0x00;
    /// `Pause`.
    pub const PAUSE: CommandId = 0x01;
    /// `Stop`.
    pub const STOP: CommandId = 0x02;
    /// `StartOver`.
    pub const START_OVER: CommandId = 0x03;
    /// `Previous`.
    pub const PREVIOUS: CommandId = 0x04;
    /// `Next`.
    pub const NEXT: CommandId = 0x05;
    /// `Rewind`.
    pub const REWIND: CommandId = 0x06;
    /// `FastForward`.
    pub const FAST_FORWARD: CommandId = 0x07;
    /// `SkipForward`.
    pub const SKIP_FORWARD: CommandId = 0x08;
    /// `SkipBackward`.
    pub const SKIP_BACKWARD: CommandId = 0x09;
    /// `PlaybackResponse`.
    pub const PLAYBACK_RESPONSE: CommandId = 0x0A;
    /// `Seek`.
    pub const SEEK: CommandId = 0x0B;
    /// `ActivateAudioTrack`.
    pub const ACTIVATE_AUDIO_TRACK: CommandId = 0x0C;
    /// `ActivateTextTrack`.
    pub const ACTIVATE_TEXT_TRACK: CommandId = 0x0D;
    /// `DeactivateTextTrack`.
    pub const DEACTIVATE_TEXT_TRACK: CommandId = 0x0E;
}

/// The event ids.
pub mod event {
    use crate::im::EventId;

    /// `StateChanged`.
    pub const STATE_CHANGED: EventId = 0x00;
}

/// `CharacteristicEnum`.
#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash)]
#[repr(u8)]
pub enum CharacteristicEnum {
    /// Textual information meant for display when no other text representation is selected. It is used to clarify dialogue, alternate languages, texted graphics or location/person IDs that are not otherwise covered in the dubbed/localized audio.
    ForcedSubtitles = 0,
    /// Textual or audio media component containing a textual description (intended for audio synthesis) or an audio description describing a visual component
    DescribesVideo = 1,
    /// Simplified or reduced captions as specified in [United States Code Title 47 CFR 79.103(c)(9)].
    EasyToRead = 2,
    /// A media characteristic that indicates that a track selection option includes frame-based content.
    FrameBased = 3,
    /// Main media component(s) which is/are intended for presentation if no other information is provided
    MainProgram = 4,
    /// A media characteristic that indicates that a track or media selection option contains original content.
    OriginalContent = 5,
    /// A media characteristic that indicates that a track or media selection option contains a language translation and verbal interpretation of spoken dialog.
    VoiceOverTranslation = 6,
    /// Textual media component containing transcriptions of spoken dialog and auditory cues such as sound effects and music for the hearing impaired.
    Caption = 7,
    /// Textual transcriptions of spoken dialog.
    Subtitle = 8,
    /// Textual media component containing transcriptions of spoken dialog and auditory cues such as sound effects and music for the hearing impaired.
    Alternate = 9,
    /// Media content component that is supplementary to a media content component of a different media component type.
    Supplementary = 10,
    /// Experience that contains a commentary (e.g. director’s commentary) (typically audio)
    Commentary = 11,
    /// Experience that contains an element that is presented in a different language from the original (e.g. dubbed audio, translated captions)
    DubbedTranslation = 12,
    /// Textual or audio media component containing a textual description (intended for audio synthesis) or an audio description describing a visual component
    Description = 13,
    /// Media component containing information intended to be processed by application specific elements.
    Metadata = 14,
    /// Experience containing an element for improved intelligibility of the dialogue.
    EnhancedAudioIntelligibility = 15,
    /// Experience that provides information, about a current emergency, that is intended to enable the protection of life, health, safety, and property, and may also include critical details regarding the emergency and how to respond to the emergency.
    Emergency = 16,
    /// Textual representation of a songs’ lyrics, usually in the same language as the associated song as specified in [SMPTE ST 2067-2].
    Karaoke = 17,
}

impl CharacteristicEnum {
    /// Reads a wire value, or `None` for one this revision does not define.
    #[must_use]
    pub const fn from_value(value: u8) -> Option<Self> {
        match value {
            0 => Some(Self::ForcedSubtitles),
            1 => Some(Self::DescribesVideo),
            2 => Some(Self::EasyToRead),
            3 => Some(Self::FrameBased),
            4 => Some(Self::MainProgram),
            5 => Some(Self::OriginalContent),
            6 => Some(Self::VoiceOverTranslation),
            7 => Some(Self::Caption),
            8 => Some(Self::Subtitle),
            9 => Some(Self::Alternate),
            10 => Some(Self::Supplementary),
            11 => Some(Self::Commentary),
            12 => Some(Self::DubbedTranslation),
            13 => Some(Self::Description),
            14 => Some(Self::Metadata),
            15 => Some(Self::EnhancedAudioIntelligibility),
            16 => Some(Self::Emergency),
            17 => Some(Self::Karaoke),
            _ => None,
        }
    }

    /// Its wire value.
    #[must_use]
    pub const fn value(self) -> u8 {
        self as u8
    }
}

impl ToTlv for CharacteristicEnum {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        self.value().to_tlv(w, tag)
    }
}

impl<'a> FromTlv<'a> for CharacteristicEnum {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        let value = <u8 as FromTlv<'a>>::from_tlv(reader, element)?;
        Self::from_value(value).ok_or(crate::Error::new(crate::ErrorCode::TlvOutOfRange))
    }
}

/// `PlaybackStateEnum`.
#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash)]
#[repr(u8)]
pub enum PlaybackStateEnum {
    /// Media is currently playing (includes FF and REW)
    Playing = 0,
    /// Media is currently paused
    Paused = 1,
    /// Media is not currently playing
    NotPlaying = 2,
    /// Media is not currently buffering and playback will start when buffer has been filled
    Buffering = 3,
}

impl PlaybackStateEnum {
    /// Reads a wire value, or `None` for one this revision does not define.
    #[must_use]
    pub const fn from_value(value: u8) -> Option<Self> {
        match value {
            0 => Some(Self::Playing),
            1 => Some(Self::Paused),
            2 => Some(Self::NotPlaying),
            3 => Some(Self::Buffering),
            _ => None,
        }
    }

    /// Its wire value.
    #[must_use]
    pub const fn value(self) -> u8 {
        self as u8
    }
}

impl ToTlv for PlaybackStateEnum {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        self.value().to_tlv(w, tag)
    }
}

impl<'a> FromTlv<'a> for PlaybackStateEnum {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        let value = <u8 as FromTlv<'a>>::from_tlv(reader, element)?;
        Self::from_value(value).ok_or(crate::Error::new(crate::ErrorCode::TlvOutOfRange))
    }
}

/// `StatusEnum`.
#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash)]
#[repr(u8)]
pub enum StatusEnum {
    /// Succeeded
    Success = 0,
    /// Requested playback command is invalid in the current playback state.
    InvalidStateForCommand = 1,
    /// Requested playback command is not allowed in the current playback state. For example, attempting to fast-forward during a commercial might return NotAllowed.
    NotAllowed = 2,
    /// This endpoint is not active for playback.
    NotActive = 3,
    /// The FastForward or Rewind Command was issued but the media is already playing back at the fastest speed supported by the server in the respective direction.
    SpeedOutOfRange = 4,
    /// The Seek Command was issued with a value of position outside of the allowed seek range of the media.
    SeekOutOfRange = 5,
}

impl StatusEnum {
    /// Reads a wire value, or `None` for one this revision does not define.
    #[must_use]
    pub const fn from_value(value: u8) -> Option<Self> {
        match value {
            0 => Some(Self::Success),
            1 => Some(Self::InvalidStateForCommand),
            2 => Some(Self::NotAllowed),
            3 => Some(Self::NotActive),
            4 => Some(Self::SpeedOutOfRange),
            5 => Some(Self::SeekOutOfRange),
            _ => None,
        }
    }

    /// Its wire value.
    #[must_use]
    pub const fn value(self) -> u8 {
        self as u8
    }
}

impl ToTlv for StatusEnum {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        self.value().to_tlv(w, tag)
    }
}

impl<'a> FromTlv<'a> for StatusEnum {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        let value = <u8 as FromTlv<'a>>::from_tlv(reader, element)?;
        Self::from_value(value).ok_or(crate::Error::new(crate::ErrorCode::TlvOutOfRange))
    }
}

/// `PlaybackPositionStruct`.
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct PlaybackPositionStruct {
    /// `UpdatedAt`, field 0.
    pub updated_at: u64,
    /// `Position`, field 1.
    pub position: Nullable<u64>,
}

impl ToTlv for PlaybackPositionStruct {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.updated_at.to_tlv(w, Tag::Context(0))?;
        self.position.to_tlv(w, Tag::Context(1))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for PlaybackPositionStruct {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut updated_at = None;
        let mut position = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => updated_at = Some(<u64>::from_tlv(reader, &field)?),
                Some(1) => position = Some(<Nullable<u64>>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            updated_at: updated_at.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            position: position.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// `TrackAttributesStruct`.
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct TrackAttributesStruct<'a> {
    /// `LanguageCode`, field 0.
    pub language_code: &'a str,
    /// `Characteristics`, field 1.
    pub characteristics: Option<Nullable<TlvList<'a, CharacteristicEnum>>>,
    /// `DisplayName`, field 2.
    pub display_name: Option<Nullable<&'a str>>,
}

impl<'a> ToTlv for TrackAttributesStruct<'a> {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.language_code.to_tlv(w, Tag::Context(0))?;
        if let Some(value) = &self.characteristics {
            value.to_tlv(w, Tag::Context(1))?;
        }
        if let Some(value) = &self.display_name {
            value.to_tlv(w, Tag::Context(2))?;
        }
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for TrackAttributesStruct<'a> {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut language_code = None;
        let mut characteristics = None;
        let mut display_name = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => language_code = Some(<&'a str>::from_tlv(reader, &field)?),
                Some(1) => {
                    characteristics = Some(<Nullable<TlvList<'a, CharacteristicEnum>>>::from_tlv(
                        reader, &field,
                    )?)
                }
                Some(2) => display_name = Some(<Nullable<&'a str>>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            language_code: language_code.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            characteristics,
            display_name,
        })
    }
}

/// `TrackStruct`.
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct TrackStruct<'a> {
    /// `ID`, field 0.
    pub id: &'a str,
    /// `TrackAttributes`, field 1.
    pub track_attributes: TrackAttributesStruct<'a>,
}

impl<'a> ToTlv for TrackStruct<'a> {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.id.to_tlv(w, Tag::Context(0))?;
        self.track_attributes.to_tlv(w, Tag::Context(1))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for TrackStruct<'a> {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut id = None;
        let mut track_attributes = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => id = Some(<&'a str>::from_tlv(reader, &field)?),
                Some(1) => {
                    track_attributes = Some(<TrackAttributesStruct<'a>>::from_tlv(reader, &field)?)
                }
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            id: id.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            track_attributes: track_attributes
                .ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// The fields of `Rewind` (command `0x06`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct RewindFields {
    /// `AudioAdvanceUnmuted`, field 0.
    pub audio_advance_unmuted: Option<bool>,
}

impl ToTlv for RewindFields {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        if let Some(value) = &self.audio_advance_unmuted {
            value.to_tlv(w, Tag::Context(0))?;
        }
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for RewindFields {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut audio_advance_unmuted = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => audio_advance_unmuted = Some(<bool>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            audio_advance_unmuted,
        })
    }
}

/// The fields of `FastForward` (command `0x07`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct FastForwardFields {
    /// `AudioAdvanceUnmuted`, field 0.
    pub audio_advance_unmuted: Option<bool>,
}

impl ToTlv for FastForwardFields {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        if let Some(value) = &self.audio_advance_unmuted {
            value.to_tlv(w, Tag::Context(0))?;
        }
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for FastForwardFields {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut audio_advance_unmuted = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => audio_advance_unmuted = Some(<bool>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            audio_advance_unmuted,
        })
    }
}

/// The fields of `SkipForward` (command `0x08`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct SkipForwardFields {
    /// `DeltaPositionMilliseconds`, field 0.
    pub delta_position_milliseconds: u64,
}

impl ToTlv for SkipForwardFields {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.delta_position_milliseconds
            .to_tlv(w, Tag::Context(0))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for SkipForwardFields {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut delta_position_milliseconds = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => delta_position_milliseconds = Some(<u64>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            delta_position_milliseconds: delta_position_milliseconds
                .ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// The fields of `SkipBackward` (command `0x09`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct SkipBackwardFields {
    /// `DeltaPositionMilliseconds`, field 0.
    pub delta_position_milliseconds: u64,
}

impl ToTlv for SkipBackwardFields {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.delta_position_milliseconds
            .to_tlv(w, Tag::Context(0))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for SkipBackwardFields {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut delta_position_milliseconds = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => delta_position_milliseconds = Some(<u64>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            delta_position_milliseconds: delta_position_milliseconds
                .ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// The fields of `PlaybackResponse` (command `0x0A`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct PlaybackResponseFields<'a> {
    /// `Status`, field 0.
    pub status: StatusEnum,
    /// `Data`, field 1.
    pub data: Option<&'a str>,
}

impl<'a> ToTlv for PlaybackResponseFields<'a> {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.status.to_tlv(w, Tag::Context(0))?;
        if let Some(value) = &self.data {
            value.to_tlv(w, Tag::Context(1))?;
        }
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for PlaybackResponseFields<'a> {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut status = None;
        let mut data = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => status = Some(<StatusEnum>::from_tlv(reader, &field)?),
                Some(1) => data = Some(<&'a str>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            status: status.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            data,
        })
    }
}

/// The fields of `Seek` (command `0x0B`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct SeekFields {
    /// `Position`, field 0.
    pub position: u64,
}

impl ToTlv for SeekFields {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.position.to_tlv(w, Tag::Context(0))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for SeekFields {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut position = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => position = Some(<u64>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            position: position.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// The fields of `ActivateAudioTrack` (command `0x0C`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct ActivateAudioTrackFields<'a> {
    /// `TrackID`, field 0.
    pub track_id: &'a str,
    /// `AudioOutputIndex`, field 1.
    pub audio_output_index: Option<Nullable<u8>>,
}

impl<'a> ToTlv for ActivateAudioTrackFields<'a> {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.track_id.to_tlv(w, Tag::Context(0))?;
        if let Some(value) = &self.audio_output_index {
            value.to_tlv(w, Tag::Context(1))?;
        }
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for ActivateAudioTrackFields<'a> {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut track_id = None;
        let mut audio_output_index = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => track_id = Some(<&'a str>::from_tlv(reader, &field)?),
                Some(1) => audio_output_index = Some(<Nullable<u8>>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            track_id: track_id.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
            audio_output_index,
        })
    }
}

/// The fields of `ActivateTextTrack` (command `0x0D`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct ActivateTextTrackFields<'a> {
    /// `TrackID`, field 0.
    pub track_id: &'a str,
}

impl<'a> ToTlv for ActivateTextTrackFields<'a> {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        self.track_id.to_tlv(w, Tag::Context(0))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for ActivateTextTrackFields<'a> {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut track_id = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => track_id = Some(<&'a str>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            track_id: track_id.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// Every attribute the specification defines, with its conformance.
pub const ATTRIBUTES: &[Attribute] = &[
    Attribute {
        id: 0x0000,
        name: "CurrentState",
        kind: "PlaybackStateEnum",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::empty(),
        reporting: Reporting::OnChange,
        conform: Conform::MANDATORY,
    },
    Attribute {
        id: 0x0001,
        name: "StartTime",
        kind: "epoch-us",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::NULLABLE,
        reporting: Reporting::OnChange,
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(0),
            }],
        },
    },
    Attribute {
        id: 0x0002,
        name: "Duration",
        kind: "uint64",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::NULLABLE,
        reporting: Reporting::OnChange,
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(0),
            }],
        },
    },
    Attribute {
        id: 0x0003,
        name: "SampledPosition",
        kind: "PlaybackPositionStruct",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::NULLABLE,
        reporting: Reporting::OnChange,
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(0),
            }],
        },
    },
    Attribute {
        id: 0x0004,
        name: "PlaybackSpeed",
        kind: "single",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::empty(),
        reporting: Reporting::OnChange,
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(0),
            }],
        },
    },
    Attribute {
        id: 0x0005,
        name: "SeekRangeEnd",
        kind: "uint64",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::NULLABLE,
        reporting: Reporting::OnChange,
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(0),
            }],
        },
    },
    Attribute {
        id: 0x0006,
        name: "SeekRangeStart",
        kind: "uint64",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::NULLABLE,
        reporting: Reporting::OnChange,
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(0),
            }],
        },
    },
    Attribute {
        id: 0x0007,
        name: "ActiveAudioTrack",
        kind: "TrackStruct",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::NULLABLE,
        reporting: Reporting::OnChange,
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(3),
            }],
        },
    },
    Attribute {
        id: 0x0008,
        name: "AvailableAudioTracks",
        kind: "list[TrackStruct]",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::NULLABLE,
        reporting: Reporting::OnChange,
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(3),
            }],
        },
    },
    Attribute {
        id: 0x0009,
        name: "ActiveTextTrack",
        kind: "TrackStruct",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::NULLABLE,
        reporting: Reporting::OnChange,
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(2),
            }],
        },
    },
    Attribute {
        id: 0x000A,
        name: "AvailableTextTracks",
        kind: "list[TrackStruct]",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::NULLABLE,
        reporting: Reporting::OnChange,
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(2),
            }],
        },
    },
];

/// Every command, with its conformance.
pub const COMMANDS: &[Command] = &[
    Command {
        id: 0x00,
        name: "Play",
        to_server: true,
        response: Some(0x0A),
        access: Access::invoke(Privilege::Operate),
        conform: Conform::MANDATORY,
    },
    Command {
        id: 0x01,
        name: "Pause",
        to_server: true,
        response: Some(0x0A),
        access: Access::invoke(Privilege::Operate),
        conform: Conform::MANDATORY,
    },
    Command {
        id: 0x02,
        name: "Stop",
        to_server: true,
        response: Some(0x0A),
        access: Access::invoke(Privilege::Operate),
        conform: Conform::MANDATORY,
    },
    Command {
        id: 0x03,
        name: "StartOver",
        to_server: true,
        response: Some(0x0A),
        access: Access::invoke(Privilege::Operate),
        conform: Conform::OPTIONAL,
    },
    Command {
        id: 0x04,
        name: "Previous",
        to_server: true,
        response: Some(0x0A),
        access: Access::invoke(Privilege::Operate),
        conform: Conform::OPTIONAL,
    },
    Command {
        id: 0x05,
        name: "Next",
        to_server: true,
        response: Some(0x0A),
        access: Access::invoke(Privilege::Operate),
        conform: Conform::OPTIONAL,
    },
    Command {
        id: 0x06,
        name: "Rewind",
        to_server: true,
        response: Some(0x0A),
        access: Access::invoke(Privilege::Operate),
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(1),
            }],
        },
    },
    Command {
        id: 0x07,
        name: "FastForward",
        to_server: true,
        response: Some(0x0A),
        access: Access::invoke(Privilege::Operate),
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(1),
            }],
        },
    },
    Command {
        id: 0x08,
        name: "SkipForward",
        to_server: true,
        response: Some(0x0A),
        access: Access::invoke(Privilege::Operate),
        conform: Conform::OPTIONAL,
    },
    Command {
        id: 0x09,
        name: "SkipBackward",
        to_server: true,
        response: Some(0x0A),
        access: Access::invoke(Privilege::Operate),
        conform: Conform::OPTIONAL,
    },
    Command {
        id: 0x0A,
        name: "PlaybackResponse",
        to_server: false,
        response: None,
        access: Access::invoke(Privilege::Operate),
        conform: Conform::MANDATORY,
    },
    Command {
        id: 0x0B,
        name: "Seek",
        to_server: true,
        response: Some(0x0A),
        access: Access::invoke(Privilege::Operate),
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(0),
            }],
        },
    },
    Command {
        id: 0x0C,
        name: "ActivateAudioTrack",
        to_server: true,
        response: None,
        access: Access::invoke(Privilege::Operate),
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(3),
            }],
        },
    },
    Command {
        id: 0x0D,
        name: "ActivateTextTrack",
        to_server: true,
        response: None,
        access: Access::invoke(Privilege::Operate),
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(2),
            }],
        },
    },
    Command {
        id: 0x0E,
        name: "DeactivateTextTrack",
        to_server: true,
        response: None,
        access: Access::invoke(Privilege::Operate),
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(2),
            }],
        },
    },
];

/// Every event, with its conformance.
pub const EVENTS: &[Event] = &[Event {
    id: 0x00,
    name: "StateChanged",
    priority: EventPriority::Info,
    access: Access::read_only(Privilege::View),
    conform: Conform::OPTIONAL,
}];

/// The whole cluster, as the specification defines it.
pub const CLUSTER: Cluster = Cluster {
    id: ID,
    name: "Media Playback",
    revision: REVISION,
    pics: "MEDIAPLAYBACK",
    derived_from: None,
    features: FEATURES,
    attributes: ATTRIBUTES,
    commands: COMMANDS,
    events: EVENTS,
};
