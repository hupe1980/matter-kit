// Generated by `cargo xtask` from the CSA data model 1.6. Do not edit.
//
// Closure Control — cluster 0x0104.
//
// Regenerate with `cargo xtask clusters --dm <checkout>/data_model/1.6`; `cargo xtask check`
// is the CI gate that keeps this file honest. The XML itself is not vendored here — it
// carries the CSA's own copyright notice and is not redistributable.

//! Closure Control (cluster `0x0104`, revision 1).
//!
//! 13 identifiers, 9 feature bits, and the specification's own conformance for
//! every element. `CLUSTER` is the whole table.

#![allow(unused_imports)]

use crate::dm::conformance::{Clause, Condition, Conform, Conformance};
use crate::dm::meta::{EventPriority, Reporting};
use crate::dm::spec::{Attribute, Cluster, Command, Event, Feature};
use crate::dm::{Access, AccessQualities, AttributeQualities, Privilege};
use crate::error::Result;
use crate::im::{AttributeId, ClusterId, CommandId, EventId};
use crate::tlv::{
    ContainerKind, Element, FromTlv, Nullable, Tag, TlvList, TlvReader, TlvWriter, ToTlv, Value,
};

/// The cluster id.
pub const ID: ClusterId = 0x0104;

/// The highest revision the specification's history defines — what `ClusterRevision`
/// must report (Core §7.13.1).
pub const REVISION: u16 = 1;

/// The PICS code, which a certification declaration names the cluster by.
pub const PICS: &str = "CLCTRL";

/// The `FeatureMap` bits (Core §7.13.2).
///
/// A feature has conformance of its own: a device may not claim one the
/// specification forbids it alongside another.
pub mod feature {
    /// Supports Positioning with at least Fully Opened (0%) and Fully Closed (100%) discrete positions
    ///
    /// Code `PS`, bit 0.
    pub const POSITIONING: u32 = 1 << 0;
    /// Supports a latch (securing a position, or a state)
    ///
    /// Code `LT`, bit 1.
    pub const MOTION_LATCHING: u32 = 1 << 1;
    /// Supports the Instantaneous feature
    ///
    /// Code `IS`, bit 2.
    pub const INSTANTANEOUS: u32 = 1 << 2;
    /// Supports Speed motion throttling
    ///
    /// Code `SP`, bit 3.
    pub const SPEED: u32 = 1 << 3;
    /// Supports Ventilation discrete state
    ///
    /// Code `VT`, bit 4.
    pub const VENTILATION: u32 = 1 << 4;
    /// Supports Pedestrian discrete state
    ///
    /// Code `PD`, bit 5.
    pub const PEDESTRIAN: u32 = 1 << 5;
    /// Supports the Calibration feature
    ///
    /// Code `CL`, bit 6.
    pub const CALIBRATION: u32 = 1 << 6;
    /// Supports the Protection feature
    ///
    /// Code `PT`, bit 7.
    pub const PROTECTION: u32 = 1 << 7;
    /// Supports the manual operation feature
    ///
    /// Code `MO`, bit 8.
    pub const MANUALLY_OPERABLE: u32 = 1 << 8;
}

/// Every feature bit, with its conformance.
pub const FEATURES: &[Feature] = &[
    Feature {
        bit: 0,
        code: "PS",
        name: "Positioning",
        conform: Conform::OPTIONAL,
    },
    Feature {
        bit: 1,
        code: "LT",
        name: "MotionLatching",
        conform: Conform::OPTIONAL,
    },
    Feature {
        bit: 2,
        code: "IS",
        name: "Instantaneous",
        conform: Conform::OPTIONAL,
    },
    Feature {
        bit: 3,
        code: "SP",
        name: "Speed",
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Optional,
                when: Condition::All(&[
                    Condition::Feature(0),
                    Condition::Not(&Condition::Feature(2)),
                ]),
            }],
        },
    },
    Feature {
        bit: 4,
        code: "VT",
        name: "Ventilation",
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Optional,
                when: Condition::Feature(0),
            }],
        },
    },
    Feature {
        bit: 5,
        code: "PD",
        name: "Pedestrian",
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Optional,
                when: Condition::Feature(0),
            }],
        },
    },
    Feature {
        bit: 6,
        code: "CL",
        name: "Calibration",
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Optional,
                when: Condition::Feature(0),
            }],
        },
    },
    Feature {
        bit: 7,
        code: "PT",
        name: "Protection",
        conform: Conform::OPTIONAL,
    },
    Feature {
        bit: 8,
        code: "MO",
        name: "ManuallyOperable",
        conform: Conform::OPTIONAL,
    },
];

/// The attribute ids.
pub mod attribute {
    use crate::im::AttributeId;

    /// `CountdownTime`.
    pub const COUNTDOWN_TIME: AttributeId = 0x00;
    /// `MainState`.
    pub const MAIN_STATE: AttributeId = 0x01;
    /// `CurrentErrorList`.
    pub const CURRENT_ERROR_LIST: AttributeId = 0x02;
    /// `OverallCurrentState`.
    pub const OVERALL_CURRENT_STATE: AttributeId = 0x03;
    /// `OverallTargetState`.
    pub const OVERALL_TARGET_STATE: AttributeId = 0x04;
    /// `LatchControlModes`.
    pub const LATCH_CONTROL_MODES: AttributeId = 0x05;
}

/// The command ids.
pub mod command {
    use crate::im::CommandId;

    /// `Stop`.
    pub const STOP: CommandId = 0x00;
    /// `MoveTo`.
    pub const MOVE_TO: CommandId = 0x01;
    /// `Calibrate`.
    pub const CALIBRATE: CommandId = 0x02;
}

/// The event ids.
pub mod event {
    use crate::im::EventId;

    /// `OperationalError`.
    pub const OPERATIONAL_ERROR: EventId = 0x00;
    /// `MovementCompleted`.
    pub const MOVEMENT_COMPLETED: EventId = 0x01;
    /// `EngageStateChanged`.
    pub const ENGAGE_STATE_CHANGED: EventId = 0x02;
    /// `SecureStateChanged`.
    pub const SECURE_STATE_CHANGED: EventId = 0x03;
}

/// `ClosureErrorEnum`.
#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash)]
#[repr(u8)]
pub enum ClosureErrorEnum {
    /// An obstacle is blocking the closure movement
    PhysicallyBlocked = 0,
    /// The closure is unsafe to move, as determined by a sensor (e.g. photoelectric sensor) before attempting movement
    BlockedBySensor = 1,
    /// A warning raised by the closure that indicates an over-temperature, e.g. due to excessive drive or stall current
    TemperatureLimited = 2,
    /// Some malfunctions that are not easily recoverable are detected, or urgent servicing is needed
    MaintenanceRequired = 3,
    /// An internal element is prohibiting motion, e.g. an integrated door within a bigger garage door is open and prevents motion
    InternalInterference = 4,
}

impl ClosureErrorEnum {
    /// Reads a wire value, or `None` for one this revision does not define.
    #[must_use]
    pub const fn from_value(value: u8) -> Option<Self> {
        match value {
            0 => Some(Self::PhysicallyBlocked),
            1 => Some(Self::BlockedBySensor),
            2 => Some(Self::TemperatureLimited),
            3 => Some(Self::MaintenanceRequired),
            4 => Some(Self::InternalInterference),
            _ => None,
        }
    }

    /// Its wire value.
    #[must_use]
    pub const fn value(self) -> u8 {
        self as u8
    }
}

impl ToTlv for ClosureErrorEnum {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        self.value().to_tlv(w, tag)
    }
}

impl<'a> FromTlv<'a> for ClosureErrorEnum {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        let value = <u8 as FromTlv<'a>>::from_tlv(reader, element)?;
        Self::from_value(value).ok_or(crate::Error::new(crate::ErrorCode::TlvOutOfRange))
    }
}

/// `CurrentPositionEnum`.
#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash)]
#[repr(u8)]
pub enum CurrentPositionEnum {
    /// Fully closed state
    FullyClosed = 0,
    /// Fully opened state
    FullyOpened = 1,
    /// Partially opened state (closure is not fully opened or fully closed)
    PartiallyOpened = 2,
    /// Closure is in the Pedestrian position
    OpenedForPedestrian = 3,
    /// Closure is in the Ventilation position
    OpenedForVentilation = 4,
    /// Closure is in its "Signature position"
    OpenedAtSignature = 5,
}

impl CurrentPositionEnum {
    /// Reads a wire value, or `None` for one this revision does not define.
    #[must_use]
    pub const fn from_value(value: u8) -> Option<Self> {
        match value {
            0 => Some(Self::FullyClosed),
            1 => Some(Self::FullyOpened),
            2 => Some(Self::PartiallyOpened),
            3 => Some(Self::OpenedForPedestrian),
            4 => Some(Self::OpenedForVentilation),
            5 => Some(Self::OpenedAtSignature),
            _ => None,
        }
    }

    /// Its wire value.
    #[must_use]
    pub const fn value(self) -> u8 {
        self as u8
    }
}

impl ToTlv for CurrentPositionEnum {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        self.value().to_tlv(w, tag)
    }
}

impl<'a> FromTlv<'a> for CurrentPositionEnum {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        let value = <u8 as FromTlv<'a>>::from_tlv(reader, element)?;
        Self::from_value(value).ok_or(crate::Error::new(crate::ErrorCode::TlvOutOfRange))
    }
}

/// `MainStateEnum`.
#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash)]
#[repr(u8)]
pub enum MainStateEnum {
    /// Closure is stopped
    Stopped = 0,
    /// Closure is actively moving
    Moving = 1,
    /// Closure is waiting before a motion (e.g. pre-heat, pre-check)
    WaitingForMotion = 2,
    /// Closure is in an error state
    Error = 3,
    /// Closure is currently calibrating its Opened and Closed limits to determine effective physical range
    Calibrating = 4,
    /// Some protective measures are activated to prevent damage to the closure. Commands MAY be rejected.
    Protected = 5,
    /// Closure has a disengaged element preventing any actuator movements
    Disengaged = 6,
    /// Movement commands are ignored since the closure is not operational and requires further setup and/or calibration
    SetupRequired = 7,
}

impl MainStateEnum {
    /// Reads a wire value, or `None` for one this revision does not define.
    #[must_use]
    pub const fn from_value(value: u8) -> Option<Self> {
        match value {
            0 => Some(Self::Stopped),
            1 => Some(Self::Moving),
            2 => Some(Self::WaitingForMotion),
            3 => Some(Self::Error),
            4 => Some(Self::Calibrating),
            5 => Some(Self::Protected),
            6 => Some(Self::Disengaged),
            7 => Some(Self::SetupRequired),
            _ => None,
        }
    }

    /// Its wire value.
    #[must_use]
    pub const fn value(self) -> u8 {
        self as u8
    }
}

impl ToTlv for MainStateEnum {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        self.value().to_tlv(w, tag)
    }
}

impl<'a> FromTlv<'a> for MainStateEnum {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        let value = <u8 as FromTlv<'a>>::from_tlv(reader, element)?;
        Self::from_value(value).ok_or(crate::Error::new(crate::ErrorCode::TlvOutOfRange))
    }
}

/// `TargetPositionEnum`.
#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash)]
#[repr(u8)]
pub enum TargetPositionEnum {
    /// Move to a fully closed state
    MoveToFullyClosed = 0,
    /// Move to a fully open state
    MoveToFullyOpen = 1,
    /// Move to the Pedestrian position
    MoveToPedestrianPosition = 2,
    /// Move to the Ventilation position
    MoveToVentilationPosition = 3,
    /// Move to the Signature position
    MoveToSignaturePosition = 4,
}

impl TargetPositionEnum {
    /// Reads a wire value, or `None` for one this revision does not define.
    #[must_use]
    pub const fn from_value(value: u8) -> Option<Self> {
        match value {
            0 => Some(Self::MoveToFullyClosed),
            1 => Some(Self::MoveToFullyOpen),
            2 => Some(Self::MoveToPedestrianPosition),
            3 => Some(Self::MoveToVentilationPosition),
            4 => Some(Self::MoveToSignaturePosition),
            _ => None,
        }
    }

    /// Its wire value.
    #[must_use]
    pub const fn value(self) -> u8 {
        self as u8
    }
}

impl ToTlv for TargetPositionEnum {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        self.value().to_tlv(w, tag)
    }
}

impl<'a> FromTlv<'a> for TargetPositionEnum {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        let value = <u8 as FromTlv<'a>>::from_tlv(reader, element)?;
        Self::from_value(value).ok_or(crate::Error::new(crate::ErrorCode::TlvOutOfRange))
    }
}

bitflags::bitflags! {
    /// `LatchControlModesBitmap`.
    #[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Default)]
    pub struct LatchControlModesBitmap: u8 {
        /// Remote latching capability
        const REMOTE_LATCHING = 1 << 0;
        /// Remote unlatching capability
        const REMOTE_UNLATCHING = 1 << 1;
    }
}

impl ToTlv for LatchControlModesBitmap {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        self.bits().to_tlv(w, tag)
    }
}

impl<'a> FromTlv<'a> for LatchControlModesBitmap {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        let bits = <u8 as FromTlv<'a>>::from_tlv(reader, element)?;
        Self::from_bits(bits).ok_or(crate::Error::new(crate::ErrorCode::TlvOutOfRange))
    }
}

/// `OverallCurrentStateStruct`.
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct OverallCurrentStateStruct {
    /// `Position`, field 0.
    pub position: Option<Nullable<CurrentPositionEnum>>,
    /// `Latch`, field 1.
    pub latch: Option<Nullable<bool>>,
    /// `Speed`, field 2.
    pub speed: Option<crate::clusters::generated::globals::ThreeLevelAutoEnum>,
    /// `SecureState`, field 3.
    pub secure_state: Nullable<bool>,
}

impl ToTlv for OverallCurrentStateStruct {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        if let Some(value) = &self.position {
            value.to_tlv(w, Tag::Context(0))?;
        }
        if let Some(value) = &self.latch {
            value.to_tlv(w, Tag::Context(1))?;
        }
        if let Some(value) = &self.speed {
            value.to_tlv(w, Tag::Context(2))?;
        }
        self.secure_state.to_tlv(w, Tag::Context(3))?;
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for OverallCurrentStateStruct {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut position = None;
        let mut latch = None;
        let mut speed = None;
        let mut secure_state = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => {
                    position = Some(<Nullable<CurrentPositionEnum>>::from_tlv(reader, &field)?)
                }
                Some(1) => latch = Some(<Nullable<bool>>::from_tlv(reader, &field)?),
                Some(2) => {
                    speed = Some(
                        <crate::clusters::generated::globals::ThreeLevelAutoEnum>::from_tlv(
                            reader, &field,
                        )?,
                    )
                }
                Some(3) => secure_state = Some(<Nullable<bool>>::from_tlv(reader, &field)?),
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            position,
            latch,
            speed,
            secure_state: secure_state.ok_or(crate::Error::new(crate::ErrorCode::TlvNotFound))?,
        })
    }
}

/// `OverallTargetStateStruct`.
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct OverallTargetStateStruct {
    /// `Position`, field 0.
    pub position: Option<Nullable<TargetPositionEnum>>,
    /// `Latch`, field 1.
    pub latch: Option<Nullable<bool>>,
    /// `Speed`, field 2.
    pub speed: Option<crate::clusters::generated::globals::ThreeLevelAutoEnum>,
}

impl ToTlv for OverallTargetStateStruct {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        if let Some(value) = &self.position {
            value.to_tlv(w, Tag::Context(0))?;
        }
        if let Some(value) = &self.latch {
            value.to_tlv(w, Tag::Context(1))?;
        }
        if let Some(value) = &self.speed {
            value.to_tlv(w, Tag::Context(2))?;
        }
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for OverallTargetStateStruct {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut position = None;
        let mut latch = None;
        let mut speed = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => {
                    position = Some(<Nullable<TargetPositionEnum>>::from_tlv(reader, &field)?)
                }
                Some(1) => latch = Some(<Nullable<bool>>::from_tlv(reader, &field)?),
                Some(2) => {
                    speed = Some(
                        <crate::clusters::generated::globals::ThreeLevelAutoEnum>::from_tlv(
                            reader, &field,
                        )?,
                    )
                }
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            position,
            latch,
            speed,
        })
    }
}

/// The fields of `MoveTo` (command `0x01`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct MoveToFields {
    /// `Position`, field 0.
    pub position: Option<TargetPositionEnum>,
    /// `Latch`, field 1.
    pub latch: Option<bool>,
    /// `Speed`, field 2.
    pub speed: Option<crate::clusters::generated::globals::ThreeLevelAutoEnum>,
}

impl ToTlv for MoveToFields {
    fn to_tlv(&self, w: &mut TlvWriter<'_>, tag: Tag) -> Result<()> {
        w.start_structure(tag)?;
        if let Some(value) = &self.position {
            value.to_tlv(w, Tag::Context(0))?;
        }
        if let Some(value) = &self.latch {
            value.to_tlv(w, Tag::Context(1))?;
        }
        if let Some(value) = &self.speed {
            value.to_tlv(w, Tag::Context(2))?;
        }
        w.end_container()
    }
}

impl<'a> FromTlv<'a> for MoveToFields {
    fn from_tlv(reader: &mut TlvReader<'a>, element: &Element<'a>) -> Result<Self> {
        if element.value.container() != Some(ContainerKind::Structure) {
            return Err(crate::Error::new(crate::ErrorCode::TlvWrongType));
        }
        let mut position = None;
        let mut latch = None;
        let mut speed = None;
        loop {
            let Some(field) = reader.next_element()? else {
                return Err(crate::Error::new(crate::ErrorCode::TlvTruncated));
            };
            if field.value == Value::EndOfContainer {
                break;
            }
            match field.tag.context() {
                Some(0) => position = Some(<TargetPositionEnum>::from_tlv(reader, &field)?),
                Some(1) => latch = Some(<bool>::from_tlv(reader, &field)?),
                Some(2) => {
                    speed = Some(
                        <crate::clusters::generated::globals::ThreeLevelAutoEnum>::from_tlv(
                            reader, &field,
                        )?,
                    )
                }
                // A field this revision does not define. §7.19.2: a receiver
                // ignores what it does not know rather than refusing the whole
                // structure, which is what lets a newer peer talk to an older one.
                _ => reader.skip_value(&field)?,
            }
        }
        Ok(Self {
            position,
            latch,
            speed,
        })
    }
}

/// Every attribute the specification defines, with its conformance.
pub const ATTRIBUTES: &[Attribute] = &[
    Attribute {
        id: 0x0000,
        name: "CountdownTime",
        kind: "elapsed-s",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::NULLABLE,
        reporting: Reporting::Quieter,
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Optional,
                when: Condition::All(&[
                    Condition::Feature(0),
                    Condition::Not(&Condition::Feature(2)),
                ]),
            }],
        },
    },
    Attribute {
        id: 0x0001,
        name: "MainState",
        kind: "MainStateEnum",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::empty(),
        reporting: Reporting::OnChange,
        conform: Conform::MANDATORY,
    },
    Attribute {
        id: 0x0002,
        name: "CurrentErrorList",
        kind: "list[ClosureErrorEnum]",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::empty(),
        reporting: Reporting::OnChange,
        conform: Conform::MANDATORY,
    },
    Attribute {
        id: 0x0003,
        name: "OverallCurrentState",
        kind: "OverallCurrentStateStruct",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::NULLABLE,
        reporting: Reporting::OnChange,
        conform: Conform::MANDATORY,
    },
    Attribute {
        id: 0x0004,
        name: "OverallTargetState",
        kind: "OverallTargetStateStruct",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::NULLABLE,
        reporting: Reporting::OnChange,
        conform: Conform::MANDATORY,
    },
    Attribute {
        id: 0x0005,
        name: "LatchControlModes",
        kind: "LatchControlModesBitmap",
        access: Access::read_only(Privilege::View),
        qualities: AttributeQualities::FIXED,
        reporting: Reporting::OnChange,
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(1),
            }],
        },
    },
];

/// Every command, with its conformance.
pub const COMMANDS: &[Command] = &[
    Command {
        id: 0x00,
        name: "Stop",
        to_server: true,
        response: None,
        access: Access::invoke(Privilege::Operate),
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Not(&Condition::Feature(2)),
            }],
        },
    },
    Command {
        id: 0x01,
        name: "MoveTo",
        to_server: true,
        response: None,
        access: Access::invoke(Privilege::Operate).with_qualities(AccessQualities::TIMED),
        conform: Conform::MANDATORY,
    },
    Command {
        id: 0x02,
        name: "Calibrate",
        to_server: true,
        response: None,
        access: Access::invoke(Privilege::Manage).with_qualities(AccessQualities::TIMED),
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(6),
            }],
        },
    },
];

/// Every event, with its conformance.
pub const EVENTS: &[Event] = &[
    Event {
        id: 0x00,
        name: "OperationalError",
        priority: EventPriority::Critical,
        access: Access::read_only(Privilege::View),
        conform: Conform::MANDATORY,
    },
    Event {
        id: 0x01,
        name: "MovementCompleted",
        priority: EventPriority::Info,
        access: Access::read_only(Privilege::View),
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Not(&Condition::Feature(2)),
            }],
        },
    },
    Event {
        id: 0x02,
        name: "EngageStateChanged",
        priority: EventPriority::Info,
        access: Access::read_only(Privilege::View),
        conform: Conform {
            clauses: &[Clause {
                verdict: Conformance::Mandatory,
                when: Condition::Feature(8),
            }],
        },
    },
    Event {
        id: 0x03,
        name: "SecureStateChanged",
        priority: EventPriority::Info,
        access: Access::read_only(Privilege::View),
        conform: Conform::MANDATORY,
    },
];

/// The whole cluster, as the specification defines it.
pub const CLUSTER: Cluster = Cluster {
    id: ID,
    name: "Closure Control",
    revision: REVISION,
    pics: "CLCTRL",
    derived_from: None,
    features: FEATURES,
    attributes: ATTRIBUTES,
    commands: COMMANDS,
    events: EVENTS,
};
